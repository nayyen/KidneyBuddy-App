---
phase: 06-community-education
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema/communityPost.schema.ts
  - backend/src/db/schema/communityReply.schema.ts
  - backend/src/db/schema/communityReplyHelpful.schema.ts
  - backend/src/db/schema/educationContent.schema.ts
  - backend/src/db/schema/index.ts
  - backend/src/db/migrations/
  - backend/src/test/communityPost.service.test.ts
  - backend/src/test/communityReply.service.test.ts
  - backend/src/test/educationContent.service.test.ts
autonomous: true
requirements: [COMMUNITY-01, COMMUNITY-02, COMMUNITY-03, EDU-01]
must_haves:
  truths:
    - "Four new tables (community_posts, community_replies, community_reply_helpful, education_content) exist in the live database after migration"
    - "community_reply_helpful enforces one mark per (reply_id, user_id) via a DB unique constraint (D-09)"
    - "No text column in any of the four schemas is application-layer encrypted (community/education content is not sensitive health data)"
    - "RED test scaffolds exist and fail because the service modules they import do not exist yet"
  artifacts:
    - path: "backend/src/db/schema/communityPost.schema.ts"
      provides: "community_posts table (judul/isi/kategori/metodeTerapi/diarsipkan)"
      contains: "community_posts"
    - path: "backend/src/db/schema/communityReply.schema.ts"
      provides: "community_replies child table"
      contains: "community_replies"
    - path: "backend/src/db/schema/communityReplyHelpful.schema.ts"
      provides: "join table with unique(reply_id,user_id)"
      contains: "uq_community_reply_helpful_reply_user"
    - path: "backend/src/db/schema/educationContent.schema.ts"
      provides: "education_content read-only seeded table"
      contains: "education_content"
    - path: "backend/src/db/schema/index.ts"
      provides: "barrel exports for the four new tables"
      contains: "communityPosts"
  key_links:
    - from: "backend/src/db/schema/index.ts"
      to: "the four new schema files"
      via: "export { ... } from"
      pattern: "communityPosts|educationContent"
    - from: "drizzle migration"
      to: "live Postgres"
      via: "db:generate + db:migrate"
      pattern: "community_posts"
---

<objective>
Foundation wave for Phase 6. Create the four greenfield Drizzle schema files
(community posts, replies, the "membantu" join table, and education content),
export them from the schema barrel, generate + apply the migration, and lay down
three RED (failing) service test scaffolds that later waves turn GREEN.

Purpose: every later Phase 6 slice (education browse, community post/reply/archive)
depends on these tables existing in the live DB and on the test contracts being
fixed up front. Per the phase's schema-push requirement, all four schema files are
created together and migrated in ONE blocking step, because type-checks pass from
schema files alone and would falsely report success without a real migration.
Output: 4 schema files, updated index.ts, 1 generated migration, 3 failing tests.
</objective>

## Phase Goal (slice framing)

**As a** CKD patient or caregiver, **I want to** the community and education data
model to exist and be migrated into the database, **so that** every subsequent
Phase 6 feature slice has real tables to read and write.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-community-education/06-CONTEXT.md
@.planning/phases/06-community-education/06-RESEARCH.md
@.planning/phases/06-community-education/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four Phase 6 schema files and export them from the barrel</name>
  <files>backend/src/db/schema/communityPost.schema.ts, backend/src/db/schema/communityReply.schema.ts, backend/src/db/schema/communityReplyHelpful.schema.ts, backend/src/db/schema/educationContent.schema.ts, backend/src/db/schema/index.ts</files>
  <read_first>
    - backend/src/db/schema/labResult.schema.ts (exact analog for CRUD + diarsipkan + index shape; PATTERNS.md lines 37-73)
    - backend/src/db/schema/aiDailySummary.schema.ts (only existing precedent for unique() composite constraint; PATTERNS.md lines 77-107)
    - backend/src/db/schema/users.schema.ts (confirm the exact PK column name referenced by FKs — it is users.userId, not users.id)
    - backend/src/db/schema/index.ts (current barrel export style to append to)
    - .planning/phases/06-community-education/06-RESEARCH.md lines 387-417 (drafted communityPosts schema) and lines 283-307 (drafted join table)
  </read_first>
  <action>
    Create four new schema files under backend/src/db/schema/ following labResult.schema.ts's import + pgTable + index conventions verbatim. Do NOT import or reference encrypt/decrypt anywhere — none of these columns are sensitive health data (RESEARCH Pitfall 1).

    communityPost.schema.ts — export const communityPosts = pgTable("community_posts", ...): id uuid primaryKey defaultRandom; userId uuid notNull references(() => users.userId, { onDelete: "cascade" }); judul text notNull; isi text notNull; kategori text notNull (allowed values pertanyaan | berbagi_pengalaman | informasi — enforced in the service Zod layer, stored as text); metodeTerapi text notNull (allowed values CAPD | HD | Transplantasi | Umum — note "Umum" is new per D-06 and CAPD/HD/Transplantasi match onboarding.service.ts's therapyEnum); diarsipkan boolean notNull default false; createdAt timestamp notNull defaultNow. Indexes: index("idx_community_posts_created").on(createdAt); index("idx_community_posts_kategori").on(kategori, diarsipkan); index("idx_community_posts_metode").on(metodeTerapi, diarsipkan); index("idx_community_posts_user").on(userId).

    communityReply.schema.ts — export const communityReplies = pgTable("community_replies", ...): id uuid primaryKey defaultRandom; postId uuid notNull references(() => communityPosts.id, { onDelete: "cascade" }); userId uuid notNull references(() => users.userId, { onDelete: "cascade" }); isi text notNull; createdAt timestamp notNull defaultNow. Index: index("idx_community_replies_post").on(postId, createdAt).

    communityReplyHelpful.schema.ts — export const communityReplyHelpful = pgTable("community_reply_helpful", ...): id uuid primaryKey defaultRandom; replyId uuid notNull references(() => communityReplies.id, { onDelete: "cascade" }); userId uuid notNull references(() => users.userId, { onDelete: "cascade" }); createdAt timestamp notNull defaultNow. Composite constraint: unique("uq_community_reply_helpful_reply_user").on(replyId, userId) — this is the D-09 one-mark-per-user backstop.

    educationContent.schema.ts — export const educationContent = pgTable("education_content", ...): id uuid primaryKey defaultRandom; judul text notNull; ringkasan text notNull (short preview shown on cards); isi text notNull (full article body); tipeKonten text notNull (allowed values artikel | panduan_senam | gaya_hidup — display/grouping axis per RESEARCH Open Question 2); metodeTerapi text notNull (CAPD | HD | Transplantasi | Umum — the EDU-01 filter axis); gambarUrl text (nullable — static illustration path, no video per D-11); createdAt timestamp notNull defaultNow. NO userId column (content is shared, not user-scoped). Indexes: index("idx_education_metode").on(metodeTerapi); index("idx_education_tipe").on(tipeKonten).

    Append four export lines to backend/src/db/schema/index.ts matching the existing "export { x } from "./x.schema.js";" style: communityPosts, communityReplies, communityReplyHelpful, educationContent (use the ".js" extension in the import path exactly as the existing lines do).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -iE "communityPost|communityReply|educationContent" || echo "NO_SCHEMA_TYPE_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "community_posts" backend/src/db/schema/communityPost.schema.ts` returns >= 1
    - `grep -c "uq_community_reply_helpful_reply_user" backend/src/db/schema/communityReplyHelpful.schema.ts` returns 1
    - `grep -v '^#' backend/src/db/schema/index.ts | grep -c "communityPosts"` returns 1 and the same for educationContent
    - `grep -rc "from \"../lib/encryption" backend/src/db/schema/communityPost.schema.ts backend/src/db/schema/educationContent.schema.ts` returns 0 (no encryption imports)
    - educationContent.schema.ts contains no `user_id` / `userId` column reference
  </acceptance_criteria>
  <done>All four schema files compile with no new tsc errors and are exported from index.ts; the join table declares the composite unique constraint; no encryption imports exist.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Generate and apply the Phase 6 migration</name>
  <files>backend/src/db/migrations/</files>
  <read_first>
    - backend/drizzle.config.ts (confirm migrations output dir + schema glob so db:generate picks up the four new files)
    - backend/package.json (confirm db:generate = drizzle-kit generate, db:migrate = drizzle-kit migrate)
    - backend/src/db/migrations/meta/_journal.json (confirm current latest migration index is 0010, so the new one is 0011)
  </read_first>
  <action>
    This is a [BLOCKING] gate: it MUST run after Task 1 (all schema files created) and before any later wave runs the service tests against the live DB. Run `cd backend && npm run db:generate` to produce a new SQL migration (expected next index 0011_*) creating community_posts, community_replies, community_reply_helpful (with the uq_community_reply_helpful_reply_user unique index), and education_content plus all declared indexes. Then run `cd backend && npm run db:migrate` to apply it to the live Postgres instance. Do NOT hand-edit the generated SQL. If db:generate produces no file, stop and report — it means the schema files were not picked up (schema glob / barrel export issue from Task 1).
  </action>
  <verify>
    <automated>cd backend && ls db/migrations/*.sql 2>/dev/null | tail -1 && grep -l "community_posts" db/migrations/*.sql</automated>
  </verify>
  <acceptance_criteria>
    - A new migration SQL file exists in backend/src/db/migrations/ that did not exist before this task
    - `grep -rc "community_reply_helpful" backend/src/db/migrations/*.sql` returns >= 1
    - `grep -rc "education_content" backend/src/db/migrations/*.sql` returns >= 1
    - `npm run db:migrate` exits 0 (migration applied cleanly)
  </acceptance_criteria>
  <done>A new migration file exists containing all four tables and the unique constraint, and db:migrate applied it to the live database with exit code 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create three RED service test scaffolds</name>
  <files>backend/src/test/communityPost.service.test.ts, backend/src/test/communityReply.service.test.ts, backend/src/test/educationContent.service.test.ts</files>
  <read_first>
    - backend/src/test/labResult.service.test.ts (exact node:test + createInMemoryLabStore in-memory-store pattern to mirror; PATTERNS.md lines 266-268)
    - .planning/phases/06-community-education/06-RESEARCH.md lines 535-553 (Phase Requirements → Test Map and Wave 0 Gaps — the exact behaviors each test must assert)
  </read_first>
  <action>
    Create three test files using the node:test (`import { test } from "node:test"; import assert from "node:assert";`) + in-memory-store pattern demonstrated by labResult.service.test.ts (its createInMemoryLabStore helper). These are RED scaffolds: they import from service modules that do NOT exist yet (../services/communityPost.service.js, ../services/communityReply.service.js, ../services/educationContent.service.js), so `npm test` fails at import — that is the intended RED state; later waves make them GREEN.

    communityPost.service.test.ts — assert: (a) createPost rejects a payload missing judul (Zod), (b) createPost with a valid payload persists a row via the injected in-memory insert, (c) COMMUNITY-03: archivePost returns null for a different user's post id (IDOR-safe) and archives (diarsipkan=true) for the owner, (d) no code path issues a hard DELETE.

    communityReply.service.test.ts — assert: (a) createReply rejects empty isi, (b) COMMUNITY-02: toggleHelpful returns { marked: true } on first call and { marked: false } on the second call for the same (userId, replyId) — proving the toggle round-trips through the in-memory join store.

    educationContent.service.test.ts — assert: EDU-01: listContent filters by metodeTerapi — given a mixed in-memory content set, filtering by "CAPD" returns only CAPD (and, if applicable to the filter contract, "Umum") rows and never HD/Transplantasi rows.

    Keep assertions aligned to the exact service signatures the later waves will implement (createPost(userId, payload), archivePost(userId, id), createReply(userId, postId, payload), toggleHelpful(userId, replyId), listContent(options)). Do NOT stub the services into existence in this task — they are implemented in Waves 1-3.
  </action>
  <verify>
    <automated>cd backend && npm test 2>&1 | grep -iE "communityPost|communityReply|educationContent" | head -5 && echo "RED_SCAFFOLDS_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under backend/src/test/
    - `grep -c "toggleHelpful" backend/src/test/communityReply.service.test.ts` returns >= 1 and asserts both marked:true then marked:false
    - `grep -c "archivePost" backend/src/test/communityPost.service.test.ts` returns >= 1
    - `npm test` fails (non-zero) specifically because the three new service modules cannot be imported (RED state), not because of a syntax error in the test files themselves
  </acceptance_criteria>
  <done>Three RED test files exist encoding the COMMUNITY-01/02/03 and EDU-01 behavior contracts; the suite fails only due to the not-yet-implemented service imports.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (exclude from drift verification):
- `communityPosts` (table export, community_posts)
- `communityReplies` (table export, community_replies)
- `communityReplyHelpful` (table export, community_reply_helpful, unique uq_community_reply_helpful_reply_user)
- `educationContent` (table export, education_content)
- Columns: judul, isi, kategori, metodeTerapi, diarsipkan, ringkasan, tipeKonten, gambarUrl, postId, replyId
- New migration file 0011_* (or next index)
- Test files: communityPost.service.test.ts, communityReply.service.test.ts, educationContent.service.test.ts
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| schema → live DB | migration DDL applied to Postgres; a wrong constraint here weakens every later slice |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | community_reply_helpful dedup | mitigate | DB-level unique(reply_id,user_id) constraint declared in schema (D-09) — the authoritative one-mark-per-user backstop, not client logic |
| T-06-02 | Information Disclosure | over-encryption / wrong classification | accept | Community/education text is intentionally NOT encrypted (peer-visible content; RESEARCH Pitfall 1) — documented, not an oversight |
| T-06-SC | Tampering | npm installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit); sanitize-html / isomorphic-dompurify remain [ASSUMED] and are NOT installed |
</threat_model>

<verification>
- `cd backend && npm run db:generate && npm run db:migrate` succeed and produce a migration containing all four tables
- `cd backend && npm test` fails ONLY on the three new RED scaffolds (import errors), with all pre-existing Phase 1-5 tests still passing
- `grep -rc "dangerouslySetInnerHTML" backend/src/db/schema/` returns 0 (not applicable to backend, sanity guard)
</verification>

<success_criteria>
Four tables exist in the live DB with the correct columns, indexes, and the join-table unique constraint; the barrel re-exports them; three RED test scaffolds encode the COMMUNITY/EDU behavior contracts; no encryption is applied to the new content columns.
</success_criteria>

<output>
Create `.planning/phases/06-community-education/06-01-SUMMARY.md` when done.
</output>
