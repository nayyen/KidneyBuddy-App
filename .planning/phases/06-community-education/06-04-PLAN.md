---
phase: 06-community-education
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - backend/src/repositories/communityPost.repository.ts
  - backend/src/services/communityPost.service.ts
  - backend/src/controllers/community.controller.ts
  - backend/src/routes/community.routes.ts
  - backend/src/app.ts
autonomous: true
requirements: [COMMUNITY-01, COMMUNITY-03]
must_haves:
  truths:
    - "POST /api/community creates a post with judul/isi/kategori/metodeTerapi for the authenticated user (COMMUNITY-01)"
    - "GET /api/community returns non-archived posts newest-first, filterable by kategori and metodeTerapi (D-05/D-06/D-07)"
    - "GET /api/community/:id returns a post with an isMine flag derived server-side from req.user.id"
    - "PATCH /api/community/:id/archive archives only the caller's own post; another user's attempt returns 404 (COMMUNITY-03, IDOR-safe)"
    - "No repository path issues a hard DELETE against community_posts"
    - "communityPost.service.test.ts passes (GREEN)"
  artifacts:
    - path: "backend/src/repositories/communityPost.repository.ts"
      provides: "create, findFeed, findById, archiveById (userId-first)"
      exports: ["create", "findFeed", "findById", "archiveById"]
    - path: "backend/src/services/communityPost.service.ts"
      provides: "createPostSchema, createPost, listFeed, getPostDetail, archivePost"
      exports: ["createPost", "listFeed", "getPostDetail", "archivePost"]
    - path: "backend/src/routes/community.routes.ts"
      provides: "post CRUD routes behind authenticate"
  key_links:
    - from: "backend/src/app.ts"
      to: "community.routes"
      via: "app.use(\"/api/community\", communityRoutes)"
      pattern: "api/community"
    - from: "communityPost.repository.archiveById"
      to: "community_posts"
      via: "and(eq(userId), eq(id)) WHERE clause"
      pattern: "and\\(eq"
---

<objective>
Community vertical (post backend): deliver create / feed / detail / archive for
community posts. After this plan a user (via API) can create a post, see it in a
filtered newest-first feed, and archive their own post — the COMMUNITY-01 and
COMMUNITY-03 capabilities are real at the API layer; replies (06-05) and the feed
UI (06-06) build on top.

Purpose: COMMUNITY-01 (create post) and COMMUNITY-03 (archive-own, never
hard-delete) as an authenticated, IDOR-safe API following the labResult layering
exactly. Output: repository + service + community controller (post handlers) +
routes + app.ts mount.
</objective>

## Phase Goal (slice framing)

**As a** CKD patient or caregiver, **I want to** create posts and browse a
filtered community feed via the API, **so that** I can share and find peer
experiences; **and** archive my own posts without ever losing them permanently.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-community-education/06-CONTEXT.md
@.planning/phases/06-community-education/06-RESEARCH.md
@.planning/phases/06-community-education/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: communityPost repository + service (create/feed/detail/archive, GREEN test)</name>
  <files>backend/src/repositories/communityPost.repository.ts, backend/src/services/communityPost.service.ts</files>
  <behavior>
    - createPost with a valid payload persists a row scoped to the passed userId
    - createPost rejects a payload missing judul (Zod validation error)
    - listFeed with no filter returns only non-archived posts, newest-first
    - listFeed({ kategori, metodeTerapi }) applies both filters in SQL
    - archivePost(ownerId, id) sets diarsipkan=true and returns the row
    - archivePost(otherUserId, id) returns null (IDOR-safe) — no cross-user archive
    - No code path calls a hard DELETE
  </behavior>
  <read_first>
    - backend/src/repositories/labResult.repository.ts lines 29-108 (findByUser options, archiveById IDOR guard, findById; PATTERNS.md lines 111-148)
    - backend/src/services/labResult.service.ts lines 1-90 (Zod + injectable-core seam; PATTERNS.md lines 173-198)
    - backend/src/test/communityPost.service.test.ts (the RED contract from 06-01 to turn GREEN)
    - backend/src/db/schema/communityPost.schema.ts (created in 06-01)
    - .planning/phases/06-community-education/06-RESEARCH.md lines 419-442 (drafted findFeed) and lines 263-281 (archiveById IDOR pattern)
  </read_first>
  <action>
    Create communityPost.repository.ts exporting: create(row) inserting into communityPosts and returning the inserted row; findFeed(options?: { kategori?: string; metodeTerapi?: string }) building a conditions array whose BASE condition is eq(communityPosts.diarsipkan, false) (feed is public across all users — NOT userId-scoped), pushing eq(kategori) and eq(metodeTerapi) when provided, returning db.select().from(communityPosts).where(and(...conditions)).orderBy(desc(communityPosts.createdAt)) for D-07 newest-first; findById(id) single-row lookup; archiveById(userId, id) copying labResult.repository.ts::archiveById VERBATIM (substitute communityPosts) — update set diarsipkan=true where and(eq(communityPosts.userId, userId), eq(communityPosts.id, id)) returning row ?? null. Export NewCommunityPost / CommunityPost inferred types. Do NOT write any db.delete(communityPosts) call anywhere.

    Create communityPost.service.ts with createPostSchema = z.object({ judul: string min1 max200 with Indonesian required_error/messages, isi: string min1 max5000, kategori: z.enum(["pertanyaan","berbagi_pengalaman","informasi"]), metodeTerapi: z.enum(["CAPD","HD","Transplantasi","Umum"]) }). Export createPost(userId, payload) (validate then repository.create with userId), listFeed(options) (delegate to findFeed; optionally validate filter enums), getPostDetail(currentUserId, id) (findById; return null when not found; map the row to include isMine = row.userId === currentUserId), archivePost(userId, id) (delegate to archiveById; returns null → controller 404). Follow the injectable-core seam (accept optional deps defaulting to the real repository) so the test uses an in-memory store. Do NOT import encrypt/decrypt (RESEARCH Pitfall 1). pino logger named "communityPost.service".
  </action>
  <verify>
    <automated>cd backend && node --import tsx --test src/test/communityPost.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - communityPost.service.test.ts passes (exit 0), including the cross-user archive-returns-null case
    - `grep -c "db.delete" backend/src/repositories/communityPost.repository.ts` returns 0 (never hard-delete, COMMUNITY-03)
    - `grep -Ec "and\(eq\(.*userId.*\), eq\(.*id" backend/src/repositories/communityPost.repository.ts` returns >= 1 (compound IDOR WHERE on archive)
    - `grep -c "from \"../lib/encryption" backend/src/services/communityPost.service.ts` returns 0
    - findFeed base condition is diarsipkan=false and is NOT scoped to a single userId
  </acceptance_criteria>
  <done>Repository + service implement create/feed/detail/archive with an IDOR-safe archive, no hard delete, public newest-first filtered feed, and a GREEN service test.</done>
</task>

<task type="auto">
  <name>Task 2: community controller (post handlers) + routes + app.ts mount</name>
  <files>backend/src/controllers/community.controller.ts, backend/src/routes/community.routes.ts, backend/src/app.ts</files>
  <read_first>
    - backend/src/controllers/labResult.controller.ts lines 19-96 (create/list/archive thin-handler templates; PATTERNS.md lines 202-224)
    - backend/src/routes/labResult.routes.ts (authenticate-on-every-route; PATTERNS.md lines 229-241)
    - backend/src/app.ts lines 6-19 and 49-61 (import + mount blocks)
  </read_first>
  <action>
    Create community.controller.ts with thin handlers (each try/catch → next(err), no Zod, no db): createPost(req,res) → communityPostService.createPost(req.user.id, req.body), res.status(201).json(row); listPosts(req,res) → listFeed({ kategori: req.query.kategori, metodeTerapi: req.query.metodeTerapi }), res.json({ posts }); getPost(req,res) → getPostDetail(req.user.id, req.params.id); 404 { code:"NOT_FOUND", message:"Postingan tidak ditemukan" } when null else res.json(row); archivePost(req,res) → archivePost(req.user.id, req.params.id); 404 with the same message when null else res.json(row). (Reply handlers are added by 06-05 — leave room; do not implement them here.)

    Create community.routes.ts: Router with router.post("/", authenticate, community.controller.createPost); router.get("/", authenticate, community.controller.listPosts); router.get("/:id", authenticate, community.controller.getPost); router.patch("/:id/archive", authenticate, community.controller.archivePost); export default. Order the specific "/:id/archive" and "/:id" routes so Express matches correctly (archive PATCH is distinct method, safe).

    Modify app.ts: add `import communityRoutes from "./routes/community.routes.js";` to the import block and `app.use("/api/community", communityRoutes);` to the mount block. NOTE: 06-02 also edits app.ts (education mount); this plan is sequenced after 06-02 to avoid a merge conflict — add the community line without disturbing the education line.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -iE "community" || echo "OK" && grep -c "api/community" src/app.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/community" backend/src/app.ts` returns 1 AND `grep -c "api/education" backend/src/app.ts` still returns 1 (education mount preserved)
    - `grep -c "authenticate" backend/src/routes/community.routes.ts` returns 4 (all four routes guarded)
    - `grep -c "Postingan tidak ditemukan" backend/src/controllers/community.controller.ts` returns 2 (getPost + archivePost 404s)
    - Controller handlers contain no z. / Zod calls and no direct repository/db imports
  </acceptance_criteria>
  <done>POST/GET/GET:id/PATCH:id/archive for community posts are mounted at /api/community, all authenticated, delegating through thin controllers.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `create`, `findFeed`, `findById`, `archiveById` (communityPost.repository.ts)
- `createPostSchema`, `createPost`, `listFeed`, `getPostDetail`, `archivePost` (communityPost.service.ts)
- `createPost`, `listPosts`, `getPost`, `archivePost` (community.controller.ts)
- `communityRoutes` default export (community.routes.ts)
- app.ts mount `/api/community`
- `isMine` server-derived flag on post detail responses
- Endpoints: POST /api/community, GET /api/community, GET /api/community/:id, PATCH /api/community/:id/archive
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → /api/community | authenticated create/read/archive of user-generated posts |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering / Elevation of Privilege | archive-own-post (IDOR) | mitigate | archiveById uses compound and(eq(userId), eq(id)) WHERE; cross-user id returns null → 404, never archives another user's post (RESEARCH Pitfall 2) |
| T-06-09 | Denial of Service | unbounded judul/isi length | mitigate | createPostSchema enforces judul .max(200) and isi .max(5000) server-side before INSERT |
| T-06-10 | Tampering (data loss) | hard delete of posts | mitigate | No db.delete against community_posts anywhere; archive is a diarsipkan boolean flip (COMMUNITY-03) |
| T-06-11 | Tampering (injection) | kategori/metodeTerapi values | mitigate | Zod enum validation restricts kategori/metodeTerapi to fixed sets before persistence and filtering |
| T-06-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && node --import tsx --test src/test/communityPost.service.test.ts` passes
- `cd backend && npm test` full suite green
- Manual/authFetch: POST a post, GET the feed (appears newest-first), GET by id (isMine true for author), PATCH archive as author (succeeds), and confirm a second user's archive attempt returns 404
</verification>

<success_criteria>
COMMUNITY-01 and COMMUNITY-03 are satisfiable at the API layer: authenticated create, filtered newest-first feed, isMine-aware detail, and IDOR-safe archive-not-delete; the communityPost service test is GREEN.
</success_criteria>

<output>
Create `.planning/phases/06-community-education/06-04-SUMMARY.md` when done.
</output>
