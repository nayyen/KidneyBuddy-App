---
phase: 06-community-education
plan: 07
type: execute
wave: 5
depends_on: ["06-05", "06-06"]
files_modified:
  - frontend/app/(app)/edukasi/komunitas/[id]/page.tsx
  - frontend/components/komunitas/PostDetail.tsx
  - frontend/components/komunitas/ReplyList.tsx
  - frontend/components/komunitas/ReplyItem.tsx
autonomous: true
requirements: [COMMUNITY-02, COMMUNITY-03]
must_haves:
  truths:
    - "User can open a post detail page at /edukasi/komunitas/[id], read the full body, and see its replies"
    - "User can submit a reply and see it appear in the reply list (COMMUNITY-02)"
    - "User can mark/unmark ANY reply as 'membantu' with an optimistic toggle; the count and marked state reflect the server (COMMUNITY-02, D-08)"
    - "User can archive their OWN post via a confirmation dialog; the archive control appears only when isMine is true (COMMUNITY-03)"
    - "All post/reply text renders as plain escaped JSX with whitespace-pre-wrap (no dangerouslySetInnerHTML)"
  artifacts:
    - path: "frontend/app/(app)/edukasi/komunitas/[id]/page.tsx"
      provides: "dynamic post detail route (D-03 bookmarkable URL)"
    - path: "frontend/components/komunitas/PostDetail.tsx"
      provides: "full post view + reply composer + archive dialog"
      min_lines: 50
    - path: "frontend/components/komunitas/ReplyItem.tsx"
      provides: "reply + Tandai Membantu optimistic toggle"
  key_links:
    - from: "frontend/components/komunitas/ReplyItem.tsx"
      to: "/api/community/replies/:replyId/helpful"
      via: "authFetch POST (optimistic)"
      pattern: "helpful"
    - from: "frontend/components/komunitas/PostDetail.tsx"
      to: "/api/community/:id/archive"
      via: "authFetch PATCH from AlertDialog confirm"
      pattern: "archive"
---

<objective>
Community vertical (post detail + replies + membantu + archive UI): the final
slice. Replace the feed-only experience with a full post detail page where a user
reads a post, replies, marks helpful replies, and archives their own post. After
this plan COMMUNITY-02 and the interactive half of COMMUNITY-03 are demoable
end-to-end.

Purpose: render 06-05's reply/helpful API and 06-04's archive API as the
post-detail experience on the bookmarkable /edukasi/komunitas/[id] route (D-03).
Output: dynamic detail route + PostDetail + ReplyList + ReplyItem.
</objective>

## Phase Goal (slice framing)

**As a** CKD patient or caregiver, **I want to** read a post in full, reply to it,
mark helpful replies, and archive my own posts, **so that** I can participate in
peer discussion and manage what I've shared — without ever losing it permanently.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-community-education/06-CONTEXT.md
@.planning/phases/06-community-education/06-UI-SPEC.md
@.planning/phases/06-community-education/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic post detail route + PostDetail (full body, reply composer, archive dialog)</name>
  <files>frontend/app/(app)/edukasi/komunitas/[id]/page.tsx, frontend/components/komunitas/PostDetail.tsx</files>
  <read_first>
    - frontend/app/(app)/edukasi/komunitas/page.tsx (auth-guard boilerplate to reuse for the [id] route)
    - frontend/components/komunitas/CommunityFeed.tsx (from 06-06 — fetch/state conventions to mirror)
    - frontend/components/ui/alert-dialog.tsx (existing AlertDialog primitive)
    - a DeleteReminderConfirm.tsx-style AlertDialog in the repo (exact confirm-dialog structure/typography referenced by UI-SPEC)
    - frontend/lib/api.ts (authFetch GET/POST/PATCH)
    - .planning/phases/06-community-education/06-UI-SPEC.md (Display 18/700 title; archive dialog copy "Arsipkan Postingan?" / body / "Batal" / "Arsipkan" destructive #d4183d; "Kirim Balasan" CTA; whitespace-pre-wrap body)
    - .planning/phases/06-community-education/06-RESEARCH.md lines 472-480 (safe text rendering)
  </read_first>
  <action>
    Create the dynamic route [id]/page.tsx ("use client"): reuse the auth-guard boilerplate (useAuth, redirect to /login, "Memuat..." loading, return null when unauthenticated), read the id via useParams(), and render <PostDetail postId={id} accessToken={accessToken} />. This is a separate bookmarkable route per D-03.

    Create PostDetail.tsx ("use client", props { postId, accessToken }). On mount, authFetch GET /api/community/:id to load the post (including the server-derived isMine flag from 06-04) and render: author Avatar + name + timestamp, judul as Display (18px/700), full isi as a plain JSX text child with className "whitespace-pre-wrap" (Body 13/400) — never dangerouslySetInnerHTML; category + therapy-tag badges. Below the post, render <ReplyList postId={postId} accessToken={accessToken} /> (Task 2). Render a reply composer: a Textarea + "Kirim Balasan" button (accent #2a9d8f, 44px) that authFetch POSTs /api/community/:id/replies and, on success, triggers a reply-list refetch (lift a refetch handler or use a shared key). When post.isMine is true, render an archive affordance (e.g. an Archive icon/menu) that opens an AlertDialog with title "Arsipkan Postingan?", body "Postingan ini akan disembunyikan dari feed komunitas dan tidak dapat dipulihkan sendiri. Balasan yang sudah ada akan tetap tersimpan.", Cancel "Batal", Confirm "Arsipkan" (destructive #d4183d); confirming authFetch PATCHes /api/community/:id/archive and on success navigates back to /edukasi/komunitas (router.push). When post.isMine is false, do NOT render the archive control (server still enforces IDOR regardless). Show loading/error/not-found states (404 → a "Postingan tidak ditemukan" message with a back link).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "PostDetail|komunitas" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" frontend/components/komunitas/PostDetail.tsx` returns 0
    - `grep -c "whitespace-pre-wrap" frontend/components/komunitas/PostDetail.tsx` returns >= 1
    - The archive AlertDialog only renders when the post's isMine flag is true
    - `grep -c "Arsipkan Postingan" frontend/components/komunitas/PostDetail.tsx` returns 1
    - `grep -c "api/community" frontend/components/komunitas/PostDetail.tsx` returns >= 2 (GET detail + PATCH archive and/or POST reply)
    - The [id] route reads useParams and reuses the /login auth guard
  </acceptance_criteria>
  <done>The bookmarkable detail route shows the full post safely, a working reply composer, and an owner-only archive confirmation that archives (never deletes) and returns to the feed.</done>
</task>

<task type="auto">
  <name>Task 2: ReplyList + ReplyItem with optimistic 'Tandai Membantu' toggle</name>
  <files>frontend/components/komunitas/ReplyList.tsx, frontend/components/komunitas/ReplyItem.tsx</files>
  <read_first>
    - frontend/components/anomaly/AlertHistoryList.tsx lines 51-68 (optimistic-update toggle with revert-on-failure; PATTERNS.md lines 349-359)
    - frontend/lib/api.ts (authFetch)
    - .planning/phases/06-community-education/06-UI-SPEC.md (reply content Body 13/400; "Tandai Membantu" toggle label + marked state accent teal; empty state "Belum Ada Balasan" / "Jadilah yang pertama membalas postingan ini.")
  </read_first>
  <action>
    Create ReplyList.tsx ("use client", props { postId, accessToken } and an exposed refetch mechanism so the composer in PostDetail can refresh it — e.g. forwardRef or a key/refetch callback). Fetch authFetch<{ replies: ReplyItem[] }> GET /api/community/:id/replies; each reply carries isi, author name, createdAt, helpfulCount, and markedByMe (from 06-05). Show loading "Memuat...", error + "Coba Lagi", and the empty state (heading "Belum Ada Balasan", body "Jadilah yang pertama membalas postingan ini."). Render each reply as <ReplyItem>.

    Create ReplyItem.tsx (props: reply { id, isi, author, createdAt, helpfulCount, markedByMe }, accessToken). Render author + timestamp (Label 12/700), isi as a plain JSX text child with "whitespace-pre-wrap" (Body 13/400) — never dangerouslySetInnerHTML. Render a "Tandai Membantu" toggle button showing the helpful count and a lucide icon; when markedByMe is true, show the marked/active accent-teal (#2a9d8f) state (label may read "Membantu ✓" or count-highlighted per UI-SPEC — same button toggles, no separate unmark copy). On click, optimistically flip markedByMe and adjust helpfulCount by ±1 in local state, then authFetch POST /api/community/replies/:replyId/helpful; on failure, revert the optimistic change (mirror AlertHistoryList's revert pattern). Any authenticated user can mark any reply (D-08) — do NOT gate the toggle on authorship.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "ReplyItem|ReplyList" || echo "OK" && cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" frontend/components/komunitas/ReplyItem.tsx frontend/components/komunitas/ReplyList.tsx` returns 0
    - `grep -c "replies/" frontend/components/komunitas/ReplyItem.tsx` returns >= 1 (POST helpful toggle path)
    - ReplyItem optimistically updates then reverts on failure (revert branch present)
    - The toggle is NOT gated on the reply author (any user may mark, D-08)
    - `grep -c "Belum Ada Balasan" frontend/components/komunitas/ReplyList.tsx` returns 1
    - `cd frontend && npm run build` succeeds
  </acceptance_criteria>
  <done>Replies render safely with an optimistic, any-user, toggleable Tandai Membantu control backed by the server, plus correct empty/error states.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- Route `frontend/app/(app)/edukasi/komunitas/[id]/page.tsx`
- `PostDetail`, `ReplyList`, `ReplyItem` components
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-generated post/reply text → React render | full post + reply bodies in the detail DOM |
| client archive control → /api/community/:id/archive | ownership-gated UI over an IDOR-safe server endpoint |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering / Elevation of Privilege | post/reply body rendering | mitigate | All bodies rendered as plain JSX text children with whitespace-pre-wrap; zero dangerouslySetInnerHTML (RESEARCH Pitfall 3) |
| T-06-19 | Elevation of Privilege | archive control visibility | mitigate | Archive UI gated on server-derived isMine; server still enforces the compound-WHERE IDOR check (06-04) regardless of client state |
| T-06-20 | Tampering (data integrity) | optimistic helpful toggle drift | mitigate | Optimistic update reverts on request failure; server + unique constraint remain authoritative for the true marked state/count |
| T-06-SC | Tampering | npm installs | accept | No package installs; all shadcn primitives already present |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes; `cd frontend && npm run build` succeeds
- `cd backend && npm test` full suite still green (no regression)
- <human-check>Open a post from the feed: full body renders, replies load. Submit a reply → it appears. Mark a reply "membantu" → count increments and state highlights; unmark → reverts; a second account can also mark the same reply. On your OWN post the archive control appears → confirm "Arsipkan" → post leaves the feed (not deleted); on another user's post no archive control appears. Verify at 375px and 1024px.</human-check>
</verification>

<success_criteria>
COMMUNITY-02 and the interactive half of COMMUNITY-03 are demoable in the browser: a bookmarkable post detail with safe rendering, working replies, an open-access optimistic membantu toggle, and an owner-only archive-not-delete confirmation.
</success_criteria>

<output>
Create `.planning/phases/06-community-education/06-07-SUMMARY.md` when done.
</output>
