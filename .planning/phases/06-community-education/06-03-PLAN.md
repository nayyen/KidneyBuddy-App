---
phase: 06-community-education
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - frontend/app/(app)/edukasi/page.tsx
  - frontend/app/(app)/edukasi/komunitas/page.tsx
  - frontend/components/edukasi/EdukasiSubNav.tsx
  - frontend/components/edukasi/EducationList.tsx
  - frontend/components/edukasi/EducationCard.tsx
  - frontend/components/edukasi/EducationDetail.tsx
autonomous: true
requirements: [EDU-01]
must_haves:
  truths:
    - "User visiting /edukasi sees a two-pill sub-nav (Edukasi active by default, per D-04) with LifestyleSuggestionCard still above the education list"
    - "User can filter the education list by therapy method and all content shows by default"
    - "User can open an article and read its full body (plain text, whitespace-pre-wrap, no dangerouslySetInnerHTML)"
    - "The Komunitas pill links to /edukasi/komunitas which renders a placeholder ready for 06-06"
  artifacts:
    - path: "frontend/components/edukasi/EducationList.tsx"
      provides: "fetch + filter + loading/empty/error + card list"
      min_lines: 40
    - path: "frontend/components/edukasi/EdukasiSubNav.tsx"
      provides: "Edukasi/Komunitas pill row driven by usePathname"
    - path: "frontend/app/(app)/edukasi/komunitas/page.tsx"
      provides: "new route (D-03) placeholder"
  key_links:
    - from: "frontend/components/edukasi/EducationList.tsx"
      to: "/api/education"
      via: "authFetch"
      pattern: "api/education"
    - from: "frontend/components/edukasi/EdukasiSubNav.tsx"
      to: "/edukasi and /edukasi/komunitas"
      via: "next/link + usePathname"
      pattern: "edukasi/komunitas"
---

<objective>
Education vertical (frontend half): replace the Phase 5 "Konten Segera Hadir"
placeholder on /edukasi with a real, filterable education content browser, add the
two-pill Edukasi/Komunitas sub-navigation (D-01/D-02/D-03), and stand up the new
/edukasi/komunitas route as a placeholder for the community feed (06-06). After
this plan a real user can browse and filter education content in the browser —
EDU-01 is demoable end-to-end.

Purpose: complete EDU-01 as a user-visible capability on top of 06-02's API.
Output: updated edukasi/page.tsx, new komunitas/page.tsx, sub-nav + list + card +
detail components.
</objective>

## Phase Goal (slice framing)

**As a** CKD patient, **I want to** browse and filter education articles by my
therapy method inside the app, **so that** I can read relevant guidance without
sifting through irrelevant content.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-community-education/06-CONTEXT.md
@.planning/phases/06-community-education/06-UI-SPEC.md
@.planning/phases/06-community-education/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sub-nav pills + updated /edukasi page + /edukasi/komunitas placeholder route</name>
  <files>frontend/components/edukasi/EdukasiSubNav.tsx, frontend/app/(app)/edukasi/page.tsx, frontend/app/(app)/edukasi/komunitas/page.tsx</files>
  <read_first>
    - frontend/app/(app)/edukasi/page.tsx (existing auth-guard + LifestyleSuggestionCard + "Konten Segera Hadir" placeholder to replace; PATTERNS.md lines 274-292)
    - frontend/app/(app)/catatan/page.tsx lines 20-34 and 154-188 (exact pill styling: 12px/700, 36px height, 20px radius, active #2a9d8f bg / inactive #f0faf9 bg; PATTERNS.md lines 294-317)
    - .planning/phases/06-community-education/06-UI-SPEC.md (Color, Typography, Spacing, sub-nav pill spec at Layout Notes)
  </read_first>
  <action>
    Create EdukasiSubNav.tsx ("use client"): a horizontal pill row with two pills, "Edukasi" (href "/edukasi") and "Komunitas" (href "/edukasi/komunitas"), rendered as next/link Link elements. Use usePathname() to compute isActive (Edukasi active when pathname === "/edukasi"; Komunitas active when pathname startsWith "/edukasi/komunitas"). Copy the pill visual styling verbatim from catatan/page.tsx's TABS pill block: fontSize 12, fontWeight 700 (font-sans), height 36, borderRadius 20, paddingLeft/Right 16, active backgroundColor #2a9d8f + color #ffffff, inactive backgroundColor #f0faf9 + color #1a2e2c. Set aria-current="page" on the active pill. Per D-03 these are route Links, not local useState tabs.

    Modify edukasi/page.tsx: keep the existing auth-guard boilerplate and keep <LifestyleSuggestionCard> exactly where it is (do NOT remove it — 06-CONTEXT code_context). Render <EdukasiSubNav /> near the top (below the page header, above content). Replace the "Konten Segera Hadir" BookOpen placeholder block with <EducationList accessToken={accessToken} />.

    Create komunitas/page.tsx (new route per D-03): reuse the exact auth-guard boilerplate from edukasi/page.tsx (useAuth, redirect to /login when unauthenticated, "Memuat..." loading, return null when unauthenticated). Render <EdukasiSubNav /> (so Komunitas pill shows active) and a simple placeholder container (heading "Komunitas" + short body) that 06-06 replaces with <CommunityFeed />. This keeps the Komunitas pill link non-broken during the phase.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "edukasi|EdukasiSubNav|EducationList" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LifestyleSuggestionCard" frontend/app/(app)/edukasi/page.tsx` returns >= 1 (kept)
    - `grep -c "Konten Segera Hadir" frontend/app/(app)/edukasi/page.tsx` returns 0 (placeholder removed)
    - `grep -c "usePathname" frontend/components/edukasi/EdukasiSubNav.tsx` returns 1
    - `grep -c "/edukasi/komunitas" frontend/components/edukasi/EdukasiSubNav.tsx` returns >= 1
    - komunitas/page.tsx exists and contains the auth-guard redirect to /login
  </acceptance_criteria>
  <done>/edukasi shows the two-pill sub-nav with LifestyleSuggestionCard retained and the education list mounted; /edukasi/komunitas renders an auth-guarded placeholder with the Komunitas pill active.</done>
</task>

<task type="auto">
  <name>Task 2: EducationList (fetch + therapy filter) and EducationCard</name>
  <files>frontend/components/edukasi/EducationList.tsx, frontend/components/edukasi/EducationCard.tsx</files>
  <read_first>
    - frontend/components/anomaly/AlertHistoryList.tsx lines 23-47 and 88-130 (fetch + loading/error/empty state + list render pattern; PATTERNS.md lines 323-347)
    - frontend/lib/api.ts (authFetch<T>(path, accessToken, init?) — Bearer + 401-refresh, use unchanged)
    - .planning/phases/06-community-education/06-UI-SPEC.md (EducationCard spec, therapy-tag badge colors, empty/error copy "Konten Tidak Ditemukan" / "Coba Lagi")
  </read_first>
  <action>
    Create EducationList.tsx ("use client", props: { accessToken: string }). Hold state: items, isLoading, hasError, and an active therapy-method filter (default "Semua" meaning no filter — content shows all by default). Render a horizontal pill-chip filter row (Semua/CAPD/HD/Transplantasi/Umum) using the same 36px/20px-radius pill styling as EdukasiSubNav (active #2a9d8f). fetchItems calls authFetch<{ content: EducationItem[] }>(`/api/education${metode ? "?metodeTerapi=" + metode : ""}`, accessToken); on change of the active filter, refetch (server-side filtering per EDU-01, not client .filter()). Show "Memuat..." while loading, the error block with retry button labeled "Coba Lagi" and copy "Gagal memuat konten. Periksa koneksi internet Anda, lalu coba lagi." on failure, and the empty state (heading "Konten Tidak Ditemukan", body "Coba pilih kategori atau metode terapi lain.") when zero rows. Render items as a vertical list of <EducationCard>. Manage a selected-article state to open <EducationDetail> (see Task 3) in a Dialog.

    Create EducationCard.tsx (props: the education item + onClick). Card (white bg #ffffff, 0.5px border, 14px radius, 12-14px padding) showing: judul (Heading 14px/700, Plus Jakarta Sans), ringkasan (Body 13px/400, line-clamp 2), a therapy-tag badge (CAPD #2a9d8f/bg #f0faf9, HD #ef9f27/bg #fdf3e3, Transplantasi #6b5ca5/bg #f1eef9, Umum #7a8c8a/bg #f3ede5 per UI-SPEC), and a small lucide icon reflecting tipeKonten (e.g. BookOpen for artikel, Activity/Dumbbell for panduan_senam, Salad/Heart for gaya_hidup). Clicking the card calls onClick(item). Never use dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "EducationList|EducationCard" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/education" frontend/components/edukasi/EducationList.tsx` returns >= 1
    - EducationList refetches from the server when the therapy filter changes (metodeTerapi passed as a query param, not a client-side array filter)
    - `grep -c "dangerouslySetInnerHTML" frontend/components/edukasi/EducationList.tsx frontend/components/edukasi/EducationCard.tsx` returns 0
    - `grep -c "Konten Tidak Ditemukan" frontend/components/edukasi/EducationList.tsx` returns 1
    - Default filter state fetches all content (no metodeTerapi param) on first load
  </acceptance_criteria>
  <done>EducationList fetches from /api/education, filters by therapy method server-side with all-content default, and renders EducationCards with loading/empty/error states.</done>
</task>

<task type="auto">
  <name>Task 3: EducationDetail article view</name>
  <files>frontend/components/edukasi/EducationDetail.tsx</files>
  <read_first>
    - frontend/lib/api.ts (authFetch for GET /api/education/:id if the list item lacks full isi)
    - frontend/components/ui/dialog.tsx (existing Dialog primitive used to present the detail)
    - .planning/phases/06-community-education/06-RESEARCH.md lines 472-480 (safe whitespace-pre-wrap plain-text rendering)
    - .planning/phases/06-community-education/06-UI-SPEC.md (Display 18/700 title, Body 13/400 article text, static image, no video D-11)
  </read_first>
  <action>
    Create EducationDetail.tsx ("use client") presented inside a Dialog (ui/dialog) opened from EducationList's selected-article state. Props: the selected education item (and accessToken if a follow-up GET /api/education/:id is needed to fetch full isi). Render: judul as Display (18px/700, Plus Jakarta Sans), the therapy-tag badge, an optional static illustration if gambarUrl is present (plain <img> or an inline flat SVG — no video/iframe per D-11), and the full isi body as a plain JSX text child with className including "whitespace-pre-wrap" and Body typography (13px/400) so user-visible line breaks are preserved WITHOUT HTML injection. Never use dangerouslySetInnerHTML. Include a close affordance (Dialog's built-in close). Keep tone/reading-friendly spacing (md/16px section rhythm).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "EducationDetail" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "whitespace-pre-wrap" frontend/components/edukasi/EducationDetail.tsx` returns >= 1
    - `grep -c "dangerouslySetInnerHTML" frontend/components/edukasi/EducationDetail.tsx` returns 0
    - `grep -Ec "iframe|youtube|video" frontend/components/edukasi/EducationDetail.tsx` returns 0 (no video per D-11)
    - The article body renders {item.isi} as a JSX text child (auto-escaped)
  </acceptance_criteria>
  <done>Users can open an article detail in a dialog and read the full body as safely-escaped plain text with preserved line breaks and an optional static illustration.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `EdukasiSubNav` component
- `EducationList`, `EducationCard`, `EducationDetail` components
- Route `frontend/app/(app)/edukasi/komunitas/page.tsx` (placeholder)
- Updated `frontend/app/(app)/edukasi/page.tsx` (LifestyleSuggestionCard retained, list mounted)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| seeded content → React render | education body text rendered in the DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering / Elevation of Privilege | education body rendering | mitigate | Render all article text as plain JSX text children with whitespace-pre-wrap; never dangerouslySetInnerHTML (RESEARCH Pitfall 3) |
| T-06-07 | Information Disclosure | unauthenticated page access | mitigate | Both /edukasi and /edukasi/komunitas use the existing useAuth guard redirecting to /login |
| T-06-SC | Tampering | npm installs | accept | No package installs; all shadcn primitives already present (UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes with no education-related errors
- `cd frontend && npm run build` succeeds
- <human-check>Visit /edukasi: LifestyleSuggestionCard still shows, the Edukasi/Komunitas pills render, education cards load and filter by therapy method, and opening an article shows its full body. Visit /edukasi/komunitas: Komunitas pill is active and the placeholder renders. Verify at 375px and 1024px per the responsive system.</human-check>
</verification>

<success_criteria>
EDU-01 is demoable in the browser: filterable education browsing lives under /edukasi with the two-pill sub-nav, LifestyleSuggestionCard retained, safe plain-text article detail, and a working (placeholder) Komunitas route.
</success_criteria>

<output>
Create `.planning/phases/06-community-education/06-03-SUMMARY.md` when done.
</output>
