---
phase: 06-community-education
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-04"]
files_modified:
  - frontend/app/(app)/edukasi/komunitas/page.tsx
  - frontend/components/komunitas/CommunityFeed.tsx
  - frontend/components/komunitas/PostCard.tsx
  - frontend/components/komunitas/CreatePostSheet.tsx
autonomous: true
requirements: [COMMUNITY-01, COMMUNITY-03]
must_haves:
  truths:
    - "User visiting /edukasi/komunitas sees a newest-first feed of non-archived posts"
    - "User can filter the feed by category (D-05) and by therapy method (D-06), with ALL posts shown by default (no auto-filter to own method)"
    - "User can create a post via the 'Buat Postingan' sheet and see it appear in the feed after submit"
    - "Post/reply text renders as plain escaped JSX (no dangerouslySetInnerHTML)"
  artifacts:
    - path: "frontend/components/komunitas/CommunityFeed.tsx"
      provides: "feed fetch + category/therapy filters + create button"
      min_lines: 50
    - path: "frontend/components/komunitas/PostCard.tsx"
      provides: "post preview card with badges + counts"
    - path: "frontend/components/komunitas/CreatePostSheet.tsx"
      provides: "post composer (title/content/category/therapy)"
  key_links:
    - from: "frontend/components/komunitas/CommunityFeed.tsx"
      to: "/api/community"
      via: "authFetch GET with kategori/metodeTerapi query"
      pattern: "api/community"
    - from: "frontend/components/komunitas/CreatePostSheet.tsx"
      to: "/api/community"
      via: "authFetch POST"
      pattern: "api/community"
---

<objective>
Community vertical (feed + create UI): replace the /edukasi/komunitas placeholder
with a real, filterable, newest-first community feed and a "Buat Postingan"
composer. After this plan a real user can browse the community and publish a post
in the browser — COMMUNITY-01 is demoable end-to-end and archived posts are absent
from the feed (COMMUNITY-03 visible half).

Purpose: render 06-04's post API as the D-05/D-06/D-07 feed and composer. Output:
komunitas/page.tsx wired to CommunityFeed, PostCard, CreatePostSheet.
</objective>

## Phase Goal (slice framing)

**As a** CKD patient or caregiver, **I want to** browse and filter the community
feed and publish my own post, **so that** I can share experiences and ask
questions with peers.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-community-education/06-CONTEXT.md
@.planning/phases/06-community-education/06-UI-SPEC.md
@.planning/phases/06-community-education/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommunityFeed with category + therapy filters, wired into komunitas page</name>
  <files>frontend/app/(app)/edukasi/komunitas/page.tsx, frontend/components/komunitas/CommunityFeed.tsx</files>
  <read_first>
    - frontend/app/(app)/edukasi/komunitas/page.tsx (placeholder created in 06-03 — replace its placeholder body with CommunityFeed, keep the auth guard + EdukasiSubNav)
    - frontend/components/anomaly/AlertHistoryList.tsx lines 23-47, 88-130 (fetch + loading/error/empty + list render; PATTERNS.md lines 323-347)
    - frontend/components/edukasi/EdukasiSubNav.tsx (from 06-03) and the pill styling in catatan/page.tsx (for filter chips)
    - frontend/lib/api.ts (authFetch)
    - .planning/phases/06-community-education/06-UI-SPEC.md (feed empty-state copy "Belum Ada Diskusi", filter chip styling, "Buat Postingan" CTA color #2a9d8f)
  </read_first>
  <action>
    Modify komunitas/page.tsx: keep the 06-03 auth-guard boilerplate and <EdukasiSubNav />, and replace the placeholder container with <CommunityFeed accessToken={accessToken} />.

    Create CommunityFeed.tsx ("use client", props { accessToken }). State: posts, isLoading, hasError, activeKategori (default "Semua" = no filter), activeMetode (default "Semua" = no filter — D-06: show ALL posts by default, do NOT auto-filter to the viewer's own therapy method), and a create-sheet-open boolean. Render TWO horizontal pill-chip filter rows using the shared 36px/20px-radius pill styling: (1) category chips Semua/Pertanyaan/Berbagi Pengalaman/Informasi (D-05), (2) therapy chips Semua/CAPD/HD/Transplantasi/Umum (D-06). fetchPosts calls authFetch<{ posts: PostItem[] }>(`/api/community` + query string built from activeKategori/activeMetode when not "Semua", mapping the display labels to the API enum values pertanyaan|berbagi_pengalaman|informasi and CAPD|HD|Transplantasi|Umum); refetch whenever a filter changes (server-side filtering, not client .filter()). Feed is already newest-first from the API (D-07) — do not client-sort. Show "Memuat...", the error block + "Coba Lagi" retry, and the empty state (heading "Belum Ada Diskusi", body "Jadi yang pertama berbagi cerita, bertanya, atau memberi informasi di sini.") for zero rows. Render a prominent "Buat Postingan" button (accent #2a9d8f, height 44px) that opens <CreatePostSheet>; on successful create, close the sheet and refetch the feed. Render each post as <PostCard post={p} /> linking to /edukasi/komunitas/[id] (06-07 provides the detail route).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "CommunityFeed|komunitas" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/community" frontend/components/komunitas/CommunityFeed.tsx` returns >= 1
    - Default filter state sends NO kategori and NO metodeTerapi param on first load (all posts shown — D-06)
    - `grep -c "Belum Ada Diskusi" frontend/components/komunitas/CommunityFeed.tsx` returns 1
    - Feed does not apply a client-side .sort() (relies on API newest-first, D-07)
    - komunitas/page.tsx renders <CommunityFeed> and retains <EdukasiSubNav> + the /login auth guard
  </acceptance_criteria>
  <done>/edukasi/komunitas shows a newest-first, category+therapy-filterable feed defaulting to all posts, with a Buat Postingan CTA and correct empty/error states.</done>
</task>

<task type="auto">
  <name>Task 2: PostCard preview component</name>
  <files>frontend/components/komunitas/PostCard.tsx</files>
  <read_first>
    - frontend/components/anomaly/AlertHistoryList.tsx (card render + badge usage reference)
    - frontend/components/ui/badge.tsx, frontend/components/ui/avatar.tsx (existing primitives)
    - .planning/phases/06-community-education/06-UI-SPEC.md (PostCard spec: Heading 14/700 title, Body 13/400 preview line-clamp, Label 12/700 badges; therapy-tag badge colors; reply + membantu counts)
  </read_first>
  <action>
    Create PostCard.tsx (props: post item with id, judul, isi, kategori, metodeTerapi, createdAt, author display name, replyCount, helpfulTotal). Wrap in a next/link Link to `/edukasi/komunitas/${post.id}`. Card (white bg, 12-14px padding, 14px radius) showing: an Avatar + author name (Label 12/700) + relative/formatted timestamp; judul (Heading 14/700); isi preview as a plain JSX text child with className "whitespace-pre-wrap line-clamp-2" (Body 13/400) — never dangerouslySetInnerHTML; a category Badge (Pertanyaan/Berbagi Pengalaman/Informasi, Label 12/700) and a therapy-tag Badge using the established colors (CAPD #2a9d8f/bg #f0faf9, HD #ef9f27/bg #fdf3e3, Transplantasi #6b5ca5/bg #f1eef9, Umum #7a8c8a/bg #f3ede5); a footer row with reply count and total membantu count (lucide icons + Label 12/700). Map the stored kategori enum (pertanyaan|berbagi_pengalaman|informasi) to display labels. If the backend feed response does not yet include replyCount/helpfulTotal, render them defensively (0 fallback) — the counts are refined in 06-07's detail view.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "PostCard" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" frontend/components/komunitas/PostCard.tsx` returns 0
    - `grep -c "whitespace-pre-wrap" frontend/components/komunitas/PostCard.tsx` returns >= 1
    - PostCard links to `/edukasi/komunitas/${post.id}`
    - Category and therapy-tag badges render with the UI-SPEC colors (no new color tokens introduced)
  </acceptance_criteria>
  <done>PostCard renders a safe, badge-tagged, linkable post preview matching the UI-SPEC typography and therapy-tag colors.</done>
</task>

<task type="auto">
  <name>Task 3: CreatePostSheet composer</name>
  <files>frontend/components/komunitas/CreatePostSheet.tsx</files>
  <read_first>
    - frontend/components/ui/sheet.tsx, textarea.tsx, input.tsx, select.tsx, label.tsx, form.tsx (existing primitives — UI-SPEC Registry Safety, all installed)
    - frontend/lib/api.ts (authFetch POST)
    - .planning/phases/06-community-education/06-UI-SPEC.md (CreatePostSheet spec, "Buat Postingan" CTA, submit error toast copy)
    - a reference react-hook-form + zod form in the repo (e.g. an existing add sheet such as components/lab or components/pengingat) to mirror the form wiring convention
  </read_first>
  <action>
    Create CreatePostSheet.tsx ("use client", props { accessToken, open, onOpenChange, onCreated }). A Sheet containing a form (react-hook-form + @hookform/resolvers/zod, mirroring existing data-entry forms) with: Input for judul (label "Judul", max 200), Textarea for isi (label "Isi", max 5000), a Select for kategori with options Pertanyaan/Berbagi Pengalaman/Informasi (submitting the enum values pertanyaan|berbagi_pengalaman|informasi), and a Select (or pill-chip group) for metodeTerapi with options CAPD/HD/Transplantasi/Umum. A submit button "Buat Postingan" (accent #2a9d8f, height 44px). On submit, authFetch POST /api/community with the JSON body; on success call onCreated() (which closes the sheet and refetches) and reset the form; on failure show toast.error("Gagal mengirim. Periksa koneksi internet Anda dan coba lagi.") matching the existing toast pattern. Mirror the client-side zod field constraints to the server createPostSchema (judul 1-200, isi 1-5000, kategori/metodeTerapi enums) so validation fails fast inline.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "CreatePostSheet" || echo "OK" && cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/community" frontend/components/komunitas/CreatePostSheet.tsx` returns >= 1 (POST)
    - The composer submits kategori enum values (pertanyaan|berbagi_pengalaman|informasi), not display labels
    - The composer includes a metodeTerapi selector offering CAPD/HD/Transplantasi/Umum
    - `grep -c "Gagal mengirim" frontend/components/komunitas/CreatePostSheet.tsx` returns 1
    - `cd frontend && npm run build` succeeds
  </acceptance_criteria>
  <done>Users can compose and submit a post (title/content/category/therapy) that appears in the feed on success, with inline validation and an error toast on failure.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `CommunityFeed`, `PostCard`, `CreatePostSheet` components
- Updated `frontend/app/(app)/edukasi/komunitas/page.tsx` (feed mounted)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user-generated post text → React render | post preview text in the feed DOM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Tampering / Elevation of Privilege | post preview rendering | mitigate | isi rendered as plain JSX text child with whitespace-pre-wrap/line-clamp; never dangerouslySetInnerHTML (RESEARCH Pitfall 3) |
| T-06-16 | Elevation of Privilege | feed auto-filtering to own method | accept | Intentionally shows ALL posts by default (D-06); user narrows manually — not a security control |
| T-06-17 | Denial of Service | oversized post submission | mitigate | Client zod mirrors server max lengths (judul 200 / isi 5000) for fail-fast; server remains authoritative |
| T-06-SC | Tampering | npm installs | accept | No package installs; all shadcn primitives already present |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes; `cd frontend && npm run build` succeeds
- <human-check>Visit /edukasi/komunitas: feed loads newest-first showing all posts by default; category and therapy chips filter correctly (all shown until a chip is selected); "Buat Postingan" opens the composer; submitting a valid post closes the sheet and the new post appears at the top; an archived post does not appear. Verify at 375px and 1024px.</human-check>
</verification>

<success_criteria>
COMMUNITY-01 is demoable in the browser: a filterable, newest-first, default-all community feed with a working post composer under /edukasi/komunitas; archived posts are absent from the feed.
</success_criteria>

<output>
Create `.planning/phases/06-community-education/06-06-SUMMARY.md` when done.
</output>
