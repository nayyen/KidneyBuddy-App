---
phase: 06-community-education
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - backend/src/repositories/communityReply.repository.ts
  - backend/src/services/communityReply.service.ts
  - backend/src/controllers/community.controller.ts
  - backend/src/routes/community.routes.ts
autonomous: true
requirements: [COMMUNITY-02]
must_haves:
  truths:
    - "POST /api/community/:id/replies creates a reply on a post for the authenticated user (COMMUNITY-02)"
    - "GET /api/community/:id/replies returns replies with helpfulCount (COUNT aggregate) and markedByMe per reply"
    - "POST /api/community/replies/:replyId/helpful toggles a membantu mark, one per user per reply, toggleable (D-08/D-09)"
    - "Any authenticated user can mark any reply (not restricted to the post author) — intentional open-access, not IDOR (D-08)"
    - "helpfulCount is computed from the join table, never a denormalized counter column"
    - "communityReply.service.test.ts passes (GREEN), including toggle-twice-returns-unmarked"
  artifacts:
    - path: "backend/src/repositories/communityReply.repository.ts"
      provides: "createReply, findByPost (with helpful aggregate), toggleHelpful, countHelpful"
      exports: ["createReply", "findByPost", "toggleHelpful"]
    - path: "backend/src/services/communityReply.service.ts"
      provides: "createReplySchema, createReply, listReplies, toggleHelpful"
      exports: ["createReply", "listReplies", "toggleHelpful"]
  key_links:
    - from: "communityReply.repository.toggleHelpful"
      to: "community_reply_helpful (unique reply_id,user_id)"
      via: "check-then insert/delete against the join table"
      pattern: "communityReplyHelpful"
    - from: "backend/src/routes/community.routes.ts"
      to: "reply + helpful handlers"
      via: "authenticate-guarded routes"
      pattern: "replies|helpful"
---

<objective>
Community vertical (reply + "membantu" backend): let users reply to a post and
mark any reply as helpful, one mark per user per reply, toggleable. After this
plan the COMMUNITY-02 capability is real at the API layer; the post-detail UI
(06-07) renders replies and the toggle.

Purpose: COMMUNITY-02 with the D-08 (reply-level, open to any user — NOT the PRD's
post-level "Jumlah Upvote") and D-09 (DB-unique one-per-user, toggleable) mechanics.
Output: reply repository + service (with COUNT-based helpfulCount) + reply/helpful
handlers appended to the existing community controller + routes.
</objective>

## Phase Goal (slice framing)

**As a** CKD patient or caregiver, **I want to** reply to a post and mark helpful
replies, **so that** the most useful peer answers surface — with each person's mark
counted once and toggleable.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-community-education/06-CONTEXT.md
@.planning/phases/06-community-education/06-RESEARCH.md
@.planning/phases/06-community-education/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: communityReply repository + service (reply create, list-with-count, toggle helpful; GREEN test)</name>
  <files>backend/src/repositories/communityReply.repository.ts, backend/src/services/communityReply.service.ts</files>
  <behavior>
    - createReply persists a reply scoped to (postId, userId)
    - createReply rejects empty isi (Zod)
    - toggleHelpful(userId, replyId) returns { marked: true } on first call, { marked: false } on second call for the same pair
    - listReplies returns each reply with a numeric helpfulCount and a boolean markedByMe for the current user
    - helpfulCount reflects the join-table row count, not a denormalized column
  </behavior>
  <read_first>
    - backend/src/repositories/labResult.repository.ts (repository shape/imports; PATTERNS.md lines 111-148)
    - backend/src/services/labResult.service.ts lines 1-90 (Zod + injectable-core seam)
    - backend/src/test/communityReply.service.test.ts (the RED contract from 06-01 to turn GREEN)
    - backend/src/db/schema/communityReply.schema.ts and communityReplyHelpful.schema.ts (created in 06-01)
    - .planning/phases/06-community-education/06-RESEARCH.md lines 444-469 (drafted toggleHelpful) and lines 283-307 (join-table semantics)
    - .planning/phases/06-community-education/06-PATTERNS.md lines 152-169 (toggle placement guidance)
  </read_first>
  <action>
    Create communityReply.repository.ts exporting: createReply(row: { postId, userId, isi }) inserting into communityReplies and returning the row; findByPost(postId, currentUserId) returning replies ordered by asc(createdAt), each augmented with helpfulCount (a COUNT(*) aggregate against communityReplyHelpful for that replyId — via a Drizzle count() subquery/group-by or a per-reply count, whichever matches existing aggregate usage) and markedByMe (true when a community_reply_helpful row exists for (replyId, currentUserId)); toggleHelpful(userId, replyId) implementing the RESEARCH check-then-act pattern — select the existing (userId, replyId) row; if present, delete it and return { marked: false }; else insert { userId, replyId } and return { marked: true }. Do NOT add or mutate any denormalized counter column (RESEARCH Anti-Patterns). Do NOT apply a userId-ownership guard to toggle (D-08 — any user may mark any reply).

    Create communityReply.service.ts with createReplySchema = z.object({ isi: z.string min1 max2000 with Indonesian required_error/messages }). Export createReply(userId, postId, payload) (validate then repository.createReply), listReplies(postId, currentUserId) (delegate to findByPost), toggleHelpful(userId, replyId) (delegate to repository.toggleHelpful; validate replyId is a non-empty string). Injectable-core seam so the test injects an in-memory reply store + in-memory helpful set. Do NOT import encrypt/decrypt. pino logger "communityReply.service".
  </action>
  <verify>
    <automated>cd backend && node --import tsx --test src/test/communityReply.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - communityReply.service.test.ts passes (exit 0), asserting toggleHelpful returns marked:true then marked:false across two calls
    - `grep -Ec "helpful_count|helpfulCount.*integer|denormaliz" backend/src/db/schema/communityReply.schema.ts` returns 0 (no counter column added to replies)
    - `grep -c "communityReplyHelpful" backend/src/repositories/communityReply.repository.ts` returns >= 1 (count/toggle use the join table)
    - `grep -c "from \"../lib/encryption" backend/src/services/communityReply.service.ts` returns 0
    - toggleHelpful has NO userId-ownership WHERE guard tying the mark to the reply's author (D-08 open access)
  </acceptance_criteria>
  <done>Reply create, list-with-COUNT-based-helpfulCount+markedByMe, and a DB-backed toggle are implemented; the RED scaffold is now GREEN; marking is open to any user and dedup is enforced by the join table.</done>
</task>

<task type="auto">
  <name>Task 2: Append reply + helpful handlers to community controller and routes</name>
  <files>backend/src/controllers/community.controller.ts, backend/src/routes/community.routes.ts</files>
  <read_first>
    - backend/src/controllers/community.controller.ts (created in 06-04 — append, do not rewrite the post handlers)
    - backend/src/routes/community.routes.ts (created in 06-04 — append routes)
    - backend/src/controllers/labResult.controller.ts lines 19-55 (thin-handler template)
  </read_first>
  <action>
    Append to community.controller.ts three thin handlers (try/catch → next(err), no Zod, no db): createReply(req,res) → communityReplyService.createReply(req.user.id, req.params.id, req.body), res.status(201).json(row); listReplies(req,res) → listReplies(req.params.id, req.user.id), res.json({ replies }); toggleHelpful(req,res) → toggleHelpful(req.user.id, req.params.replyId), res.json(result). Import communityReplyService without disturbing the existing communityPostService import.

    Append to community.routes.ts: router.post("/:id/replies", authenticate, community.controller.createReply); router.get("/:id/replies", authenticate, community.controller.listReplies); router.post("/replies/:replyId/helpful", authenticate, community.controller.toggleHelpful). Keep the existing post routes intact. Ensure the "/replies/:replyId/helpful" route is registered so it does not collide with "/:id" GET (it is a distinct POST path segment "replies", safe).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -iE "community" || echo "OK" && grep -Ec "replies|helpful" src/routes/community.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "replies" backend/src/routes/community.routes.ts` returns >= 2 (create + list reply routes)
    - `grep -c "helpful" backend/src/routes/community.routes.ts` returns 1 (toggle route)
    - The three new routes each include the authenticate middleware
    - The post handlers from 06-04 (createPost/listPosts/getPost/archivePost) remain present and unchanged
  </acceptance_criteria>
  <done>Reply-create, reply-list, and helpful-toggle endpoints are mounted under /api/community, all authenticated, delegating to the reply service; post endpoints are untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `createReply`, `findByPost`, `toggleHelpful` (communityReply.repository.ts)
- `createReplySchema`, `createReply`, `listReplies`, `toggleHelpful` (communityReply.service.ts)
- `createReply`, `listReplies`, `toggleHelpful` handlers appended to community.controller.ts
- Routes: POST /api/community/:id/replies, GET /api/community/:id/replies, POST /api/community/replies/:replyId/helpful
- `helpfulCount`, `markedByMe` fields on reply list responses
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → reply/helpful endpoints | authenticated create + open-access toggle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering (data integrity) | double/repeated membantu marking | mitigate | DB-level unique(reply_id,user_id) constraint (06-01) is the backstop; toggle service checks-then-acts, constraint holds even under a race |
| T-06-13 | Denial of Service | unbounded reply length | mitigate | createReplySchema enforces isi .max(2000) server-side |
| T-06-14 | Elevation of Privilege (false positive) | any-user reply/toggle | accept | Open-access reply + mark is intentional (D-08) — NOT an IDOR gap; documented so a reviewer does not "fix" it back to post-author-only |
| T-06-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && node --import tsx --test src/test/communityReply.service.test.ts` passes
- `cd backend && npm test` full suite green
- Manual/authFetch: POST a reply, GET replies (helpfulCount 0, markedByMe false), POST helpful (marked true, count reflects 1), POST helpful again (marked false, count back to 0), and confirm a second user can also mark the same reply
</verification>

<success_criteria>
COMMUNITY-02 is satisfiable at the API layer: authenticated reply creation, reply listing with COUNT-based helpfulCount + markedByMe, and an open-access, DB-deduped, toggleable membantu mark; the communityReply service test is GREEN.
</success_criteria>

<output>
Create `.planning/phases/06-community-education/06-05-SUMMARY.md` when done.
</output>
