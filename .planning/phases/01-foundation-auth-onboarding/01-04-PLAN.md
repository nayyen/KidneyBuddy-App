---
phase: 01-foundation-auth-onboarding
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/src/db/schema/onboardingProgress.schema.ts
  - backend/src/db/schema/reminderSchedule.schema.ts
  - backend/src/db/schema/index.ts
  - backend/src/services/onboarding.service.ts
  - backend/src/repositories/onboardingProgress.repository.ts
  - backend/src/repositories/reminderSchedule.repository.ts
  - backend/src/repositories/user.repository.ts
  - backend/src/controllers/onboarding.controller.ts
  - backend/src/routes/onboarding.routes.ts
  - backend/src/app.ts
  - backend/src/config/therapyContent.ts
  - backend/test/onboarding.service.test.ts
  - frontend/app/onboarding/page.tsx
  - frontend/app/onboarding/_components/StepProgress.tsx
  - frontend/app/onboarding/_components/TherapySelectStep.tsx
  - frontend/app/onboarding/_components/FirstReminderStep.tsx
  - frontend/app/onboarding/_components/OnboardingSuccess.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/lib/validators/onboarding.schema.ts
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03, ONBOARD-04]
must_haves:
  truths:
    - "A new user completes onboarding (therapy method selection -> set or skip first reminder -> success) and lands on the dashboard"
    - "Each therapy card has an inline 'Apa ini?' expand showing a short calm explanation + the per-therapy illustration color"
    - "A user who closes the app mid-onboarding resumes from the last completed step"
    - "A user who skips the first reminder reaches the dashboard, which shows a banner reminding them to configure a reminder"
  artifacts:
    - path: "backend/src/db/schema/onboardingProgress.schema.ts"
      provides: "onboarding_progress table (resume support, ONBOARD-04)"
      contains: "pgTable"
    - path: "backend/src/services/onboarding.service.ts"
      provides: "save therapy method + first reminder + progress logic"
      exports: ["saveTherapyMethod", "saveFirstReminder", "skipFirstReminder", "getProgress"]
    - path: "frontend/app/onboarding/page.tsx"
      provides: "3-step wizard shell with progress indicator (D-06) and Kembali (D-07)"
    - path: "frontend/app/onboarding/_components/TherapySelectStep.tsx"
      provides: "therapy cards with inline Apa ini? expand (D-02..D-05)"
  key_links:
    - from: "frontend/app/onboarding/page.tsx"
      to: "POST /api/onboarding/therapy and /api/onboarding/reminder"
      via: "apiFetch per step"
      pattern: "onboarding"
    - from: "backend/src/services/onboarding.service.ts"
      to: "users table (metodeTerapiAktif) + reminder_schedule + onboarding_progress"
      via: "repository calls"
      pattern: "userRepository|reminderSchedule|onboardingProgress"
    - from: "frontend/app/onboarding/page.tsx"
      to: "GET /api/onboarding/progress"
      via: "fetch on load to resume"
      pattern: "progress"
---

<objective>
Build the interactive onboarding wizard vertical slice: a 3-step full-page wizard (therapy method selection with inline "Apa ini?" explanations -> set or skip first reminder -> success animation) that selects the user's active therapy method, persists progress so it can be resumed, and lands the user on the dashboard.

Purpose: Therapy-method selection is the modality foundation every later feature depends on (conditional UI, schedule types). The <5-minute completion target and resume-on-reopen are explicit success criteria. This slice realizes CONTEXT.md decisions D-02..D-09.
Output: working onboarding wizard; therapy method saved to the user; resume-from-last-step; skip-reminder banner on dashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-onboarding/01-CONTEXT.md
@.planning/phases/01-foundation-auth-onboarding/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@DESIGN_SYSTEM_KidneyBuddy_v3.md
@.planning/phases/01-foundation-auth-onboarding/01-02-SUMMARY.md

<interfaces>
<!-- From Plans 01-02 (already built): -->
users table: userId, metodeTerapiAktif (nullable text CAPD|HD|Transplantasi), tanggalMulaiTerapi (nullable date), riwayatTerapi (jsonb)
authenticate middleware (backend/src/middleware/authenticate.ts) — sets req.user.id
useAuth hook (frontend/lib/hooks/useAuth.ts); apiFetch (frontend/lib/api.ts)
auth GET /api/auth/me

<!-- Contracts this plan CREATES (Plan 05 reuses reminder + therapy-history patterns; Phase 2 owns full reminders): -->
onboarding_progress table: userId FK (unique), lastCompletedStep int, completedAt nullable, updatedAt
reminder_schedule table (minimal Phase-1 subset; Phase 2 extends): id, userId FK, jenis (obat|capd|hd), nama, jamPengingat time, hariAktif jsonb, catatanWaktu text nullable, aktif boolean, createdAt
  NOTE: Phase 1 only CREATES the first reminder; full reminder CRUD/push/cron is Phase 2. Keep columns forward-compatible with ARCHITECTURE.md ReminderSchedule.
therapyContent.ts: per-therapy {id, nama, namaPanjang, penjelasan (1-2 calm sentences, D-03/D-05), warna, warnaBg} for CAPD/HD/Transplantasi using DESIGN_SYSTEM therapy identity colors
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Onboarding backend — therapy method save, first-reminder create/skip, resumable progress</name>
  <files>backend/src/db/schema/onboardingProgress.schema.ts, backend/src/db/schema/reminderSchedule.schema.ts, backend/src/db/schema/index.ts, backend/src/services/onboarding.service.ts, backend/src/repositories/onboardingProgress.repository.ts, backend/src/repositories/reminderSchedule.repository.ts, backend/src/repositories/user.repository.ts, backend/src/controllers/onboarding.controller.ts, backend/src/routes/onboarding.routes.ts, backend/src/app.ts, backend/src/config/therapyContent.ts, backend/test/onboarding.service.test.ts</files>
  <read_first>
    - backend/src/middleware/authenticate.ts (req.user.id — all onboarding routes are authenticated)
    - backend/src/repositories/user.repository.ts (add updateTherapyMethod; reuse patterns)
    - backend/src/db/schema/users.schema.ts (metodeTerapiAktif, tanggalMulaiTerapi, riwayatTerapi columns from Plan 01)
    - backend/src/app.ts (route mount pattern + middleware order)
    - .planning/research/ARCHITECTURE.md (ReminderSchedule entity shape, config/therapyRules.js intent; layered Pattern 1)
    - .planning/phases/01-foundation-auth-onboarding/01-CONTEXT.md (D-02..D-09: inline expand, 1-2 sentence calm copy, per-therapy illustration colors, 3-step wizard, ONE reminder required to proceed, skip allowed)
    - PRD.md section 8.1 + 8.4 (Pengguna therapy fields; ReminderSchedule attributes)
  </read_first>
  <behavior>
    - Test 1: saveTherapyMethod(userId, 'CAPD') sets users.metodeTerapiAktif='CAPD', sets tanggalMulaiTerapi to today, and advances onboarding_progress.lastCompletedStep to the therapy step.
    - Test 2: saveTherapyMethod rejects an invalid value (not in CAPD|HD|Transplantasi) with a validation error.
    - Test 3: saveFirstReminder(userId, validReminder) creates a reminder_schedule row tied to the user and marks onboarding complete (completedAt set).
    - Test 4: skipFirstReminder(userId) marks onboarding complete WITHOUT creating a reminder, and getProgress reflects that no reminder was configured (so the dashboard can show the banner — ONBOARD-03).
    - Test 5: getProgress(userId) for a user who only finished the therapy step returns lastCompletedStep = therapy step (so the frontend resumes at the reminder step — ONBOARD-04).
  </behavior>
  <action>
    Add Drizzle tables: `onboarding_progress` (userId FK->users UNIQUE, lastCompletedStep int not null default 0, reminderConfigured boolean default false, completedAt timestamp nullable, updatedAt timestamp) and `reminder_schedule` (id uuid PK, userId FK, jenis text — 'obat'|'capd'|'hd', nama text, jamPengingat text/time, hariAktif jsonb default '[]', catatanWaktu text nullable, aktif boolean default true, createdAt) — keep columns forward-compatible with ARCHITECTURE.md ReminderSchedule (Phase 2 extends; do NOT build push/cron here). Export from index.ts; generate+apply migration.

    `config/therapyContent.ts`: export the three therapies with id (CAPD|HD|Transplantasi), nama, namaPanjang, a 1-2 sentence CALM Bahasa Indonesia penjelasan (D-03/D-05 tone, e.g. "CAPD adalah cuci darah mandiri di rumah pakai cairan khusus, dilakukan 3-5x sehari. Banyak pasien menjalaninya dengan nyaman setelah terbiasa."), and the DESIGN_SYSTEM therapy identity colors: CAPD teal #2a9d8f / bg #f0faf9, HD amber #ef9f27 / bg #fdf3e3, Transplantasi purple #6b5ca5 / bg #f1eef9. Expose this via `GET /api/onboarding/therapy-content` so the frontend renders identical copy (no business content hardcoded in the frontend).

    Layered service (ARCHITECTURE.md): repositories hold SQL only. `onboarding.service.ts` exports saveTherapyMethod, saveFirstReminder, skipFirstReminder, getProgress — validate inputs with zod (therapy enum; reminder fields), update the user/therapy + progress within the service. saveTherapyMethod also seeds riwayatTerapi with the initial entry (so Plan 05's change-history has a baseline). Controllers thin; routes under `/api/onboarding`, ALL behind the authenticate middleware: `GET /progress`, `GET /therapy-content`, `POST /therapy`, `POST /reminder`, `POST /skip-reminder`. Add `userRepository.updateTherapyMethod`.

    Write the failing test (onboarding.service.test.ts) first covering the five behaviors, then implement to green.
  </action>
  <verify>
    <automated>cd backend && npm test -- onboarding.service</automated>
    <automated>cd backend && grep -q "pgTable" src/db/schema/onboardingProgress.schema.ts && grep -q "pgTable" src/db/schema/reminderSchedule.schema.ts && echo OK</automated>
    <automated>cd backend && grep -Eq "CAPD|HD|Transplantasi" src/config/therapyContent.ts && grep -q "6b5ca5" src/config/therapyContent.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - onboarding.service.test.ts passes all five behavior cases.
    - onboarding_progress and reminder_schedule tables exist (migration applied), reminder_schedule columns forward-compatible with ARCHITECTURE.md ReminderSchedule.
    - therapyContent.ts contains all three therapies with the correct DESIGN_SYSTEM identity colors (including Transplantasi #6b5ca5) and calm 1-2 sentence explanations.
    - All /api/onboarding routes require authentication (401 without a valid access token).
    - saveTherapyMethod seeds riwayatTerapi with the initial therapy entry.
  </acceptance_criteria>
  <done>Backend persists therapy method (with history baseline), creates or skips the first reminder, and exposes resumable progress + therapy explanation content — all behind auth and layered cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: 3-step onboarding wizard UI with inline Apa ini?, resume, skip, and success animation</name>
  <files>frontend/app/onboarding/page.tsx, frontend/app/onboarding/_components/StepProgress.tsx, frontend/app/onboarding/_components/TherapySelectStep.tsx, frontend/app/onboarding/_components/FirstReminderStep.tsx, frontend/app/onboarding/_components/OnboardingSuccess.tsx, frontend/app/dashboard/page.tsx, frontend/lib/validators/onboarding.schema.ts</files>
  <read_first>
    - frontend/lib/hooks/useAuth.ts (auth state; redirect unauthenticated users to /login)
    - frontend/lib/api.ts (apiFetch)
    - frontend/app/dashboard/page.tsx (Plan 02 — add the skip-reminder banner)
    - DESIGN_SYSTEM_KidneyBuddy_v3.md (therapy identity colors, Button Primary pill, Card specs, Plus Jakarta Sans/DM Sans, cream bg, no pure black)
    - .planning/phases/01-foundation-auth-onboarding/01-PATTERNS.md (onboarding/page.tsx gradient-header shell, TherapySelectStep selectable-card + accordion inline-expand, CTA disabled-until-selected pattern, StepProgress from shadcn progress)
    - .planning/phases/01-foundation-auth-onboarding/01-CONTEXT.md (D-02 inline expand not modal; D-04 illustration per therapy; D-06 step indicator 1/3 2/3 3/3; D-07 Kembali at every step; D-08 1-2s success animation auto-redirect; D-09 only ONE reminder type required to proceed)
    - KidneyBuddy_Design/src/app/App.tsx (Onboarding function lines 590-702 — VISUAL reference only; rebuild fresh in Next.js, do NOT port)
  </read_first>
  <action>
    `onboarding.schema.ts`: zod schemas for the therapy step (enum CAPD|HD|Transplantasi) and the first-reminder step (D-09: at least one reminder type filled — e.g. either a medication reminder OR a CAPD/HD reminder — enable "Lanjutkan" only when one is complete).

    `page.tsx` (wizard shell): client component; on load redirect to /login if unauthenticated, then call `GET /api/onboarding/progress` to resume at the correct step (ONBOARD-04). Render the gradient-header + scrollable-body shell (PATTERNS.md), with `<StepProgress>` between header and body, a "Kembali" back button at every step (D-07), and the three steps. Use the design tokens (cream bg, teal gradient header).

    `StepProgress.tsx` (D-06): discrete 1/3, 2/3, 3/3 indicator (dots/fractions), built on the shadcn Progress primitive concept but discrete — visible step count so 50+ users always know how many steps remain.

    `TherapySelectStep.tsx` (ONBOARD-02, D-02..D-05): fetch `GET /api/onboarding/therapy-content`, render a selectable card per therapy (selected-state border/bg swap + checkmark per PATTERNS.md), each card with an inline "Apa ini?" that expands the card downward (accordion-style animate, NOT a modal — D-02) to reveal the calm penjelasan (D-03/D-05) and a per-therapy illustration/color block (D-04). "Lanjutkan" disabled until a therapy is selected; on continue call `POST /api/onboarding/therapy`.

    `FirstReminderStep.tsx` (ONBOARD-01/03, D-09): a minimal first-reminder form (Phase 1 only — name, time, type; full reminder UX is Phase 2). "Lanjutkan/Simpan & Mulai" enabled once ONE reminder type is filled (D-09); a "Lewati untuk sekarang" link (ONBOARD-03) calls `POST /api/onboarding/skip-reminder`. Saving calls `POST /api/onboarding/reminder`.

    `OnboardingSuccess.tsx` (D-08): a 1-2 second success animation ("KidneyBuddy siap mendampingi kamu!") that auto-redirects to /dashboard — no manual button.

    `dashboard/page.tsx`: if onboarding progress indicates the reminder was skipped (reminderConfigured=false), show a calm banner reminding the user to configure a reminder later (ONBOARD-03), using the warning/AI-info card token.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && grep -q "Apa ini" app/onboarding/_components/TherapySelectStep.tsx && grep -Eq "Kembali" app/onboarding/page.tsx && echo OK</automated>
    <automated>cd frontend && grep -q "skip-reminder\|Lewati" app/onboarding/_components/FirstReminderStep.tsx && grep -q "progress" app/onboarding/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Wizard shows a 1/3 -> 2/3 -> 3/3 step indicator and a "Kembali" button at every step (D-06/D-07).
    - Each therapy card has an inline "Apa ini?" that expands in place (not a modal) showing the calm explanation + per-therapy color/illustration (D-02..D-05); therapy copy comes from the backend therapy-content endpoint.
    - "Lanjutkan" on the therapy step is disabled until a therapy is selected; first-reminder step enables proceed once ONE reminder type is filled (D-09) and offers "Lewati untuk sekarang" (ONBOARD-03).
    - On load the wizard resumes at the last completed step from GET /progress (ONBOARD-04).
    - Success shows a brief animation that auto-redirects to /dashboard (D-08, no manual button).
    - Dashboard shows the configure-reminder banner when the reminder was skipped.
    - frontend tsc passes.
  </acceptance_criteria>
  <done>A new user completes the 3-step onboarding (therapy + reminder/skip + success) with inline explanations, can go back, resumes if interrupted, and lands on the dashboard (with a banner if they skipped the reminder).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full interactive onboarding wizard: therapy selection with inline "Apa ini?" explanations, set-or-skip first reminder, resume-on-reopen, and a success animation into the dashboard.</what-built>
  <how-to-verify>
    1. With `docker-compose up` running, register a NEW account (or log in with an account that has not onboarded). Confirm you are taken to /onboarding.
    2. Step 1/3: confirm the step indicator shows 1/3. On a therapy card, tap "Apa ini?" — confirm the card expands IN PLACE (no modal) showing a short calm explanation and the per-therapy color (CAPD teal, HD amber, Transplantasi purple). Select a therapy; confirm "Lanjutkan" enables.
    3. Confirm a "Kembali" button is present at each step.
    4. Step 2/3 (first reminder): confirm "Lanjutkan/Simpan" stays disabled until you fill one reminder, and that "Lewati untuk sekarang" is available.
    5. Resume test: midway (after step 1), refresh/close and reopen /onboarding — confirm it resumes at step 2, not step 1.
    6. Skip path: choose "Lewati untuk sekarang" — confirm the success animation plays (~1-2s) and auto-redirects to the dashboard, which shows a calm banner reminding you to configure a reminder.
    7. (Time check) The whole flow should be completable comfortably under 5 minutes.
  </how-to-verify>
  <resume-signal>Type "approved" if onboarding (inline explain, back, resume, skip+banner, success redirect) works, or describe what failed.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols/paths created by THIS plan:
- Files: backend/src/db/schema/{onboardingProgress.schema,reminderSchedule.schema}.ts, backend/src/services/onboarding.service.ts, backend/src/repositories/{onboardingProgress.repository,reminderSchedule.repository}.ts, backend/src/controllers/onboarding.controller.ts, backend/src/routes/onboarding.routes.ts, backend/src/config/therapyContent.ts, backend/test/onboarding.service.test.ts, frontend/app/onboarding/page.tsx, frontend/app/onboarding/_components/{StepProgress,TherapySelectStep,FirstReminderStep,OnboardingSuccess}.tsx, frontend/lib/validators/onboarding.schema.ts
- Exports: saveTherapyMethod, saveFirstReminder, skipFirstReminder, getProgress (onboarding.service); updateTherapyMethod (user.repository extension); therapyContent (config)
- DB objects: onboarding_progress table, reminder_schedule table (Phase-1 subset)
- Routes: GET /api/onboarding/progress, GET /api/onboarding/therapy-content, POST /api/onboarding/therapy, POST /api/onboarding/reminder, POST /api/onboarding/skip-reminder
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Backend (onboarding) | Authenticated user input (therapy choice, reminder); must not be settable for another user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Elevation of Privilege | onboarding writes for another user | mitigate | All /api/onboarding routes use the authenticate middleware and act on req.user.id only — never a userId from the request body. |
| T-01-15 | Tampering | invalid therapy/reminder payload | mitigate | zod-validate therapy enum + reminder fields server-side in onboarding.service. |
| T-01-16 | Information Disclosure | therapy content (medical-adjacent copy) | accept | Therapy explanations are general public education text (non-diagnostic, calm tone per D-05); no sensitive data exposed. |
| T-01-SC | Tampering | npm installs (shadcn accordion/progress primitives) | mitigate | shadcn components regenerated via the official CLI (STACK.md HIGH-confidence); no new runtime package outside STACK.md; no [ASSUMED]/[SUS]/[SLOP] packages. |
</threat_model>

<verification>
- backend onboarding.service.test.ts passes.
- onboarding_progress + reminder_schedule tables created; therapyContent has all three therapies with correct colors.
- frontend tsc passes; inline "Apa ini?", Kembali, resume, skip-banner present.
- Manual checkpoint confirms the full wizard.
</verification>

<success_criteria>
- ONBOARD-01: new user completes register -> therapy select -> set/skip first reminder, comfortably under 5 minutes.
- ONBOARD-02: therapy method selected with inline "Apa ini?" explanation (D-02..D-05).
- ONBOARD-03: first reminder can be skipped; dashboard shows a configure-reminder banner.
- ONBOARD-04: onboarding resumes from the last completed step after an interruption.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-04-SUMMARY.md` when done.
</output>
