---
phase: 05-ai-insights-anomaly-detection
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - backend/src/services/aiLabAnalysis.service.ts
  - backend/src/services/aiLifestyle.service.ts
  - backend/src/repositories/aiLabAnalysis.repository.ts
  - backend/src/repositories/aiLifestyleSuggestion.repository.ts
  - backend/src/services/labResult.service.ts
  - backend/src/controllers/ai.controller.ts
  - backend/src/routes/ai.routes.ts
autonomous: true
requirements: [AI-03, AI-04, AI-05]

must_haves:
  truths:
    - "Saving a manual lab result triggers a non-blocking async lab analysis; the save returns immediately (AI-03, D-14)"
    - "Lab analysis explains out-of-range values in plain Bahasa Indonesia without diagnosing, cached per lab_result_id (AI-03, D-16)"
    - "Lifestyle suggestions generate from >=3 days tracking + latest lab + therapy method, gated and cached per user+date (AI-04, D-16)"
    - "Both outputs have the fixed disclaimer appended by the backend (AI-05, D-19)"
  artifacts:
    - path: "backend/src/services/aiLabAnalysis.service.ts"
      provides: "AI-03 lab analysis generate+cache, non-blocking trigger"
      exports: ["generateAndCacheLabAnalysis", "getLabAnalysis"]
    - path: "backend/src/services/aiLifestyle.service.ts"
      provides: "AI-04 lifestyle suggestion generate+cache with >=3-day gate"
      exports: ["generateAndCacheLifestyle", "getLifestyleSuggestion"]
  key_links:
    - from: "backend/src/services/labResult.service.ts"
      to: "aiLabAnalysis.generateAndCacheLabAnalysis"
      via: "fire-and-forget after createEntry"
      pattern: "generateAndCacheLabAnalysis"
    - from: "backend/src/routes/ai.routes.ts"
      to: "/lab-analysis + /lifestyle"
      via: "authenticated GET routes"
      pattern: "lab-analysis|lifestyle"
---

<objective>
Add the two remaining AI-content surfaces: per-lab-result analysis (AI-03, triggered non-blocking on manual lab save per D-14, cached per lab_result_id) and personalized lifestyle suggestions (AI-04, gated on >=3 days of tracking data, cached per user+date). Both reuse the generate-or-cache + disclaimer pattern established in 05-05 and extend `/api/ai`.

Purpose: Completes the AI-content backend (AI-03/AI-04) so 05-07 can render the Lab-tab and Edukasi cards.
Output: aiLabAnalysis/aiLifestyle services + repositories, the non-blocking trigger in labResult.service.ts, and new /api/ai endpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-ai-insights-anomaly-detection/05-CONTEXT.md
@.planning/phases/05-ai-insights-anomaly-detection/05-RESEARCH.md
@.planning/phases/05-ai-insights-anomaly-detection/05-PATTERNS.md
@backend/src/services/labResult.service.ts
@backend/src/services/aiSummary.service.ts
@backend/src/controllers/ai.controller.ts
@backend/src/routes/ai.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lab analysis service + repository + non-blocking trigger on lab save (AI-03, D-14)</name>
  <files>backend/src/services/aiLabAnalysis.service.ts, backend/src/repositories/aiLabAnalysis.repository.ts, backend/src/services/labResult.service.ts</files>
  <read_first>
    - backend/src/services/aiSummary.service.ts (generate-or-cache + appendDisclaimer + encrypt pattern from 05-05 — mirror it)
    - backend/src/services/labResult.service.ts (createEntry — where the non-blocking trigger attaches)
    - .planning/phases/05-ai-insights-anomaly-detection/05-CONTEXT.md (D-14 non-blocking, D-16 cache-by-lab_result_id)
    - backend/src/lib/aiDisclaimer.ts, backend/src/lib/encryption.ts
  </read_first>
  <behavior>
    - generateAndCacheLabAnalysis(userId, labResultId): cache-hit by labResultId returns cached; else fetches the lab result values, calls Groq to explain out-of-range values against general CKD reference ranges in plain Bahasa Indonesia WITHOUT diagnosing, appends disclaimer (D-19), encrypts, upserts (unique labResultId).
    - labResult.service.ts createEntry: after the save completes and returns, fire-and-forget the analysis (never awaited before the save response — D-14). Lab save must not depend on Groq availability.
  </behavior>
  <action>
    Create `aiLabAnalysis.repository.ts` (`findByLabResultId(userId, labResultId)`, `upsertAnalysis(...)`, userId-scoped for IDOR safety even though keyed by labResultId). Create `aiLabAnalysis.service.ts` mirroring aiSummary's shape with the AI-03 system prompt (plain-language, no diagnosis, general CKD reference ranges) + appendDisclaimer + encrypt. In `labResult.service.ts::createEntry`, after the successful save/return, add a fire-and-forget `generateAndCacheLabAnalysis(userId, saved.id).catch(err => logger.error(...))` placed so it never blocks or delays the save response (D-14).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && grep -q "generateAndCacheLabAnalysis" src/services/labResult.service.ts && grep -q "appendDisclaimer" src/services/aiLabAnalysis.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `labResult.service.ts` fire-and-forgets the analysis after the save returns (not awaited before response).
    - `aiLabAnalysis.service.ts` caches by labResultId, appends disclaimer, encrypts; system prompt forbids diagnosis.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Lab analysis generates asynchronously on save without blocking the lab save flow.</done>
</task>

<task type="auto">
  <name>Task 2: Lifestyle suggestion service + repository + gate (AI-04)</name>
  <files>backend/src/services/aiLifestyle.service.ts, backend/src/repositories/aiLifestyleSuggestion.repository.ts</files>
  <read_first>
    - backend/src/services/aiSummary.service.ts (generate-or-cache pattern — mirror)
    - .planning/phases/05-ai-insights-anomaly-detection/05-RESEARCH.md (Open Question 1 + Assumption A2/A3 — gate on >=3 days tracking per approved UI-SPEC; cache per user+date)
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Empty/Gated States — "<3 days tracking data" copy)
    - backend/src/lib/aiDisclaimer.ts
  </read_first>
  <behavior>
    - getLifestyleSuggestion(userId): if the user has <3 days of tracking data, return a gated empty-state marker (the UI renders "Saran Belum Tersedia") — do NOT call Groq. Else cache-hit-or-generate per user+date.
    - generateAndCacheLifestyle(userId): gathers >=3 days tracking + latest lab result + active therapy method, Groq narration of concrete food/lifestyle suggestions in Bahasa Indonesia, appendDisclaimer (D-19), encrypt, upsert per user+date (Assumption A2).
  </behavior>
  <action>
    Create `aiLifestyleSuggestion.repository.ts` (`findByUserAndDate(userId, tanggal)`, `upsertSuggestion(...)`, plus a helper or reuse to count distinct tracking days). Create `aiLifestyle.service.ts` per the behavior contract: gate on >=3 days tracking (per the approved UI-SPEC — flag in the SUMMARY that PRD FR-SYS-006 additionally wants >=1 lab result, deferred to UI-SPEC's simpler gate per Assumption A3), lazy-generate-on-view cached per user+date, disclaimer appended, encrypted. No manual regenerate button for this surface (D-13 simplicity).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && grep -q "appendDisclaimer" src/services/aiLifestyle.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `aiLifestyle.service.ts` returns a gated marker (no Groq call) when tracking data < 3 days.
    - Suggestion cached per user+date, disclaimer appended, encrypted.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Lifestyle suggestion generates lazily with the >=3-day gate.</done>
</task>

<task type="auto">
  <name>Task 3: ai.controller + ai.routes additions for lab analysis + lifestyle</name>
  <files>backend/src/controllers/ai.controller.ts, backend/src/routes/ai.routes.ts</files>
  <read_first>
    - backend/src/controllers/ai.controller.ts (existing handlers from 05-05 — add alongside)
    - backend/src/routes/ai.routes.ts (existing routes from 05-05 — add alongside, keep authenticate)
    - backend/src/controllers/fluid.controller.ts (404-on-missing + thin controller pattern)
  </read_first>
  <action>
    Add to `ai.controller.ts`: `getLabAnalysis` (GET `/lab-analysis/:labResultId` — cache read, returns the loading/empty shape if not yet generated per D-14 async), `getLifestyleSuggestion` (GET `/lifestyle` — returns the suggestion or the gated empty-state marker). Add the two authenticated routes to `ai.routes.ts` (`GET /lab-analysis/:labResultId`, `GET /lifestyle`), every route keeps `authenticate`. Do not disturb the 05-05 routes.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && grep -q "lab-analysis" src/routes/ai.routes.ts && grep -q "lifestyle" src/routes/ai.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ai.routes.ts` exposes authenticated `GET /lab-analysis/:labResultId` and `GET /lifestyle`.
    - `getLabAnalysis` returns a not-yet-ready shape when the async analysis has not completed (D-14).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Lab analysis + lifestyle endpoints live and authenticated.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- New files: `backend/src/services/aiLabAnalysis.service.ts`, `backend/src/services/aiLifestyle.service.ts`, `backend/src/repositories/aiLabAnalysis.repository.ts`, `backend/src/repositories/aiLifestyleSuggestion.repository.ts`.
- New exported symbols: `generateAndCacheLabAnalysis`, `getLabAnalysis`, `generateAndCacheLifestyle`, `getLifestyleSuggestion`, `findByLabResultId`/`upsertAnalysis`, `findByUserAndDate`/`upsertSuggestion` (lifestyle repo).
- New endpoints: `GET /api/ai/lab-analysis/:labResultId`, `GET /api/ai/lifestyle`.
- Modified: `labResult.service.ts` createEntry gains a non-blocking analysis trigger.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/ai lab-analysis/lifestyle | Health-derived AI output; user-scoped |
| lab values -> Groq | user-entered lab data flows into the analysis prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Elevation of Privilege | `/api/ai/lab-analysis/:labResultId` | mitigate | Repository scopes by `req.user!.id` in addition to labResultId; a bare labResultId cannot read another user's analysis. |
| T-05-15 | Availability | Groq failure during lab save | mitigate | Analysis is fire-and-forget after the save returns (D-14); lab save never depends on Groq. |
| T-05-16 | Information Disclosure | lab-analysis/lifestyle cache columns | mitigate | Stored as AES-256-GCM ciphertext; disclaimer backend-appended; API key never logged. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; full `npm test` suite green.
- grep confirms non-blocking trigger, disclaimer append, and new /api/ai routes.
</verification>

<success_criteria>
- Lab analysis generates asynchronously on save, cached per lab_result_id, no diagnosis.
- Lifestyle suggestion gated on >=3 days, cached per user+date.
- Both enforce the disclaimer server-side and are user-scoped.
</success_criteria>

<output>
Create `.planning/phases/05-ai-insights-anomaly-detection/05-06-SUMMARY.md` when done.
</output>
