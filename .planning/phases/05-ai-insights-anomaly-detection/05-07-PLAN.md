---
phase: 05-ai-insights-anomaly-detection
plan: 07
type: execute
wave: 5
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - frontend/components/beranda/AiDailySummaryCard.tsx
  - frontend/components/lab/WeeklyInsightCard.tsx
  - frontend/components/lab/LabAnalysisCard.tsx
  - frontend/components/edukasi/LifestyleSuggestionCard.tsx
  - frontend/app/(app)/beranda/page.tsx
  - frontend/app/(app)/catatan/page.tsx
  - frontend/app/(app)/edukasi/page.tsx
  - frontend/components/laporan/sections/Anomali.tsx
  - backend/src/repositories/report.repository.ts
  - backend/src/services/report.service.ts
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "Beranda shows the real daily AI summary card (replacing AiPlaceholderCard) with a manual regenerate button (AI-01, D-10)"
    - "The Lab tab shows the weekly insight card and per-result lab analysis card near LabTrendChart (AI-02, AI-03, D-11)"
    - "The Edukasi page shows the lifestyle suggestion card with its >=3-day gated empty state (AI-04, D-12)"
    - "Every AI card renders the backend-appended disclaimer verbatim (AI-05, D-19)"
    - "The report Anomali section renders real anomaly_alerts for the report date range (D-15)"
  artifacts:
    - path: "frontend/components/beranda/AiDailySummaryCard.tsx"
      provides: "Real daily summary card (AI-01)"
    - path: "frontend/components/lab/WeeklyInsightCard.tsx"
      provides: "Weekly insight card (AI-02)"
    - path: "frontend/components/lab/LabAnalysisCard.tsx"
      provides: "Async lab analysis card (AI-03)"
    - path: "frontend/components/edukasi/LifestyleSuggestionCard.tsx"
      provides: "Lifestyle suggestion card (AI-04)"
  key_links:
    - from: "frontend/app/(app)/beranda/page.tsx"
      to: "AiDailySummaryCard"
      via: "replaces AiPlaceholderCard"
      pattern: "AiDailySummaryCard"
    - from: "frontend/components/laporan/sections/Anomali.tsx"
      to: "anomaly_alerts range query"
      via: "report.repository.getAnomaliesByRange"
      pattern: "Anomali"
---

<objective>
Surface all four AI-content outputs in their locked UI slots (D-10/D-11/D-12/D-13): the daily summary card on Beranda (replacing AiPlaceholderCard, with manual regenerate), the weekly insight + per-result lab analysis cards on the Lab tab, and the lifestyle suggestion card on Edukasi — each rendering the backend disclaimer verbatim. Also wire the doctor report's "Anomali Terdeteksi" section to real `anomaly_alerts` data for the report date range (D-15).

Purpose: Completes the phase's user-facing AI-content surfaces and the report integration, closing AI-01..04 end-to-end.
Output: 4 AI content cards + 3 page integrations + report anomaly query and section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-ai-insights-anomaly-detection/05-CONTEXT.md
@.planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md
@.planning/phases/05-ai-insights-anomaly-detection/05-PATTERNS.md
@frontend/components/beranda/AiPlaceholderCard.tsx
@frontend/components/beranda/CAPDEffluentBanner.tsx
@frontend/components/lab/LabTrendChart.tsx
@frontend/components/laporan/sections/Anomali.tsx
@backend/src/repositories/report.repository.ts
@backend/src/services/report.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AiDailySummaryCard on Beranda (replaces AiPlaceholderCard, AI-01/D-10)</name>
  <files>frontend/components/beranda/AiDailySummaryCard.tsx, frontend/app/(app)/beranda/page.tsx</files>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contract 2 — full AiDailySummaryCard spec: states, hex, copy, disclaimer placement)
    - frontend/components/beranda/AiPlaceholderCard.tsx (card shell to replace + its slot in beranda/page.tsx)
    - frontend/components/beranda/CAPDEffluentBanner.tsx (authFetch + loading-state action pattern)
  </read_first>
  <action>
    Create `AiDailySummaryCard.tsx` per UI-SPEC Contract 2: Alert AI/Info gradient container (16px radius, 16px padding), header row with 30x30 icon container (Sparkles 18px `#2a9d8f`) + "Ringkasan AI Hari Ini" (14px PJS 700) + top-right "AI" badge chip. Fetch `GET /api/ai/daily-summary` via authFetch. Render the four body states (not-generated -> "Buat Ringkasan" teal pill button; generating -> "Membuat ringkasan..." disabled + spinner; generated -> narrative + "Buat Ulang Ringkasan" outline pill + "Dibuat pukul [HH:mm]"; error -> "Ringkasan tidak tersedia saat ini. Coba lagi nanti." + retry button). The regenerate button POSTs `/api/ai/daily-summary/regenerate`. Render the disclaimer verbatim from the backend response (10px DMS 500 italic `#7a8c8a`, mt-8px) — never omit or shorten it. In `beranda/page.tsx`, replace `<AiPlaceholderCard />` with `<AiDailySummaryCard />` in the same `md:col-span-3` slot (no layout reflow; AiPlaceholderCard file untouched, just no longer imported).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "AiDailySummaryCard" 'app/(app)/beranda/page.tsx' && ! grep -q "AiPlaceholderCard" 'app/(app)/beranda/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `AiDailySummaryCard.tsx` renders all four states and the verbatim disclaimer from the backend.
    - Regenerate button POSTs to `/api/ai/daily-summary/regenerate`.
    - `beranda/page.tsx` renders `<AiDailySummaryCard />` and no longer imports `AiPlaceholderCard`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Beranda shows the real daily summary with manual regenerate.</done>
</task>

<task type="auto">
  <name>Task 2: WeeklyInsightCard + LabAnalysisCard on Lab tab; LifestyleSuggestionCard on Edukasi (AI-02/03/04)</name>
  <files>frontend/components/lab/WeeklyInsightCard.tsx, frontend/components/lab/LabAnalysisCard.tsx, frontend/components/edukasi/LifestyleSuggestionCard.tsx, frontend/app/(app)/catatan/page.tsx, frontend/app/(app)/edukasi/page.tsx</files>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contracts 4 + 5 — WeeklyInsight/LabAnalysis/Lifestyle specs, icons Lightbulb/FlaskConical/Leaf, empty/loading/error copy)
    - frontend/components/lab/LabTrendChart.tsx (Lab tab placement + card conventions)
    - frontend/app/(app)/catatan/page.tsx (Lab tab panel — where cards mount above the chart)
    - frontend/app/(app)/edukasi/page.tsx (existing "Konten Segera Hadir" empty state to place the card above)
  </read_first>
  <action>
    Create the three cards sharing the Alert AI/Info gradient container + AI badge chip + verbatim disclaimer:
    `WeeklyInsightCard.tsx` (Lightbulb icon, "Wawasan Tren Mingguan", fetch `GET /api/ai/weekly-insight`, sub-line "Berdasarkan data 7-30 hari terakhir", empty state "Belum Ada Wawasan Minggu Ini", no regenerate button).
    `LabAnalysisCard.tsx` (FlaskConical icon, "Analisis Hasil Lab", fetch `GET /api/ai/lab-analysis/:labResultId`, "Menganalisis hasil lab..." loading state while the async analysis completes per D-14, out-of-range figures inline-colored `#d4183d` within the text — not a red card/background, error "Analisis tidak tersedia saat ini.").
    `LifestyleSuggestionCard.tsx` (Leaf icon, "Saran Gaya Hidup untuk Anda", fetch `GET /api/ai/lifestyle`, gated empty state "Saran Belum Tersedia" / "Saran personal akan muncul setelah Anda mencatat data selama 3 hari." using the Phase 4 empty-state icon convention).
    In `catatan/page.tsx` render `<WeeklyInsightCard>` and `<LabAnalysisCard>` (per newly-saved result) ABOVE `<LabTrendChart>` inside the existing Lab tab panel. In `edukasi/page.tsx` render `<LifestyleSuggestionCard>` ABOVE the existing "Konten Segera Hadir" empty state (which stays — Phase 6 scope).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "WeeklyInsightCard" 'app/(app)/catatan/page.tsx' && grep -q "LabAnalysisCard" 'app/(app)/catatan/page.tsx' && grep -q "LifestyleSuggestionCard" 'app/(app)/edukasi/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - All three cards render the Alert AI/Info gradient + AI badge chip + verbatim disclaimer.
    - `LabAnalysisCard` shows the "Menganalisis hasil lab..." loading state (D-14 async) and colors only out-of-range figures red (not the whole card).
    - `LifestyleSuggestionCard` shows the gated empty state when tracking data < 3 days.
    - Lab tab renders both cards above `<LabTrendChart>`; Edukasi renders the card above the existing empty state.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Weekly insight, lab analysis, and lifestyle cards live in their locked slots.</done>
</task>

<task type="auto">
  <name>Task 3: Report Anomali section wired to real anomaly_alerts (D-15)</name>
  <files>backend/src/repositories/report.repository.ts, backend/src/services/report.service.ts, frontend/components/laporan/sections/Anomali.tsx</files>
  <read_first>
    - backend/src/repositories/report.repository.ts (existing getFluidSummaryByRange etc. — mirror the WIB-correct range-aggregation shape)
    - backend/src/repositories/anomalyAlert.repository.ts (findByUserAndRange from 05-02 — reuse or mirror)
    - backend/src/services/report.service.ts (where the report payload is assembled; anomalies is currently a placeholder)
    - frontend/components/laporan/sections/Anomali.tsx (existing placeholder — keep the section container/heading, replace only inner content)
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contract 7 — Anomali table columns, severity colors, print compatibility)
  </read_first>
  <action>
    Add `getAnomaliesByRange(userId, startDate, endDate)` to `report.repository.ts` (WIB-correct bounds, same `and(eq(userId), gte, lte)` shape as the other report queries — or delegate to `anomalyAlert.repository.findByUserAndRange`). Wire it into `report.service.ts` so the report payload's `anomalies` field carries real rows (decrypted deskripsi, tanggal, tipeAnomali, severity) instead of the Phase 4 placeholder.
    In `Anomali.tsx`, keep the `.laporan-section-card` container + teal 3px left border + "Anomali Terdeteksi" heading UNCHANGED; replace only the inner placeholder block with: a table (columns Tanggal | Tipe Anomali | Severity | Deskripsi; header 10px uppercase `#7a8c8a`; rows 12px `#1a2e2c`; Severity "Tinggi" in `#d4183d` bold, "Normal" in `#7a8c8a`, no colored background chips for print compatibility) when >=1 anomaly, or the inline empty-state ("Tidak ada anomali terdeteksi dalam periode yang dipilih.", Lucide 32px `#cfe8e4`) when zero. Inherit Phase 4's `@media print` rules (no new print CSS).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && grep -q "getAnomaliesByRange\|findByUserAndRange" src/services/report.service.ts && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `report.service.ts` populates the report `anomalies` field from real `anomaly_alerts` rows within the date range (no placeholder).
    - `Anomali.tsx` renders the 4-column table (severity color-coded, no bg chips) or the zero-state; the section container/heading are unchanged from Phase 4.
    - Both backend and frontend `npx tsc --noEmit` pass.
  </acceptance_criteria>
  <done>Report Anomali section shows real anomaly data for the selected range, print-compatible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — AI content surfaces + report</name>
  <what-built>All four AI-content cards (daily summary, weekly insight, lab analysis, lifestyle) in their locked slots, plus the report Anomali section wired to real data.</what-built>
  <action>Pause for the human to run the app and verify the AI content surfaces and report section below. Do not close the phase until the human types "approved".</action>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contracts 2, 4, 5, 7)
  </read_first>
  <how-to-verify>
    1. On Beranda, confirm the daily summary card (not the old grey placeholder): tap "Buat Ringkasan", confirm a Bahasa Indonesia narrative appears with the verbatim disclaimer at the bottom; tap "Buat Ulang Ringkasan" and confirm it regenerates.
    2. On the Lab tab (/catatan), confirm "Wawasan Tren Mingguan" and "Analisis Hasil Lab" cards render above the trend chart; save a new manual lab result and confirm the analysis appears asynchronously (the save itself is not blocked) with the disclaimer.
    3. On Edukasi, confirm the "Saran Gaya Hidup untuk Anda" card — gated empty state if <3 days tracking, otherwise a suggestion with disclaimer.
    4. Generate a doctor report over a range that includes at least one anomaly; confirm the "Anomali Terdeteksi" section lists real rows (Tanggal/Tipe/Severity/Deskripsi) and print preview renders them correctly.
  </how-to-verify>
  <acceptance_criteria>
    - All four AI cards render real backend content with the disclaimer.
    - Lab save is not blocked by analysis generation.
    - Report anomaly section shows real data and prints correctly.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- New files: `frontend/components/beranda/AiDailySummaryCard.tsx`, `frontend/components/lab/WeeklyInsightCard.tsx`, `frontend/components/lab/LabAnalysisCard.tsx`, `frontend/components/edukasi/LifestyleSuggestionCard.tsx`.
- New backend symbol: `getAnomaliesByRange` (report.repository) and the real `anomalies` field in the report payload.
- Modified: `beranda/page.tsx` (AiDailySummaryCard replaces AiPlaceholderCard), `catatan/page.tsx` (+2 Lab cards), `edukasi/page.tsx` (+lifestyle card), `Anomali.tsx` (real data).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/ai + /api/report | AI cards and report anomalies are health-derived; user-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Elevation of Privilege | report anomaly query | mitigate | `getAnomaliesByRange`/`findByUserAndRange` scope by `req.user!.id` (report.repository.ts pattern); report already authenticates. |
| T-05-18 | Information Disclosure | disclaimer omission on a compact card | mitigate | Frontend renders the backend-appended disclaimer verbatim; never truncated regardless of card size (D-19). |
| T-05-19 | Information Disclosure | anomaly deskripsi in report | mitigate | Decrypted server-side via lib/encryption only within the authenticated report response; never exposed cross-user. |
</threat_model>

<verification>
- Backend + frontend `npx tsc --noEmit` pass.
- Full `npm test` suite green.
- Human checkpoint confirms all four AI cards + report anomaly section.
</verification>

<success_criteria>
- All four AI outputs render in their locked slots with the verbatim disclaimer.
- Lab analysis is async and non-blocking on the client.
- Report Anomali section shows real anomaly data, print-compatible.
</success_criteria>

<output>
Create `.planning/phases/05-ai-insights-anomaly-detection/05-07-SUMMARY.md` when done.
</output>
