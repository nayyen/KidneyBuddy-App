---
phase: 05-ai-insights-anomaly-detection
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - frontend/components/anomaly/EmergencyAnomalyModal.tsx
  - frontend/components/anomaly/AnomalyAlertCard.tsx
  - frontend/components/anomaly/AlertHistoryList.tsx
  - frontend/components/beranda/AnomalyAlertSection.tsx
  - frontend/app/(app)/notifikasi/page.tsx
  - frontend/components/shell/AppShell.tsx
  - frontend/components/shell/TopBar.tsx
  - frontend/components/shell/MobileHeader.tsx
  - frontend/app/(app)/beranda/page.tsx
autonomous: false
requirements: [ANOMALY-02, ANOMALY-03, ANOMALY-04]

must_haves:
  truths:
    - "A tinggi-severity active alert renders a full-screen non-dismissable modal on every app session until acknowledged (D-05, D-07, D-08)"
    - "The modal's only exit is the acknowledge button, which POSTs to the backend before closing locally (D-05)"
    - "Normal-severity active alerts render as Alert AI/Info cards on Beranda; CAPDEffluentBanner is removed from Beranda (Alert Variant Lock)"
    - "The /notifikasi page lists all alerts with relevan/tidak_relevan feedback buttons (ANOMALY-04, D-09)"
    - "TopBar and MobileHeader bell icons route to /notifikasi with an unread-count amber dot"
  artifacts:
    - path: "frontend/components/anomaly/EmergencyAnomalyModal.tsx"
      provides: "Full-screen non-dismissable emergency modal (D-05)"
    - path: "frontend/components/anomaly/AlertHistoryList.tsx"
      provides: "Alert history list + feedback (ANOMALY-04)"
    - path: "frontend/app/(app)/notifikasi/page.tsx"
      provides: "Alert history page"
  key_links:
    - from: "frontend/components/shell/AppShell.tsx"
      to: "EmergencyAnomalyModal + /api/anomaly/active-high-severity"
      via: "mount + fetch on session"
      pattern: "EmergencyAnomalyModal"
    - from: "frontend/components/anomaly/AlertHistoryList.tsx"
      to: "/api/anomaly/:id/feedback"
      via: "authFetch PATCH"
      pattern: "feedback"
---

<objective>
Build the anomaly-alert user experience against the live `/api/anomaly` backend (05-03): a full-screen non-dismissable emergency modal for tinggi-severity alerts (mounted globally, re-checked every session), normal-severity alert cards on Beranda, and the `/notifikasi` history page with relevan/tidak_relevan feedback. Wire the previously-unused notification bell icons in TopBar/MobileHeader, and remove the now-superseded CAPDEffluentBanner from Beranda.

Purpose: Delivers the user-facing safety mechanism (ANOMALY-02/03/04) — the part patients and caregivers actually see and interact with.
Output: EmergencyAnomalyModal, AnomalyAlertCard, AnomalyAlertSection, AlertHistoryList, /notifikasi page, and shell wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-ai-insights-anomaly-detection/05-CONTEXT.md
@.planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md
@.planning/phases/05-ai-insights-anomaly-detection/05-PATTERNS.md
@frontend/components/beranda/CAPDEffluentBanner.tsx
@frontend/components/shell/AppShell.tsx
@frontend/components/shell/TopBar.tsx
@frontend/components/shell/MobileHeader.tsx
@frontend/app/(app)/beranda/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmergencyAnomalyModal + global AppShell mount + session re-check (D-05/D-07/D-08)</name>
  <files>frontend/components/anomaly/EmergencyAnomalyModal.tsx, frontend/components/shell/AppShell.tsx</files>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen-by-Screen Contract 1 — full non-dismissable AlertDialog spec, exact hex/spacing/copy)
    - frontend/components/beranda/CAPDEffluentBanner.tsx (acknowledge -> authFetch -> local dismiss interaction to reuse)
    - frontend/components/shell/AppShell.tsx (how CatatCairanSheet is mounted globally + persists across nav)
    - frontend/components/ui/alert-dialog.tsx (Radix AlertDialog primitive already installed)
  </read_first>
  <action>
    Create `EmergencyAnomalyModal.tsx` on shadcn `AlertDialog` per UI-SPEC Contract 1: `open` derived from a server-fetched active tinggi alert (never a client-persisted flag), `onOpenChange={() => {}}` no-op, `onEscapeKeyDown`/`onPointerDownOutside` both `preventDefault`, NO AlertDialogCancel, red-tinted overlay `rgba(212,24,61,0.15)`, content per "Alert Darurat" spec (bg `#fdecee`, border-left 3px `#d4183d`, radius 20px, padding 24px, AlertTriangle 28px in 48x48 container, title "Peringatan Kesehatan Penting" 18px PJS 700 `#d4183d` centered, per-type body from the alert, single full-width red button "Saya Mengerti, Hubungi Dokter Segera"). The acknowledge handler POSTs to `/api/anomaly/:id/acknowledge` via `authFetch` and only closes locally on success (surface a retry state on failure — do NOT silently swallow, unlike the CAPD banner, because D-07 depends on server status).
    Mount `<EmergencyAnomalyModal>` globally in `AppShell.tsx` alongside `CatatCairanSheet` (persists across navigation). On every AppShell mount (new session), fetch `/api/anomaly/active-high-severity`; if a tinggi alert with status `aktif` exists, render the modal blocking whatever page loaded underneath, on any route (D-07). Also pass `onNotificationClick={() => router.push("/notifikasi")}` to TopBar and MobileHeader (consumed in Task 3).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "EmergencyAnomalyModal" components/shell/AppShell.tsx && grep -q "active-high-severity" components/shell/AppShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EmergencyAnomalyModal.tsx` has no AlertDialogCancel, preventDefault on escape + pointer-outside, and a single acknowledge button with the exact copy "Saya Mengerti, Hubungi Dokter Segera".
    - Acknowledge POSTs to `/api/anomaly/:id/acknowledge` and closes only on success.
    - `AppShell.tsx` mounts the modal globally and fetches active tinggi alerts on mount.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Emergency modal blocks on tinggi alerts every session; acknowledge is server-backed.</done>
</task>

<task type="auto">
  <name>Task 2: AnomalyAlertCard + AnomalyAlertSection on Beranda; remove CAPDEffluentBanner</name>
  <files>frontend/components/anomaly/AnomalyAlertCard.tsx, frontend/components/beranda/AnomalyAlertSection.tsx, frontend/app/(app)/beranda/page.tsx</files>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contract 3 — AnomalyAlertCard/Section spec; Color contract Alert Variant Lock; Copywriting "Peringatan"/"Lihat Semua Peringatan")
    - frontend/components/beranda/CAPDEffluentBanner.tsx (Alert AI/Info styling reference, fetch pattern)
    - frontend/app/(app)/beranda/page.tsx (current card layout + where CAPDEffluentBanner renders)
  </read_first>
  <action>
    Create `AnomalyAlertCard.tsx` per UI-SPEC Contract 3: Alert AI/Info gradient (`linear-gradient(135deg,#f0faf9,#e0f5f2)`, border `0.5px solid #cfe8e4`, radius 16px, padding `11px 13px`), 30x30 icon container (AlertTriangle 16px `#2a9d8f`), amber 8x8 unread dot only when status `aktif`, title = backend anomaly-type label, body ellipsized 2-3 lines, timestamp `mt-1.5` relative time. Tapping the card marks aktif->dibaca and navigates to `/notifikasi`.
    Create `AnomalyAlertSection.tsx` (Beranda): fetch the user's alerts; render ONLY if >=1 alert with `status IN (aktif,dibaca)` AND `severity==="normal"` — otherwise return null (no empty state). Heading "Peringatan", up to 3 most-recent cards, and a "Lihat Semua Peringatan" link (teal, ChevronRight) when >3 exist.
    In `beranda/page.tsx`: add `<AnomalyAlertSection />` and REMOVE the `<CAPDEffluentBanner />` render (superseded by EmergencyAnomalyModal per the Alert Variant Lock — do not delete the CAPDEffluentBanner file, just stop rendering it here).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "AnomalyAlertSection" app/\(app\)/beranda/page.tsx && ! grep -q "CAPDEffluentBanner" app/\(app\)/beranda/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AnomalyAlertSection.tsx` returns null when there are zero active/unread normal alerts.
    - `AnomalyAlertCard.tsx` shows the amber unread dot only for status `aktif` and uses the Alert AI/Info gradient (not amber, not red).
    - `beranda/page.tsx` renders `<AnomalyAlertSection />` and no longer renders `<CAPDEffluentBanner />`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Beranda shows normal-severity alerts; CAPD banner superseded by the unified modal.</done>
</task>

<task type="auto">
  <name>Task 3: /notifikasi history page + AlertHistoryList + feedback; bell wiring</name>
  <files>frontend/components/anomaly/AlertHistoryList.tsx, frontend/app/(app)/notifikasi/page.tsx, frontend/components/shell/TopBar.tsx, frontend/components/shell/MobileHeader.tsx</files>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contract 6 — AlertHistoryList/notifikasi; Status Badges + Feedback button styles; Notification Bell states; responsive rules)
    - frontend/components/shell/TopBar.tsx (existing unused onNotificationClick prop + Bell icon)
    - frontend/components/shell/MobileHeader.tsx (currently no icons — add a Bell for parity)
    - frontend/app/(app)/laporan/page.tsx or a Phase 4 page (page-title + error/loading conventions)
  </read_first>
  <action>
    Create `AlertHistoryList.tsx`: fetch ALL alerts (both severities, all statuses) newest-first via `/api/anomaly`; render each with `AnomalyAlertCard` extended to full untruncated body + a status badge (Aktif/Sudah Dibaca/Ditindaklanjuti per the Status Badges table) + a footer with "Relevan"/"Tidak Relevan" feedback pills (shown once status is `dibaca`+). Feedback buttons PATCH `/api/anomaly/:id/feedback` via `authFetch` and reflect selected state (filled; "Tidak Relevan" filled neutral `#7a8c8a`, never red). Tinggi-severity historical items use the Alert Darurat static-card style (red border-left) without modal chrome. Include empty state ("Belum Ada Peringatan"), loading ("Memuat..."), and error ("Gagal memuat riwayat peringatan...") per Copywriting Contract.
    Create `notifikasi/page.tsx`: "Riwayat Peringatan" heading (14px PJS 700), responsive padding (16px mobile / 24px tablet-desktop, max content width 640px centered), renders `<AlertHistoryList>`.
    Wire `TopBar.tsx`: connect the existing unused `onNotificationClick` Bell button; add the 8x8 amber unread-count dot (reflects normal-severity unread count only — tinggi interrupts via modal). Add an equivalent right-aligned 32x32 Bell button + dot to `MobileHeader.tsx` for parity (it currently has none).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "AlertHistoryList" app/\(app\)/notifikasi/page.tsx && grep -q "feedback" components/anomaly/AlertHistoryList.tsx && grep -q "onNotificationClick" components/shell/MobileHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `/notifikasi` page renders `<AlertHistoryList>` with the "Riwayat Peringatan" heading.
    - Feedback pills PATCH `/api/anomaly/:id/feedback`; "Tidak Relevan" selected state is neutral `#7a8c8a`, not red.
    - Both TopBar and MobileHeader have a working Bell button routing to `/notifikasi` with an amber unread dot.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Alert history + feedback shipped; bell entry points wired on desktop and mobile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — emergency modal + alert flows</name>
  <what-built>The full anomaly UI: global emergency modal (D-05/07/08), Beranda alert cards, /notifikasi history + feedback, and bell entry points.</what-built>
  <action>Pause for the human to run the app and verify the anomaly UI flows below. Do not proceed to the next plan until the human types "approved".</action>
  <read_first>
    - .planning/phases/05-ai-insights-anomaly-detection/05-UI-SPEC.md (Screen Contracts 1, 3, 6)
  </read_first>
  <how-to-verify>
    1. Run frontend + backend. Trigger a tinggi-severity alert (e.g., log a CAPD outgoing entry with condition "keruh"/"berdarah" as a CAPD patient).
    2. Confirm a full-screen red modal appears over the current page, cannot be closed by Escape, clicking outside, or a back-button, and only the "Saya Mengerti, Hubungi Dokter Segera" button dismisses it.
    3. Reload the app before acknowledging — confirm the modal re-appears (D-07). Acknowledge, reload again — confirm it does NOT re-appear.
    4. Trigger a normal-severity alert; confirm it appears as a teal Alert AI/Info card in the "Peringatan" section on Beranda (no modal), with an amber unread dot.
    5. Open /notifikasi via the bell icon (test both desktop TopBar and mobile MobileHeader); mark an alert "Relevan" then "Tidak Relevan"; confirm the selected state persists after reload and "Tidak Relevan" is neutral grey, not red.
  </how-to-verify>
  <acceptance_criteria>
    - Modal is genuinely non-dismissable except via the acknowledge button and re-appears until acknowledged.
    - Normal alerts never trigger the modal; only cards.
    - Feedback persists across reload.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- New files: `frontend/components/anomaly/EmergencyAnomalyModal.tsx`, `frontend/components/anomaly/AnomalyAlertCard.tsx`, `frontend/components/anomaly/AlertHistoryList.tsx`, `frontend/components/beranda/AnomalyAlertSection.tsx`, `frontend/app/(app)/notifikasi/page.tsx`.
- New route: `/notifikasi`.
- Modified: AppShell (global modal mount), TopBar/MobileHeader (bell wiring), beranda/page.tsx (section added, CAPDEffluentBanner render removed).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client modal state -> server ack | Modal open-state must derive from server, close must route through the acknowledge POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | Emergency-modal client-side bypass | mitigate | Modal `open` derives from `/api/anomaly/active-high-severity` fetch each session; close only after acknowledge POST succeeds (no client-persisted dismiss flag). |
| T-05-10 | Elevation of Privilege | Feedback/acknowledge from client | mitigate | All calls go through `authFetch` (bearer token); backend scopes by req.user.id (05-03). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for frontend.
- Human checkpoint confirms non-dismissable modal, re-appearance, feedback persistence.
</verification>

<success_criteria>
- Tinggi alerts interrupt via non-dismissable modal on every session until acknowledged.
- Normal alerts surface as Beranda cards + full history on /notifikasi.
- relevan/tidak_relevan feedback works and persists.
- CAPDEffluentBanner superseded on Beranda.
</success_criteria>

<output>
Create `.planning/phases/05-ai-insights-anomaly-detection/05-04-SUMMARY.md` when done.
</output>
