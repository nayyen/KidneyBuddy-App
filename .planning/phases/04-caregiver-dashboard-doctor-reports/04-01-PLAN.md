---
phase: 04-caregiver-dashboard-doctor-reports
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/db/schema/reminderSchedule.schema.ts
  - backend/src/db/migrations/0008_phase4_reminder_updated_at.sql
  - backend/src/test/report.service.test.ts
  - backend/src/test/reminders.controller.test.ts
  - frontend/components/ui/textarea.tsx
autonomous: true
requirements: [CAREGIVER-02, REPORT-01]
must_haves:
  truths:
    - "reminder_schedule table has an updated_at column in the live database (per D-02)"
    - "frontend/components/ui/textarea.tsx exists (shadcn primitive for the doctor note field)"
    - "report.service.test.ts and reminders.controller.test.ts exist and FAIL (RED) because their implementations do not exist yet"
  artifacts:
    - path: "backend/src/db/schema/reminderSchedule.schema.ts"
      provides: "updatedAt timestamp column on reminderSchedule"
      contains: "updatedAt: timestamp(\"updated_at\")"
    - path: "backend/src/db/migrations/0008_phase4_reminder_updated_at.sql"
      provides: "ALTER TABLE migration adding updated_at"
      contains: "ADD COLUMN \"updated_at\""
    - path: "frontend/components/ui/textarea.tsx"
      provides: "shadcn Textarea component for doctor note"
    - path: "backend/src/test/report.service.test.ts"
      provides: "REPORT-01 failing test scaffold (fluid summary, adherence, CAPD, 90-day, WIB)"
    - path: "backend/src/test/reminders.controller.test.ts"
      provides: "CAREGIVER-02 failing test scaffold (push fire-and-forget on update)"
  key_links:
    - from: "backend/src/db/schema/reminderSchedule.schema.ts"
      to: "live postgres reminder_schedule table"
      via: "npx drizzle-kit push"
      pattern: "updated_at"
---

<objective>
Wave 0 foundation for Phase 4. Establish the three prerequisites every later Phase 4 plan depends on: (1) the `updated_at` column on `reminder_schedule` (D-02 change-detection requires it; verified MISSING by research), applied to the live database via a [BLOCKING] schema push; (2) the shadcn `textarea` primitive (verified ABSENT) needed by the doctor-note field; (3) failing test scaffolds for REPORT-01 (`report.service.test.ts`) and CAREGIVER-02 (`reminders.controller.test.ts`) so later waves implement against a RED baseline.

Purpose: Without the `updated_at` column applied to the live DB, build/type checks pass but caregiver sync silently never fires (false-positive verification). Without the test scaffolds, later waves have no Nyquist feedback. Without textarea, the report UI cannot build.
Output: Modified schema, new migration file, applied DB column, textarea component, two failing test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-caregiver-dashboard-doctor-reports/04-RESEARCH.md
@.planning/phases/04-caregiver-dashboard-doctor-reports/04-PATTERNS.md
@.planning/phases/04-caregiver-dashboard-doctor-reports/04-VALIDATION.md
@backend/src/db/schema/reminderSchedule.schema.ts
@backend/src/test/labResult.service.test.ts
</context>

<artifacts_this_phase_produces>
Symbols/files created across all of Phase 4 (so downstream drift checks exclude them):
- DB column: `reminder_schedule.updated_at` / Drizzle field `updatedAt`
- Migration: `backend/src/db/migrations/0008_phase4_reminder_updated_at.sql`
- shadcn component: `frontend/components/ui/textarea.tsx` (exports `Textarea`)
- Tests: `backend/src/test/report.service.test.ts`, `backend/src/test/reminders.controller.test.ts`
- Backend (later plans): `backend/src/repositories/report.repository.ts` (`getFluidSummaryByRange`, `getMedicationAdherenceByRange`, `getCAPDConditionsByRange`), `backend/src/services/report.service.ts` (`reportService`, `_generateReportCore`, `reportQuerySchema`), `backend/src/controllers/report.controller.ts` (`getReport`), `backend/src/routes/report.routes.ts`, mount `/api/report`
- Frontend (later plans): `frontend/app/(app)/laporan/page.tsx`, `frontend/app/(app)/laporan/preview/page.tsx`, `frontend/components/laporan/LaporanDateRangeSelector.tsx`, `frontend/components/laporan/LaporanPreviewContent.tsx`, `frontend/components/laporan/sections/{RingkasanCairan,KepatuhanObat,KondisiCAPD,Anomali}.tsx`, print CSS classes `laporan-preview-content`, `laporan-section-card`, `adherence-metric-number`, `no-print`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Add updated_at column to reminder_schedule + migration + drizzle-kit push</name>
  <files>backend/src/db/schema/reminderSchedule.schema.ts, backend/src/db/migrations/0008_phase4_reminder_updated_at.sql</files>
  <read_first>
    - backend/src/db/schema/reminderSchedule.schema.ts (current columns; `createdAt: timestamp("created_at").notNull().defaultNow()` on line 15 is the pattern to mirror)
    - backend/src/db/migrations/ (existing migration files 0000–0007; 0008 is the next sequential number)
    - .planning/phases/04-caregiver-dashboard-doctor-reports/04-RESEARCH.md ("Migration File Content" and Finding 1)
  </read_first>
  <action>
    Add a new column `updatedAt: timestamp("updated_at").notNull().defaultNow()` to the `reminderSchedule` pgTable in reminderSchedule.schema.ts, placed after the `lastNotificationSentAt` line (the last field). Mirror the exact style of the existing `createdAt` definition. Then create `backend/src/db/migrations/0008_phase4_reminder_updated_at.sql` containing a single statement: ALTER TABLE "reminder_schedule" ADD COLUMN "updated_at" timestamp DEFAULT now() NOT NULL. Then apply it to the live database by running `cd backend && npx drizzle-kit push` (BLOCKING — the phase cannot pass verification without the live column; build/type checks pass without it, creating a false-positive). If `drizzle-kit push` prompts to confirm the new column, accept it. Do NOT modify the repository `update()` function here — that patch belongs to plan 04-02. This task only adds the column to the schema, records the migration, and syncs the live DB.
  </action>
  <verify>
    <automated>cd backend && grep -q 'updated_at' src/db/schema/reminderSchedule.schema.ts && grep -q 'ADD COLUMN "updated_at"' src/db/migrations/0008_phase4_reminder_updated_at.sql && npx drizzle-kit push && echo PUSH_OK</automated>
  </verify>
  <acceptance_criteria>
    - reminderSchedule.schema.ts contains the substring `updatedAt: timestamp("updated_at")`
    - backend/src/db/migrations/0008_phase4_reminder_updated_at.sql exists and contains `ADD COLUMN "updated_at"`
    - `npx drizzle-kit push` completes and reports no pending schema diff for `reminder_schedule.updated_at` on a second run (column is live in the database)
  </acceptance_criteria>
  <done>The `updated_at` column exists in the schema file, in migration 0008, and in the live Postgres `reminder_schedule` table.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn textarea component</name>
  <files>frontend/components/ui/textarea.tsx</files>
  <read_first>
    - frontend/components/ui/separator.tsx (an example of an already-installed shadcn primitive; confirms components.json is initialized and the new-york style)
    - .planning/phases/04-caregiver-dashboard-doctor-reports/04-UI-SPEC.md ("New shadcn component required for Phase 4" + Registry Safety section)
  </read_first>
  <action>
    Run `cd frontend && npx shadcn@latest add textarea` to generate `frontend/components/ui/textarea.tsx` from the official shadcn registry. Do NOT run `npx shadcn init` — shadcn is already initialized (components.json present, new-york style). This is the only new component required for Phase 4; all other primitives (Button, Select, Badge, Separator, Form, Input, Label) already exist. No npm package is added — textarea is a source primitive, not a registry download from an untrusted source (see threat T-04-SC).
  </action>
  <verify>
    <automated>test -f frontend/components/ui/textarea.tsx && grep -q 'Textarea' frontend/components/ui/textarea.tsx && echo TEXTAREA_OK</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/ui/textarea.tsx exists
    - The file exports a `Textarea` component (file contains `Textarea`)
  </acceptance_criteria>
  <done>The shadcn Textarea primitive is available for import by the doctor-note field in plan 04-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create failing test scaffolds for REPORT-01 and CAREGIVER-02 (RED)</name>
  <files>backend/src/test/report.service.test.ts, backend/src/test/reminders.controller.test.ts</files>
  <behavior>
    report.service.test.ts (REPORT-01) — asserts against `_generateReportCore` and `reportQuerySchema` from ../services/report.service.js (not yet implemented):
    - reportQuerySchema accepts a valid {dari, sampai} (YYYY-MM-DD, sampai >= dari, <= 90 days)
    - reportQuerySchema rejects sampai < dari with message "Tanggal akhir harus setelah tanggal mulai"
    - reportQuerySchema rejects a range > 90 days with message "Rentang maksimum adalah 90 hari"
    - reportQuerySchema rejects a malformed date (e.g. "2026/06/01")
    - _generateReportCore (with injected fake repo fns) returns fluidSummary with correct totalIn/totalOut/balance from fake fluid rows
    - _generateReportCore returns medicationAdherence with taken/scheduled/pct computed as (confirmed/total)*100
    - _generateReportCore returns capdFrequency counts grouped by kondisiKeluar
    reminders.controller.test.ts (CAREGIVER-02) — asserts updateReminder fires push fire-and-forget:
    - calling updateReminder invokes the injected sendToAllDevices fake exactly once with payload {title:"Pengingat Diperbarui", body:"Jadwal pengingat telah diperbarui dari perangkat lain."}
    - res.json is called with the updated reminder and the handler does NOT await push delivery (push rejection does not reject the handler)
  </behavior>
  <read_first>
    - backend/src/test/labResult.service.test.ts (injectable-core + in-memory fake pattern, node:test + tsx, top-level await dynamic import)
    - backend/src/test/reminders.service.test.ts (existing reminders test patterns + in-memory fakes)
    - .planning/phases/04-caregiver-dashboard-doctor-reports/04-VALIDATION.md (Per-Task Verification Map — exact behaviors to cover)
    - .planning/phases/04-caregiver-dashboard-doctor-reports/04-RESEARCH.md (Pattern 4 query shapes + Pitfall 4 WIB timezone)
  </read_first>
  <action>
    Create both test files using node:test (`describe`/`it`) with `assert`, importing target modules via top-level `await import(...)` exactly as labResult.service.test.ts does. These are RED scaffolds: report.service.test.ts imports `_generateReportCore` and `reportQuerySchema` from ../services/report.service.js (which does not exist until 04-03), so the file MUST fail to import/run until implemented. reminders.controller.test.ts imports `updateReminder` from ../controllers/reminders.controller.js and exercises it with a fake `sendToAllDevices` (use module mocking or refactor-friendly injection — if the controller imports sendToAllDevices statically, assert behavior after 04-02's patch by spying on the notification.service module via a test double; document the chosen seam in a file comment). Define the response shape the report service must satisfy: `{ fluidSummary: { totalIn, totalOut, balance }, medicationAdherence: { taken, scheduled, pct }, capdFrequency: { jernih, keruh, keruh_gumpalan, berdarah }, anomalies }`. Use the medication adherence formula from D-03 discretion: (confirmed/total)*100; "Tidak ada data" path when total is 0. Do not implement the service or patch the controller here — only the failing assertions.
  </action>
  <verify>
    <automated>cd backend && (node --import tsx --test src/test/report.service.test.ts; node --import tsx --test src/test/reminders.controller.test.ts; true) && test -f src/test/report.service.test.ts && test -f src/test/reminders.controller.test.ts && echo SCAFFOLDS_EXIST</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/test/report.service.test.ts exists and references `_generateReportCore` and `reportQuerySchema`
    - backend/src/test/reminders.controller.test.ts exists and references `sendToAllDevices` and the exact push copy "Jadwal pengingat telah diperbarui dari perangkat lain."
    - Running each test file currently FAILS (RED) because report.service.js does not exist and/or updateReminder does not yet call sendToAllDevices — failure is expected and correct at this stage
  </acceptance_criteria>
  <done>Two failing test files establish the RED baseline that plans 04-02 and 04-03 turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → live Postgres | `drizzle-kit push` mutates the live schema |
| shadcn CLI → frontend source tree | generated component code added to repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `drizzle-kit push` schema diff | accept | Column add is additive (`DEFAULT now() NOT NULL`); no data loss. Migration 0008 records the change for reproducibility. |
| T-04-SC | Tampering | shadcn textarea add | accept | Official shadcn registry component, not an npm install; no `[ASSUMED]`/`[SUS]` package (per 04-RESEARCH Package Legitimacy Audit). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd backend && npx drizzle-kit push` reports no pending diff for `reminder_schedule.updated_at` on a second run.
- `test -f frontend/components/ui/textarea.tsx`
- Both new test files exist and currently fail (RED) — confirmed by running them.
</verification>

<success_criteria>
- `reminder_schedule.updated_at` exists in schema, migration 0008, and live DB.
- shadcn Textarea primitive present.
- REPORT-01 and CAREGIVER-02 test scaffolds exist and fail, establishing the RED baseline for Wave 1.
</success_criteria>

<output>
Create `.planning/phases/04-caregiver-dashboard-doctor-reports/04-01-SUMMARY.md` when done.
</output>
