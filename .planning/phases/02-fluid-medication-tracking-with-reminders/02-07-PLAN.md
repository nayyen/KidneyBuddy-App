---
phase: 02-fluid-medication-tracking-with-reminders
plan: 07
type: execute
wave: 4
depends_on: [02-01, 02-03, 02-05]
files_modified:
  - frontend/lib/validators/reminder.schema.ts
  - frontend/components/pengingat/ReminderList.tsx
  - frontend/components/pengingat/ReminderItem.tsx
  - frontend/components/pengingat/AddReminderSheet.tsx
  - frontend/components/pengingat/MedicationReminderForm.tsx
  - frontend/components/pengingat/CAPDReminderForm.tsx
  - frontend/components/pengingat/HDReminderForm.tsx
  - frontend/components/pengingat/DeleteReminderConfirm.tsx
  - frontend/components/beranda/ObatCard.tsx
  - frontend/components/beranda/PengingatBerikutnyaCard.tsx
  - frontend/components/catatan/MedicationLogList.tsx
  - frontend/components/catatan/MedicationLogItem.tsx
  - frontend/app/(app)/pengingat/page.tsx
  - frontend/app/(app)/catatan/obat/page.tsx
  - frontend/app/(app)/dashboard/page.tsx
autonomous: false
requirements: [REMIND-01, REMIND-03, REMIND-05, REMIND-06, REMIND-07]
user_setup: []

must_haves:
  truths:
    - "User can create a medication reminder via a form (name, dose, type, timing note, active days, time, optional photo) and see it in the reminder list"
    - "CAPD patient sees a CAPD exchange reminder form (concentration); HD patient sees an HD schedule form (days + time); the form offered matches metodeTerapiAktif"
    - "User can confirm a due dose from the Obat card and from the medication log; confirmation hits POST /api/medication-log/confirm"
    - "Beranda shows today's unconfirmed meds (Obat card) and the next upcoming reminder (Pengingat berikutnya card)"
    - "Deleting a reminder requires an explicit confirmation dialog"
  artifacts:
    - path: "frontend/components/pengingat/MedicationReminderForm.tsx"
      provides: "REMIND-01 form with photo upload"
    - path: "frontend/components/pengingat/CAPDReminderForm.tsx"
      provides: "REMIND-05 CAPD exchange form"
    - path: "frontend/components/beranda/ObatCard.tsx"
      provides: "Today's unconfirmed meds with inline confirm"
  key_links:
    - from: "frontend/components/beranda/ObatCard.tsx"
      to: "/api/medication-log/confirm"
      via: "authFetch POST on confirm"
      pattern: "medication-log/confirm"
    - from: "frontend/components/pengingat/MedicationReminderForm.tsx"
      to: "/api/reminders"
      via: "authFetch POST multipart (foto_obat)"
      pattern: "api/reminders"
---

<objective>
Deliver the reminder-management UI on top of the 02-05 backend: the Pengingat page (list + add via type-aware forms for Obat/CAPD/HD), the Beranda Obat and Pengingat-berikutnya cards with inline confirm, the Catatan/Obat medication log, and the delete-confirmation dialog. This completes the user-facing half of the reminder system.

Purpose: REMIND-01/05/06 reminder creation UI and REMIND-03 dose confirmation. The form offered must match the user's active therapy method, and the Beranda cards finalize the D-04 dashboard.
Output: reminder validators + list/item/forms/delete-confirm components, ObatCard + PengingatBerikutnyaCard wired into Beranda, the Catatan/Obat log, and the Pengingat page.
</objective>

## Phase Goal (user story)

**As a** patient, **I want to** create and manage my medication and therapy reminders and confirm doses I have taken, **so that** my schedule is set up correctly and my adherence shows on my dashboard.

## Artifacts this plan produces

- New components: ReminderList, ReminderItem, AddReminderSheet, MedicationReminderForm, CAPDReminderForm, HDReminderForm, DeleteReminderConfirm, ObatCard, PengingatBerikutnyaCard, MedicationLogList, MedicationLogItem
- New: lib/validators/reminder.schema.ts
- New page: `(app)/catatan/obat/page.tsx`; completed `(app)/pengingat/page.tsx`
- Modified: `(app)/dashboard/page.tsx` (insert ObatCard + PengingatBerikutnyaCard into the D-04 stack)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fluid-medication-tracking-with-reminders/02-UI-SPEC.md
@.planning/phases/02-fluid-medication-tracking-with-reminders/02-PATTERNS.md
@.planning/phases/02-fluid-medication-tracking-with-reminders/02-RESEARCH.md
@CLAUDE.md
@frontend/app/onboarding/_components/FirstReminderStep.tsx
@frontend/lib/api.ts
@frontend/lib/hooks/useAuth.ts
@frontend/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reminder forms + AddReminderSheet + validators (Obat/CAPD/HD, type-aware)</name>
  <read_first>
    - .planning/phases/02-fluid-medication-tracking-with-reminders/02-UI-SPEC.md (MedicationReminderForm / CAPDReminderForm / HDReminderForm field tables; AddReminderSheet; error copy; "Tambah Pengingat" CTA)
    - .planning/phases/02-fluid-medication-tracking-with-reminders/02-PATTERNS.md (form analog FirstReminderStep; conditional therapy UI via useAuth user.metodeTerapiAktif)
    - frontend/app/onboarding/_components/FirstReminderStep.tsx (react-hook-form + zod + input/button styling to copy)
    - frontend/lib/api.ts (authFetch; multipart POST for foto_obat)
  </read_first>
  <action>
    Create `lib/validators/reminder.schema.ts` with three zod schemas mirroring the backend (02-05): medication (nama required max 100, dosis required, jenisObat minum|suntik, catatanWaktu optional, hariAktif min 1 day, jamPengingat HH:mm, fotoObat optional file image/jpeg|png max 10MB), capd (nama, konsentrasiCapd select 1.5%/2.5%/4.25%/Icodextrin 7.5%/Lainnya, jamPengingat, hariAktif min 1, catatanWaktu optional), hd (nama, jamPengingat, hariAktif min 1, catatanWaktu optional). Build `MedicationReminderForm.tsx`, `CAPDReminderForm.tsx`, `HDReminderForm.tsx` (react-hook-form + zodResolver) with the exact UI-SPEC field order, a 7-day checkbox row, a time input, and inline error copy; the medication form includes the photo file input and submits multipart to POST /api/reminders. Build `AddReminderSheet.tsx` (shadcn Sheet) whose type selector offers: Obat always; CAPD only when user.metodeTerapiAktif === "CAPD"; HD only when === "HD" — routing to the matching form. On success, refresh the reminder list and toast confirmation.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AddReminderSheet offers CAPD only for CAPD users and HD only for HD users; Obat always
    - MedicationReminderForm posts multipart with foto_obat to /api/reminders
    - empty hariAktif shows "Pilih minimal satu hari aktif"; empty dosis shows "Dosis wajib diisi"
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>Type-aware reminder creation forms submit to the backend with correct validation.</done>
</task>

<task type="auto">
  <name>Task 2: Pengingat page (ReminderList/Item) + DeleteReminderConfirm + Catatan/Obat log</name>
  <read_first>
    - .planning/phases/02-fluid-medication-tracking-with-reminders/02-UI-SPEC.md (ReminderItem spec; Pengingat empty state; Destructive Confirmation dialog; MedicationLogItem spec; Catatan/Obat empty state)
    - frontend/app/(app)/pengingat/page.tsx (placeholder from 02-01 to complete)
    - frontend/app/(app)/catatan/page.tsx (sub-tab shell — add the Obat route content)
    - frontend/lib/api.ts (authFetch)
  </read_first>
  <action>
    Build `ReminderList.tsx` (GET /api/reminders, sorted by jamPengingat) and `ReminderItem.tsx` per UI-SPEC (time badge, name, timing note, active-day chips, type badge Obat/CAPD/HD therapy-identity colors, active toggle via shadcn Switch calling PATCH /api/reminders/:id, done/pending check circle). Build `DeleteReminderConfirm.tsx` (shadcn AlertDialog with the UI-SPEC "Hapus Pengingat?" copy) calling DELETE /api/reminders/:id. Complete `(app)/pengingat/page.tsx`: render ReminderList, the empty state, and a "Tambah Pengingat" button that opens AddReminderSheet. Build `MedicationLogList.tsx` + `MedicationLogItem.tsx` (name, status dikonfirmasi/tertunda/terlewat with the UI-SPEC colors) and render in `(app)/catatan/obat/page.tsx` from GET /api/medication-log/today with the empty-state copy.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - pengingat page lists reminders and opens AddReminderSheet from "Tambah Pengingat"
    - ReminderItem active toggle calls PATCH /api/reminders/:id
    - DeleteReminderConfirm uses an AlertDialog with the "Hapus Pengingat?" copy before DELETE
    - catatan/obat page lists today's medication log with status colors
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>Reminder list, delete-confirmation, and the medication log are complete.</done>
</task>

<task type="auto">
  <name>Task 3: Beranda Obat + Pengingat-berikutnya cards with inline confirm</name>
  <read_first>
    - .planning/phases/02-fluid-medication-tracking-with-reminders/02-UI-SPEC.md (Beranda card order; ObatCard inline confirm; PengingatBerikutnyaCard; "Sudah diminum" checkmark)
    - frontend/app/(app)/dashboard/page.tsx (Beranda stack from 02-04 — insert the two cards in D-04 order)
    - frontend/lib/api.ts (authFetch)
  </read_first>
  <action>
    Build `ObatCard.tsx`: GET /api/medication-log/today, render today's unconfirmed meds with an inline "Sudah diminum" confirm toggle that POSTs to /api/medication-log/confirm and optimistically marks the row dikonfirmasi (teal check). Build `PengingatBerikutnyaCard.tsx`: GET /api/reminders/next, show the next upcoming reminder's time + name (UI-SPEC empty state when none). Insert both into `(app)/dashboard/page.tsx` in the D-04 order between NoReminderBanner and AiPlaceholderCard (Obat card, then Pengingat berikutnya), preserving the responsive grid (2-col md, 3-col lg per UI-SPEC). Ensure these cards coexist with the DeltaCairanCard/CAPDEffluentBanner from 02-04 without disturbing them.
  </action>
  <verify>
    <automated>cd frontend; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/page.tsx renders ObatCard and PengingatBerikutnyaCard in the D-04 order
    - ObatCard confirm POSTs to /api/medication-log/confirm and marks the row confirmed
    - PengingatBerikutnyaCard fetches /api/reminders/next and shows time + name or the empty state
    - npm run build succeeds
  </acceptance_criteria>
  <done>Beranda is complete with live medication and next-reminder cards and inline confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify reminder management UI per therapy type and dose confirmation</name>
  <what-built>The full reminder UI: type-aware create forms, list with toggle/delete, medication log, and Beranda Obat + next-reminder cards with inline confirm.</what-built>
  <how-to-verify>
    1. As a CAPD user, open Pengingat → Tambah Pengingat → confirm the options are Obat and CAPD (no HD). Create a medication reminder with a photo and a CAPD exchange reminder; confirm both appear in the list with correct type badges.
    2. Switch the account to HD (Profil) → open Tambah Pengingat → confirm the options are now Obat and HD (no CAPD), and the medication reminder still exists (REMIND-07 visible from the UI).
    3. On Beranda, confirm the Obat card lists today's unconfirmed meds; tap "Sudah diminum" → confirm it marks confirmed and the medication log (Catatan/Obat) shows dikonfirmasi.
    4. Delete a reminder → confirm the AlertDialog appears and deletion only happens on confirm.
  </how-to-verify>
  <resume-signal>Type "approved" if type-aware forms, confirmation, and delete-guard all work, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/reminders, /api/medication-log | Reminder CRUD + confirm; all auth-scoped (enforced backend in 02-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | confirming/deleting a reminder not owned by the user | mitigate | backend (02-05) validates ownership; UI only sends ids, never trusts client state |
| T-02-07-02 | Tampering | oversized/wrong-type photo from the form | mitigate | client validates image/jpeg|png max 10MB; backend multer re-validates |
| T-02-07-03 | Information Disclosure | showing wrong therapy form to a user | mitigate | AddReminderSheet gates CAPD/HD options on metodeTerapiAktif from /api/auth/me |
</threat_model>

<verification>
- frontend `npm run build` and `npx tsc --noEmit` pass
- Human checkpoint confirms type-aware forms, dose confirmation, and delete-guard
</verification>

<success_criteria>
- REMIND-01/05/06 creation UI per therapy type; REMIND-03 confirm from card and log
- Beranda D-04 card stack complete (delta, obat, pengingat berikutnya, AI placeholder)
</success_criteria>

<output>
Create `.planning/phases/02-fluid-medication-tracking-with-reminders/02-07-SUMMARY.md` when done.
</output>
