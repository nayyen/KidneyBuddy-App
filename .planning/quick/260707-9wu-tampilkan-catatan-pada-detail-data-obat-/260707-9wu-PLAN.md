---
phase: quick-260707-9wu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/catatan/MedicationLogItem.tsx
  - frontend/components/catatan/DialysisLogItem.tsx
autonomous: true
requirements: [QUICK-260707-9wu]
must_haves:
  truths:
    - "Overlay detail data obat menampilkan baris Catatan saat catatanWaktu terisi"
    - "Overlay detail cuci darah menampilkan baris Catatan saat catatanWaktu terisi"
    - "Baris Catatan tidak muncul saat catatanWaktu kosong/null"
  artifacts:
    - path: "frontend/components/catatan/MedicationLogItem.tsx"
      provides: "Baris DetailRow Catatan di overlay detail obat"
      contains: 'label="Catatan"'
    - path: "frontend/components/catatan/DialysisLogItem.tsx"
      provides: "Baris DetailRow Catatan di overlay detail cuci darah"
      contains: 'label="Catatan"'
  key_links:
    - from: "MedicationLogItem detail overlay"
      to: "log.catatanWaktu"
      via: "DetailRow value prop"
      pattern: 'label="Catatan".*log\.catatanWaktu'
    - from: "DialysisLogItem detail overlay"
      to: "log.catatanWaktu"
      via: "DetailRow value prop"
      pattern: 'label="Catatan".*log\.catatanWaktu'
---

<objective>
Tampilkan field Catatan (`catatanWaktu`) di overlay detail untuk item Data Obat dan Cuci Darah di halaman `/catatan`. Saat ini catatan sudah tampil di baris list tetapi tidak muncul saat item diklik untuk membuka overlay detail.

Purpose: Pengguna dapat melihat catatan yang mereka tulis saat membuka detail lengkap sebuah entri obat atau cuci darah, konsisten dengan yang sudah tampil di list dan di overlay pengingat.
Output: Dua file komponen frontend diperbarui, masing-masing menambahkan satu baris `DetailRow` untuk Catatan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/components/catatan/MedicationLogItem.tsx
@frontend/components/catatan/DialysisLogItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tambahkan baris Catatan di kedua overlay detail</name>
  <files>frontend/components/catatan/MedicationLogItem.tsx, frontend/components/catatan/DialysisLogItem.tsx</files>
  <action>
    Di kedua komponen, tambahkan satu baris DetailRow untuk catatan di dalam blok `<div className="divide-y divide-gray-200">`. Gunakan komponen `DetailRow` lokal yang sudah ada di masing-masing file (ia sudah me-render `null` untuk nilai kosong/null, jadi tidak perlu pembungkus kondisional).

    1. MedicationLogItem.tsx: Sisipkan `<DetailRow label="Catatan" value={log.catatanWaktu} />` tepat SETELAH baris `<DetailRow label="Jenis Obat" value={log.jenisObat} />` dan SEBELUM blok kondisional Foto Obat (`{log.fotoObat && ...}`).

    2. DialysisLogItem.tsx: Sisipkan `<DetailRow label="Catatan" value={log.catatanWaktu} />` sebagai baris TERAKHIR di dalam blok `divide-y`, yaitu setelah blok kondisional Konsentrasi CAPD (`{log.jenis === "capd" && ...}`).

    Label WAJIB "Catatan" (bukan "Catatan Waktu") agar konsisten dengan standardisasi wording dari quick task 260707-9p9. Tidak ada perubahan backend — `catatanWaktu` sudah diterima dari API (terbukti sudah dirender di baris list). Frontend only.
  </action>
  <verify>
    <automated>grep -c 'label="Catatan" value={log.catatanWaktu}' frontend/components/catatan/MedicationLogItem.tsx frontend/components/catatan/DialysisLogItem.tsx</automated>
  </verify>
  <done>Kedua file memuat satu baris `<DetailRow label="Catatan" value={log.catatanWaktu} />` pada posisi yang benar; TypeScript/lint tidak error (field `catatanWaktu` sudah ada di interface masing-masing).</done>
</task>

</tasks>

<verification>
- `grep 'label="Catatan"' frontend/components/catatan/MedicationLogItem.tsx frontend/components/catatan/DialysisLogItem.tsx` menemukan satu baris di masing-masing file.
- Manual (setelah rebuild): buka `/catatan`, klik item Data Obat yang punya catatan → overlay menampilkan baris "Catatan". Klik item Cuci Darah yang punya catatan → overlay menampilkan baris "Catatan" di posisi terakhir. Item tanpa catatan tidak menampilkan baris Catatan.
</verification>

<success_criteria>
- Overlay detail obat dan cuci darah menampilkan baris Catatan saat `catatanWaktu` terisi.
- Baris Catatan tidak muncul saat `catatanWaktu` kosong/null.
- Label menggunakan "Catatan" (Bahasa Indonesia awam).
- Tidak ada perubahan backend.
</success_criteria>

<deployment_note>
Frontend di-bake ke dalam Docker image — executor harus rebuild agar perubahan live:
`docker compose build frontend && docker compose up -d frontend`
</deployment_note>

<output>
Create `.planning/quick/260707-9wu-tampilkan-catatan-pada-detail-data-obat-/260707-9wu-SUMMARY.md` when done
</output>
