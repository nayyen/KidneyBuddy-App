---
phase: quick-260705-p9y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/lib/reminderNotificationCopy.ts
  - backend/src/jobs/reminderFollowUp.job.ts
  - backend/src/jobs/reminderDispatch.job.ts
  - backend/src/lib/forbiddenPhrases.ts
  - backend/src/services/anomalyExplanation.service.ts
  - backend/src/test/reminderNotificationCopy.test.ts
  - backend/src/test/forbiddenPhrases.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Missed-reminder (follow-up) push shows an emoji + reminder type (obat vs cuci darah)"
    - "Missed medication follow-up body says 'belum mengonfirmasi minum obat ini', not 'dosis ini'"
    - "AI anomaly alert text never claims the app/system will contact a doctor/nurse/hospital on the user's behalf"
    - "Anomaly alerts trip a defensive filter on false-contact claims and fall back to a safe static template"
  artifacts:
    - path: "backend/src/lib/reminderNotificationCopy.ts"
      provides: "Shared jenis-aware emoji/label helpers for reminder push copy"
    - path: "backend/src/lib/forbiddenPhrases.ts"
      provides: "False-contact-claim phrase list + containsFalseContactClaim()"
  key_links:
    - from: "backend/src/jobs/reminderFollowUp.job.ts"
      to: "backend/src/lib/reminderNotificationCopy.ts"
      via: "jenisEmoji/jenisLabel import"
      pattern: "reminderNotificationCopy"
    - from: "backend/src/services/anomalyExplanation.service.ts"
      to: "backend/src/lib/forbiddenPhrases.ts"
      via: "containsFalseContactClaim in getValidatedExplanation"
      pattern: "containsFalseContactClaim"
---

<objective>
Two small, patient-safety-adjacent copy/logic fixes:

1. Make the "pengingat terlewat" (missed-reminder follow-up) push notification jenis-aware — emoji + reminder type (💊 Obat vs 💧/🩸 Cuci Darah) in the title — and fix the wrong body text "belum mengonfirmasi dosis ini" to "belum mengonfirmasi minum obat ini".
2. Guarantee the AI anomaly alert never tells a patient the app/system will directly contact/notify/coordinate with a doctor, nurse, or hospital (false — no such integration exists). Enforce via both a system-prompt constraint AND a defensive post-generation keyword filter with safe-template fallback.

Purpose: Notification clarity for a 50+ user base, and eliminating a factually false patient-facing claim in a health app.
Output: Shared reminder-copy helper, corrected follow-up job, extended forbidden-phrase guard, updated anomaly system prompt + validation gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Existing jenis-aware copy pattern to mirror/extract (JENIS_EMOJI/JENIS_LABEL, lines 34-56)
@backend/src/jobs/reminderDispatch.job.ts

# Follow-up job that emits the wrong "dosis ini" text (lines 34-39)
@backend/src/jobs/reminderFollowUp.job.ts

# reminder.jenis is 'obat' | 'capd' | 'hd' (line 9)
@backend/src/db/schema/reminderSchedule.schema.ts

# Existing forbidden-phrase guard + static fallback templates to extend
@backend/src/lib/forbiddenPhrases.ts

# Anomaly LLM system prompt + getValidatedExplanation safety gate to update
@backend/src/services/anomalyExplanation.service.ts

# Existing test for the forbidden-phrase lib (mirror its style)
@backend/src/test/forbiddenPhrases.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jenis-aware missed-reminder follow-up notification + correct body text</name>
  <files>backend/src/lib/reminderNotificationCopy.ts, backend/src/jobs/reminderFollowUp.job.ts, backend/src/jobs/reminderDispatch.job.ts, backend/src/test/reminderNotificationCopy.test.ts</files>
  <action>
    Extract the existing jenis-aware copy helpers into a shared module so both reminder jobs share one source of truth, then apply them to the missed-reminder follow-up push.

    (a) Create `backend/src/lib/reminderNotificationCopy.ts` exporting the same `JENIS_EMOJI` ({ obat: "💊", capd: "💧", hd: "🩸" }), `JENIS_LABEL` ({ obat: "Obat", capd: "Cuci Darah", hd: "Cuci Darah" }), and `jenisEmoji(jenis)` / `jenisLabel(jenis)` functions currently inlined in reminderDispatch.job.ts (lines 34-56), including the same `?? "🔔"` and `?? "Pengingat"` fallbacks. Also export a helper `jenisFollowUpNoun(jenis)` returning "minum obat ini" for jenis "obat" and "sesi cuci darah ini" for "capd"/"hd" (fallback: "pengingat ini").

    (b) Refactor `reminderDispatch.job.ts` to import `jenisEmoji`/`jenisLabel` from the new module and delete its now-duplicated local `JENIS_EMOJI`/`JENIS_LABEL`/`jenisEmoji`/`jenisLabel` definitions. Behavior must stay byte-for-byte identical (same emojis, labels, fallbacks) so reminderDispatch.test.ts still passes.

    (c) In `reminderFollowUp.job.ts`, update the `sendToAllDevices` payload (lines 35-36) to use the shared helpers off `reminder.jenis`:
      - title: `${jenisEmoji(reminder.jenis)} Pengingat ${jenisLabel(reminder.jenis)} Terlewat: ${reminder.nama}`
      - body: `Kamu belum mengonfirmasi ${jenisFollowUpNoun(reminder.jenis)} — ketuk untuk konfirmasi sekarang.`
    This fixes the wrong "dosis ini" wording (obat → "minum obat ini") and adds the emoji + type. `reminder` already comes from `findById(log.reminderId)` and carries `jenis`.

    Do NOT change the follow-up query logic, the follow_up_sent guard, or markFollowUpSent behavior.
  </action>
  <verify>
    <automated>cd backend && node --import tsx --test src/test/reminderNotificationCopy.test.ts src/test/reminderDispatch.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Shared reminderNotificationCopy module exists and is imported by both jobs; follow-up push title contains a jenis emoji + label + "Terlewat", body says "belum mengonfirmasi minum obat ini" for obat reminders; reminderDispatch behavior unchanged and its test still passes; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Block AI anomaly alerts from claiming the app contacts a doctor/nurse</name>
  <files>backend/src/lib/forbiddenPhrases.ts, backend/src/services/anomalyExplanation.service.ts, backend/src/test/forbiddenPhrases.test.ts</files>
  <action>
    Add both a prompt-level instruction and a defensive post-generation filter so the anomaly explanation never claims the system will contact/notify/coordinate with a doctor, nurse, or hospital on the patient's behalf.

    (a) In `forbiddenPhrases.ts`, add a new exported `FALSE_CONTACT_PHRASES: readonly string[]` covering system-initiated contact claims (case-insensitive substrings), including at minimum: "kami akan menghubungi", "kami akan segera menghubungi", "kita akan menghubungi", "kita akan segera menghubungi", "sistem akan menghubungi", "aplikasi akan menghubungi", "kami hubungi", "menghubungi dokter anda untuk anda", "menghubungi tim perawatan", "menghubungi tim medis", "akan diperiksa oleh dokter", "akan segera diperiksa", "kami akan memberi tahu dokter", "kami akan menginformasikan dokter", "akan meneruskan ke dokter". Add exported `containsFalseContactClaim(text: string): boolean` mirroring `containsForbiddenPhrase` (lowercase substring match). Keep the module dependency-free/pure.

    (b) In `anomalyExplanation.service.ts`, append an explicit constraint to `SYSTEM_PROMPT`: the assistant must NEVER state or imply that the app, system, or "kami/kita" will contact, notify, call, coordinate with, or forward information to any doctor, nurse, hospital, or care team on the user's behalf — the app has no such capability. Next steps must always be framed as actions the USER takes themselves (e.g. "segera hubungi dokter atau faskes Anda"), never system-initiated.

    (c) In `getValidatedExplanation`, add a defensive check AFTER the successful Groq call that runs on ALL severities (not gated by severity "tinggi", unlike the false-reassurance check): if `containsFalseContactClaim(llmText)` is true, log a server-side pino warning and return `{ text: STATIC_FALLBACK_TEMPLATES[rule.tipeAnomali] ?? GENERIC_FALLBACK_TEXT, isFallback: true }`. Place it so it also catches text that simultaneously trips the existing forbidden-phrase gate. Keep the swap invisible to the UI (log only), consistent with the existing D-20 comment.

    (d) In `forbiddenPhrases.test.ts`, add cases asserting `containsFalseContactClaim` returns true for representative false-contact strings ("Langkah berikutnya adalah kami akan segera menghubungi dokter Anda", "kita akan segera menghubungi tim perawatan", "Anda akan segera diperiksa lebih lanjut oleh dokter") and false for a safe user-initiated string ("Segera hubungi dokter atau faskes Anda untuk pemeriksaan lebih lanjut").
  </action>
  <verify>
    <automated>cd backend && node --import tsx --test src/test/forbiddenPhrases.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>SYSTEM_PROMPT forbids system-initiated doctor/nurse contact claims; containsFalseContactClaim exists and is enforced in getValidatedExplanation across all severities with static-template fallback + server-side log; new tests pass; tsc clean.</done>
</task>

</tasks>

<verification>
- `cd backend && node --import tsx --test src/test/*.test.ts` — full backend suite green (no regressions in reminderDispatch).
- `cd backend && npx tsc --noEmit` — typecheck clean.
- Manual read-through: reminderFollowUp title/body use shared helpers; anomaly gate calls containsFalseContactClaim before returning LLM text.
</verification>

<success_criteria>
- Missed-reminder follow-up push distinguishes obat (💊) from dialysis (💧/🩸) and states "belum mengonfirmasi minum obat ini" for medication.
- No AI anomaly alert can surface text claiming the app/system contacts a doctor/nurse/hospital; such text is filtered to a safe static template.
- reminderDispatch behavior and tests unchanged.
</success_criteria>

<output>
Create `.planning/quick/260705-p9y-perbaiki-teks-notifikasi-pengingat-terle/260705-p9y-SUMMARY.md` when done.
</output>
