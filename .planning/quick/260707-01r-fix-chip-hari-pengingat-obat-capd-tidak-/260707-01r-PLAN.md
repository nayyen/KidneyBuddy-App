---
phase: quick-260707-01r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/pengingat/ReminderItem.tsx
  - backend/src/seed/generate-demo-data.ts
  - backend/src/seed/data/fluid-log.sari.json
  - backend/src/seed/data/fluid-log.budi.json
autonomous: true
requirements: [QUICK-260707-01r]

must_haves:
  truths:
    - "Chip hari terpilih untuk jenis obat & CAPD jelas terbedakan dari chip tidak-terpilih (solid vs pucat)"
    - "Badge TYPE_LABELS di kartu tetap subtle (tidak ikut menjadi solid)"
    - "Budi (Transplantasi) punya minimal 1 entri fluid keluar SETIAP hari (tidak ada tanggal masuk-tanpa-keluar)"
    - "Sari (HD) punya entri urin kecil mayoritas hari dengan tren menurun sepanjang 6 bulan"
    - "Generator tetap deterministik (seeded PRNG, tanpa Math.random)"
  artifacts:
    - path: "frontend/components/pengingat/ReminderItem.tsx"
      provides: "Style chip hari terpisah dari badge, kontras solid untuk state terpilih"
    - path: "backend/src/seed/generate-demo-data.ts"
      provides: "Logika fluid keluar realistis untuk Budi & Sari"
    - path: "backend/src/seed/data/fluid-log.budi.json"
      provides: "JSON fluid keluar Budi regenerated (committed)"
    - path: "backend/src/seed/data/fluid-log.sari.json"
      provides: "JSON fluid keluar Sari regenerated (committed)"
  key_links:
    - from: "ReminderItem.tsx day chips (baris ~216-217)"
      to: "CHIP_SELECTED_STYLE (baru)"
      via: "lookup by reminder.jenis untuk state terpilih"
      pattern: "CHIP_SELECTED_STYLE"
    - from: "generate-demo-data.ts budiFluid/sariFluid"
      to: "seed/data/fluid-log.*.json"
      via: "writeJSON setelah npm run seed:demo:generate"
      pattern: "writeJSON.*fluid-log"
---

<objective>
Perbaiki 2 bug terkonfirmasi:
1. Chip hari terpilih di kartu /pengingat tidak terlihat untuk jenis obat & CAPD — akar masalah: chip-terpilih memakai bg `#f0faf9` (teal sangat muda) yang nyaris identik dengan bg chip-tidak-terpilih `#f3f3f5`. Mata tidak bisa membedakan → user mengira tidak ada hari terpilih. HD (`#fdf3e3`) sudah kontras. Data `hariAktif` dari API sudah benar (diverifikasi end-to-end).
2. Generator seed cairan keluar tidak realistis: Budi (Transplantasi, urin normal) hanya 82 entri keluar dalam 180 hari (banyak hari masuk-tanpa-keluar); Sari (HD, oligurik) hanya 66 entri keluar tanpa tren. Lukman (drainase CAPD, keluar=684) SUDAH wajar → JANGAN diubah.

Purpose: Reliabilitas visual pengingat (core value) dan realisme dataset demo untuk presentasi caregiver dashboard.
Output: ReminderItem.tsx dengan style chip kontras + frontend image rebuild; generator fluid diperbaiki + JSON regenerated + reload in-container + verifikasi SQL/API.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@frontend/components/pengingat/ReminderItem.tsx
@backend/src/seed/generate-demo-data.ts
@.planning/quick/260706-q0g-rebuild-demo-seed-6-bulan-dengan-arsitek/260706-q0g-SUMMARY.md

# Palet warna (dari DESIGN_SYSTEM_KidneyBuddy_v3.md, sudah diekstrak):
# - Primary teal: #2a9d8f, teal bg muda: #f0faf9
# - Amber accent: #ef9f27, amber bg muda: #fdf3e3, amber text gelap: #7a4c0a
# - Chip tidak-terpilih existing: bg #f3f3f5, text #a8b8b6
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kontras chip hari terpilih (obat/CAPD/HD) + rebuild frontend image</name>
  <files>frontend/components/pengingat/ReminderItem.tsx</files>
  <action>
Pisahkan style chip hari dari style badge. Saat ini baris 216-217 chip-terpilih memakai `bg`/`text` dari TYPE_BADGE_STYLE — style yang SAMA dipakai time-badge (baris 150-155) dan badge TYPE_LABELS (baris 224-232). Jangan ubah TYPE_BADGE_STYLE (biarkan time-badge + label-badge tetap subtle sesuai UI-SPEC).

Tambahkan konstanta baru `CHIP_SELECTED_STYLE: Record<string, { bg: string; text: string }>` di dekat TYPE_BADGE_STYLE (baris ~69):
  - obat: { bg: "#2a9d8f", text: "#ffffff" }
  - capd: { bg: "#2a9d8f", text: "#ffffff" }
  - hd:   { bg: "#ef9f27", text: "#7a4c0a" }
Rasional: state "terpilih = solid" untuk ketiga jenis — teal solid untuk obat/capd (teks putih), amber solid untuk HD (teks amber-gelap #7a4c0a agar kontras cukup pada amber #ef9f27). Ini menyeragamkan pola "terpilih = solid, tidak = pucat" untuk semua jenis (HD sebelumnya sudah lumayan kontras, sekarang jadi satu pola).

Di dalam komponen, derive style chip terpilih (di dekat baris 138 `const { bg, text } = ...`):
  `const chipSel = CHIP_SELECTED_STYLE[reminder.jenis] ?? CHIP_SELECTED_STYLE.obat;`

Ubah HANYA day-chip style (baris 216-217) agar state terpilih pakai `chipSel.bg`/`chipSel.text`, state tidak-terpilih tetap `#f3f3f5`/`#a8b8b6`:
  - backgroundColor: reminder.hariAktif.includes(day) ? chipSel.bg : "#f3f3f5"
  - color: reminder.hariAktif.includes(day) ? chipSel.text : "#a8b8b6"

JANGAN sentuh: time-badge (baris 145-159), badge TYPE_LABELS (baris 224-233), Switch, ikon aksi. Perbarui komentar spec di header file (baris 11 "Active day chips: active=...") agar mencerminkan warna baru.

Deployment: frontend adalah production build yang di-bake ke image (TIDAK ada source volume mount) — perubahan tidak muncul tanpa rebuild.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "next-env.d.ts" | grep -c "ReminderItem" | grep -qx 0 && grep -q "CHIP_SELECTED_STYLE" components/pengingat/ReminderItem.tsx && echo TSC_OK</automated>
    <human-check>Setelah `docker compose build frontend && docker compose up -d frontend`, buka /pengingat, konfirmasi chip hari terpilih untuk kartu Obat & CAPD sekarang solid teal (teks putih) dan jelas berbeda dari hari tidak-terpilih; kartu HD solid amber.</human-check>
  </verify>
  <done>CHIP_SELECTED_STYLE ada; day-chip terpilih pakai warna solid per jenis; badge & time-badge tidak berubah; `npx tsc --noEmit` bersih untuk ReminderItem; image frontend di-rebuild via `docker compose build frontend && docker compose up -d frontend`.</done>
</task>

<task type="auto">
  <name>Task 2: Perbaiki generator fluid keluar Budi & Sari, regenerate JSON, reload, verifikasi SQL/API</name>
  <files>backend/src/seed/generate-demo-data.ts, backend/src/seed/data/fluid-log.budi.json, backend/src/seed/data/fluid-log.sari.json</files>
  <action>
Edit blok fluid di `generate-demo-data.ts`. JANGAN ubah `lukmanFluid` (baris 484-549 — drainase CAPD sudah wajar). Gunakan enum `sumber: "urine"` untuk keluar (sudah dipakai existing, keluar-only, valid). Jaga determinisme: semua stokastik lewat `rng` (mulberry32 SeededRng existing), TANPA Math.random/Date.now. Jangan ubah blok intake (masuk) → korelasi intake-berlebih→perasaan "Berat" di daily_activities tetap utuh.

**Budi (Transplantasi, urin normal)** — ganti blok `budiFluid` (baris 591-628). Untuk SETIAP tanggal di DATE_LIST, WAJIB tambahkan 3-4 entri keluar urine yang totalnya 1000-2000 mL/hari (bukan lagi `rng.chance(0.5)` single-entry):
  - `const voidCount = rng.randInt(3, 4);`
  - Tentukan total harian `const dayTotal = rng.randInt(1000, 2000);` lalu bagi ke voidCount entri (mis. porsi acak via rng yang dinormalisasi ke dayTotal, dibulatkan; pastikan tiap entri > 0 dan jumlah entri = dayTotal). Cara sederhana deterministik: buat array bobot `Array.from({length: voidCount}, () => rng.rand())`, jumlahkan, tiap volume = round(dayTotal * bobot_i/sumBobot), lalu koreksi entri terakhir agar sum tepat = dayTotal.
  - Waktu keluar tersebar sepanjang hari (mis. dari pool `["06:30","09:00","12:00","15:30","19:00","22:00"]` diambil `voidCount` slot awal, atau slot acak-deterministik). Tetap `tipe:"keluar"`, `sumber:"urine"`, `konsentrasiCapd:null`, `kondisiKeluar:null`, `catatan:null`, `satuan:"ml"`, `isLateEntry:false`.
  - HAPUS blok `if (rng.chance(0.5))` single-entry lama.

**Sari (HD, oligurik)** — ganti blok keluar `sariFluid` (baris 573-587). Ubah loop ke bentuk terindeks agar bisa tren menurun: `DATE_LIST.forEach((date, dayIdx) => { ... })` (blok intake di dalamnya tetap). Untuk keluar:
  - Mayoritas hari punya 1 entri urin kecil: `if (rng.chance(0.9)) { ... }` (sesekali/jarang tidak ada keluar — ~10%).
  - Volume kecil dengan tren menurun perlahan sepanjang 180 hari. Hitung faktor tren linear: `const t = dayIdx / (DATE_LIST.length - 1);` (0→1). Volume dasar turun dari ~200 ke ~50: `const base = Math.round(200 - 150 * t);` lalu jitter deterministik: `const vol = Math.max(30, base + rng.randInt(-30, 30));`. Clamp ke rentang ~50-200 di awal, mengecil ke ~30-80 di akhir.
  - Tetap `tipe:"keluar"`, `sumber:"urine"`, `waktu:"08:00"` (atau slot pagi), field lain null seperti existing.

Setelah edit generator:
1. `cd backend && npm run seed:demo:generate` → regenerasi SEMUA JSON (commit `fluid-log.budi.json` + `fluid-log.sari.json`; file JSON lain seharusnya byte-identical karena determinisme — hanya fluid Budi/Sari berubah). Verifikasi tsc backend tetap bersih kecuali 4 error pre-existing yang terdokumentasi (dialysisLog/medicationLog controller).
2. Reload penuh in-container (package.json di-bake ke image → invoke via path file):
   `docker compose exec backend node --import tsx src/seed/seed-demo.ts`
3. Verifikasi via `docker compose exec db psql -U kidneybuddy -d kidneybuddy`:
   - Count keluar Budi ≈ 540-720: `SELECT count(*) FROM fluid_log WHERE user_id=(SELECT id FROM users WHERE email='budi@demo.kidneybuddy.id') AND tipe='keluar';`
   - Count keluar Sari ≈ 150-180 (sama pola, email sari@).
   - Assert TIDAK ada hari Budi masuk-tanpa-keluar (harus = 0):
     `SELECT count(*) FROM (SELECT tanggal FROM fluid_log WHERE user_id=(SELECT id FROM users WHERE email='budi@demo.kidneybuddy.id') GROUP BY tanggal HAVING sum((tipe='keluar')::int)=0) x;`
   - Sanity total harian Budi dalam 1000-2000: spot-check `SELECT tanggal, sum(volume::int) FROM fluid_log WHERE user_id=(budi) AND tipe='keluar' GROUP BY tanggal ORDER BY tanggal LIMIT 5;`
4. Spot-check API: `POST /api/auth/login` budi@demo.kidneybuddy.id / Demo1234! → ambil token → `GET /api/fluid/daily-balance` salah satu tanggal → konfirmasi masuk + keluar keduanya tampil dan tanpa error decrypt.
  </action>
  <verify>
    <automated>cd backend && npm run seed:demo:generate >/dev/null 2>&1 && node -e "const b=require('./src/seed/data/fluid-log.budi.json'); const byDay={}; for(const r of b){byDay[r.tanggal]=byDay[r.tanggal]||{in:0,out:0}; byDay[r.tanggal][r.tipe==='keluar'?'out':'in']++;} const daysNoOut=Object.values(byDay).filter(d=>d.out===0).length; const outTotal=b.filter(r=>r.tipe==='keluar').length; if(daysNoOut!==0){console.error('FAIL budi days-without-keluar='+daysNoOut);process.exit(1);} if(outTotal<540||outTotal>720){console.error('FAIL budi keluar count='+outTotal);process.exit(1);} const s=require('./src/seed/data/fluid-log.sari.json').filter(r=>r.tipe==='keluar').length; if(s<150||s>180){console.error('FAIL sari keluar count='+s);process.exit(1);} console.log('BUDI_OUT='+outTotal+' SARI_OUT='+s+' OK');"</automated>
    <human-check>Setelah reload in-container + psql, konfirmasi count keluar Budi ~540-720 & Sari ~150-180, assert hari-tanpa-keluar Budi = 0, dan API daily-balance Budi menampilkan masuk+keluar.</human-check>
  </verify>
  <done>Generator budiFluid menjamin 3-4 entri keluar/hari total 1000-2000mL setiap hari (0 hari tanpa keluar); sariFluid punya urin kecil mayoritas hari dengan tren menurun; JSON regenerated & committed; reload in-container sukses; psql count Budi 540-720 & Sari 150-180 dengan 0 hari-tanpa-keluar; API daily-balance Budi menampilkan masuk+keluar. Determinisme terjaga (rng saja).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Tidak ada trust boundary baru. Task 1 = perubahan warna CSS inline (tanpa input/auth). Task 2 = data seed demo yang di-generate deterministik lalu dienkripsi oleh loader existing (encrypt() diterapkan saat insert, tidak berubah). Tidak ada package baru, tidak ada endpoint baru.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01r-01 | Information Disclosure | fluid_log seed data (kolom catatan) | accept | Data demo sintetik non-PII; loader existing tetap encrypt() kolom sensitif saat insert (tidak diubah plan ini). |
</threat_model>

<verification>
- Task 1: `npx tsc --noEmit` bersih untuk ReminderItem; CHIP_SELECTED_STYLE ada; badge/time-badge tak berubah; image frontend di-rebuild.
- Task 2: automated node-assert lolos (Budi 0 hari-tanpa-keluar, count 540-720; Sari 150-180); reload in-container sukses; psql & API spot-check lolos.
- Lukman fluid tidak berubah (drainase CAPD tetap wajar).
- Determinisme: file JSON selain fluid Budi/Sari byte-identical setelah regenerate.
</verification>

<success_criteria>
- Chip hari terpilih obat/CAPD solid teal (teks putih), HD solid amber — jelas berbeda dari chip pucat; badge kartu tetap subtle.
- Frontend image di-rebuild & up.
- Budi: minimal 1 keluar setiap hari (3-4 entri, total 1000-2000mL/hari), count keluar ~540-720.
- Sari: urin kecil mayoritas hari, tren menurun, count keluar ~150-180.
- JSON fluid regenerated + committed; reload in-container + verifikasi SQL/API lolos.
- Commit atomik per task (Task 2 menyertakan JSON regenerate).
</success_criteria>

<output>
Create `.planning/quick/260707-01r-fix-chip-hari-pengingat-obat-capd-tidak-/260707-01r-SUMMARY.md` when done
</output>
