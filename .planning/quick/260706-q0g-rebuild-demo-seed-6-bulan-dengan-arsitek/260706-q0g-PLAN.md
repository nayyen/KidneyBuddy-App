---
phase: quick-260706-q0g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/seed/seed-demo.ts
  - backend/src/seed/seed-education.ts
  - backend/src/seed/generate-demo-data.ts
  - backend/src/seed/content/education.ts
  - backend/src/seed/content/community.ts
  - backend/src/seed/content/aiText.ts
  - backend/src/seed/lib/rng.ts
  - backend/src/seed/data/*.json
autonomous: true
requirements: [DEMO-SEED]

must_haves:
  truths:
    - "npm run seed:demo:generate regenerates every JSON file deterministically (same seed -> identical bytes)"
    - "npm run seed:demo TRUNCATEs all user-data tables then loads 6 months of data for 3 personas with zero old-seed rows remaining"
    - "Login POST /api/auth/login with lukman@demo.kidneybuddy.id / Demo1234! returns 200 + token and lands on beranda (onboarding complete)"
    - "Encrypted columns decrypt without 'Invalid ciphertext' when fetched via API (fluid, activities, anomaly, AI, lab notes)"
    - "npm run seed:demo -- fluid loads only the fluid section"
  artifacts:
    - path: "backend/src/seed/generate-demo-data.ts"
      provides: "Deterministic generator: seeded PRNG writes all data/*.json"
    - path: "backend/src/seed/seed-demo.ts"
      provides: "Loader: TRUNCATE CASCADE + FK-ordered batch insert + encrypt-on-load"
    - path: "backend/src/seed/content/education.ts"
      provides: "28-32 hand-written article constants"
    - path: "backend/src/seed/content/community.ts"
      provides: "Community post/reply body constants"
    - path: "backend/src/seed/data"
      provides: "Committed JSON seed data, one file per table/shard"
  key_links:
    - from: "seed-demo.ts"
      to: "lib/encryption.ts encrypt()"
      via: "encrypt sensitive columns before INSERT"
      pattern: "encrypt\\("
    - from: "seed-demo.ts"
      to: "utils/passwordHash.ts hashPassword()"
      via: "hash Demo1234! so login works"
      pattern: "hashPassword\\("
    - from: "generate-demo-data.ts"
      to: "seed/data/*.json"
      via: "writeFileSync per section"
      pattern: "writeFileSync"
---

<objective>
Rebuild the demo seed from scratch. Replace the broken 467-line `seed-demo.ts` with a two-part architecture: a deterministic generator that writes committed JSON (one file per table, big logs sharded per patient), and a loader that TRUNCATEs all user-data tables, then batch-inserts in FK order while applying AES-256-GCM encryption and Argon2 password hashing exactly as the running app does.

The data models 180 days (2026-01-08 to 2026-07-06) of realistic CKD clinical activity for 3 personas (Lukman = CAPD, Sari = HD, Budi = Transplantasi), including Ramadan/Lebaran seasonal patterns and a handful of engineered anomaly episodes.

Purpose: A trustworthy, inspectable, reproducible demo dataset where a failed load points to exactly one JSON file, and where every login lands directly on beranda with fully-decryptable data across all 27 pages.

Output: `generate-demo-data.ts` + content constants + `data/*.json` (committed) + new `seed-demo.ts` loader + two npm scripts, verified end-to-end against the running Postgres.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Contract files the executor MUST read and copy verbatim (Task 1 recon):
@backend/src/lib/encryption.ts
@backend/src/utils/passwordHash.ts
@backend/src/lib/aiDisclaimer.ts
@backend/src/lib/therapyReminderScope.ts
@backend/src/utils/wib.ts
@backend/src/lib/db.ts
@backend/src/seed/seed-education.ts

# All 22 schemas live here -- read the ones each section touches:
@backend/src/db/schema/index.ts
</context>

<contracts_verified>
The planner already extracted these binding facts from source. The executor MUST re-open the cited files in Task 1 to confirm nothing drifted, but these are the authoritative conventions to encode:

**DB client** -- `backend/src/lib/db.ts` exports `db` (drizzle) and `pool` (pg.Pool). `import "dotenv/config"` first. Close `pool.end()` at exit.

**Encryption** -- `backend/src/lib/encryption.ts` exports `encrypt(plaintext: string): string` -> `"iv:authTag:cipher"` (all hex). Requires `ENCRYPTION_KEY` = 64 hex chars in env. JSON stores PLAINTEXT; loader calls `encrypt()` at insert time for: `fluid_log.catatan`, `daily_activities.catatanPerasaan`, `anomaly_alerts.deskripsi`, `ai_daily_summaries.ringkasanText`, `ai_weekly_insights.wawasanText`, `ai_lab_analyses.analisisText`, `ai_lifestyle_suggestions.saranText`, `lab_results.catatan`. Community + education are NOT encrypted (public content).

**Password** -- `backend/src/utils/passwordHash.ts` exports `async hashPassword(plain): Promise<string>` (Argon2id). Hash `"Demo1234!"` for all 3 users so `verifyPassword` at login passes. Do NOT paste a pre-computed hash -- call `hashPassword` at load time.

**AI disclaimer** -- `backend/src/lib/aiDisclaimer.ts` exports `AI_DISCLAIMER` and `appendDisclaimer(text)`. All AI narration text stored WITH the disclaimer already appended (existing convention). Recommend the generator writes disclaimer-inclusive plaintext (call `appendDisclaimer`) so JSON reflects exactly what is stored; loader then just `encrypt()`s it.

**Timestamp convention (WIB)** -- `backend/src/utils/wib.ts`. A timestamp column value for "HH:mm WIB on date Y-M-D" is built as `new Date(Date.UTC(y, m-1, d) - 7*3600_000 + h*3600_000 + min*60_000)` (exact math in `wibDateFromHHmm`). Reuse this so times render correctly for `users.timezone` default `Asia/Jakarta`. ISO week key format `"2026-Wxx"` (see `wibIsoWeekKey`) -- use for `ai_weekly_insights.pekan`.

**Day names** -- `reminder_schedule.hariAktif` is a jsonb array of LOWERCASE Indonesian day names: `["senin","selasa","rabu","kamis","jumat","sabtu","minggu"]`. Daily meds = all 7; HD and EPO = `["senin","rabu","jumat"]`. Confirm against `frontend/lib/validators/reminder.schema.ts` + `reminderSchedule.repository.ts findDueReminders`.

**Fluid enums** (`services/fluid.service.ts`): `tipe` in {masuk,keluar}; `sumber` in {urine,capd,lainnya,makanan,minuman} -- makanan/minuman are masuk-only, urine is keluar-only, capd used for CAPD exchange masuk; `satuan` in {ml,kg}; `konsentrasiCapd` in {1.5%,2.5%,4.25%,icodextrin_7.5%,lainnya}; `kondisiKeluar` in {jernih,keruh,keruh_gumpalan,berdarah}. `fluid_log.tanggal` = text YYYY-MM-DD, `waktu` = text HH:mm.

**Anomaly enums** (`services/anomalyRule.service.ts`): `tipeAnomali` in {penurunan_volume_keluar(normal), kondisi_cairan_abnormal(tinggi), jadwal_terlewat(tinggi), pola_asupan_menyimpang(normal)}. `severity` in {normal,tinggi}; `confidenceScore` int 0-100; `status` in {aktif,dibaca,ditindaklanjuti}; `tipePasien` text (capd/hd/transplantasi); `ruleData` jsonb; `isFallback` bool (false for seed). Effluent-keruh anomaly -> kondisi_cairan_abnormal tinggi with ruleData {kondisiKeluar}.

**Therapy enum** (`services/onboarding.service.ts`): `users.metodeTerapiAktif` stored capitalized in {CAPD,HD,Transplantasi}. `education_content.metodeTerapi` + `community_posts.metodeTerapi` in {CAPD,HD,Transplantasi,Umum}. `education_content.tipeKonten` in {artikel,panduan_senam,gaya_hidup}. `community_posts.kategori` in {pertanyaan,berbagi_pengalaman,informasi}.

**Log status enums**: `medication_log.status` / `dialysis_log.status` in {tertunda,dikonfirmasi,terlewat}; `dialysis_log.jenis` in {capd,hd}; `daily_activities.status` in {berlangsung,selesai}; `perasaan` in {null,nyaman,biasa,lelah,berat}. `lab_results.sumber` in {manual,upload} -- use manual.

**FK insert order** (parents before children): users -> onboarding_progress, therapy_history, reminder_schedule -> fluid_log, medication_log(needs reminderId), dialysis_log(needs reminderId), daily_activities, lab_results -> anomaly_alerts, ai_daily_summaries, ai_weekly_insights, ai_lifestyle_suggestions, ai_lab_analyses(needs labResultId) -> community_posts -> community_replies -> community_reply_helpful. education_content is independent.

**Cross-file id stability**: generator assigns explicit deterministic UUIDs to entities referenced across files -- users (3 fixed constant UUIDs), reminder_schedule, lab_results, community_posts, community_replies. JSON stores these ids; loader inserts them explicitly (drizzle allows overriding `id`). Child rows carry the parent id string. Log rows may omit `id` (DB default) but MUST carry the correct FK.

**DB access for verify**: docker-compose service `db` (container `kidneybuddy-db`), backend service `backend` (container `kidneybuddy-backend`). Postgres user/db = `kidneybuddy`. Root `.env` holds `ENCRYPTION_KEY` -- the loader MUST run with the SAME key the backend uses (running inside `kidneybuddy-backend` guarantees this).
</contracts_verified>

<tasks>

<task type="auto">
  <name>Task 1: Contract recon + deterministic generator + generate and commit JSON (delete old seed)</name>
  <files>
    backend/src/seed/lib/rng.ts (new),
    backend/src/seed/content/education.ts (new),
    backend/src/seed/content/community.ts (new),
    backend/src/seed/content/aiText.ts (new),
    backend/src/seed/generate-demo-data.ts (new),
    backend/src/seed/data/*.json (new, generated + committed),
    backend/src/seed/seed-demo.ts (delete old 467-line version -- replaced in Task 2),
    backend/src/seed/seed-education.ts (delete -- education now flows through new architecture)
  </files>
  <action>
FIRST, re-read every contract file in the context block and confirm each fact in contracts_verified still holds (enums, function signatures, timestamp math, day-name casing). If any drifted, follow the source file, not this plan, and note the drift in the SUMMARY.

Then build the generator subsystem. Create `seed/lib/rng.ts`: a mulberry32 seeded PRNG (constant seed, e.g. 0xC0FFEE) exposing rand() (0..1), randInt(min,max), pick(array), chance(p), and a deterministic uuid() that derives a v4-shaped UUID from PRNG bytes -- so re-running with the same seed yields byte-identical JSON. Every stochastic choice in the generator MUST route through this single RNG instance; no Math.random, no crypto.randomUUID, no Date.now.

Create content constant modules (this is the ONLY hand-written prose -- do NOT emit these as thousands of JSON literals by hand):
- content/education.ts: export an array of 28-32 article objects {judul, ringkasan, isi, tipeKonten, metodeTerapi, gambarUrl:null} matching the user's category counts (Nutrisi&Diet 7, CAPD 5, HD 4, Transplantasi 4, Obat 3, Gaya Hidup 3, Mental&Dukungan 3, Umum 4). isi = 3-6 paragraph plain-Indonesian empowering articles (reuse the calm, non-diagnostic voice already in the old seed-education.ts -- you may lift/adapt its existing article bodies). Map each to tipeKonten (artikel/panduan_senam/gaya_hidup) and metodeTerapi (CAPD/HD/Transplantasi/Umum).
- content/community.ts: export post + reply body templates (title/body strings, plus the anchored narrative posts: Lukman's April effluent-keruh question, Sari's post-Lebaran potassium/fruit post, Budi's "6 bulan pasca-transplan" milestone). Supportive tone.
- content/aiText.ts: export template builders for daily-summary / weekly-insight / lifestyle / lab-analysis narration (warm Indonesian). Each builder returns text that already ends with appendDisclaimer(...) output (import appendDisclaimer from lib/aiDisclaimer.ts) so stored JSON reflects the with-disclaimer plaintext.

Create generate-demo-data.ts -- the single deterministic writer. It computes the 180-day timeline (2026-01-08..2026-07-06), tags Ramadan (~17 Feb-19 Mar) and Lebaran (~20-21 Mar) windows for seasonal modulation, and emits JSON via fs.writeFileSync(JSON.stringify(obj, null, 2)) into seed/data/. Timestamps for timestamp-columns are precomputed as ISO strings using the WIB math in contracts_verified; text date/time columns as YYYY-MM-DD / HH:mm. Files to emit:
  - users.json (3 personas: fixed UUIDs, email lukman@/sari@/budi@demo.kidneybuddy.id, namaLengkap, informedConsent true, metodeTerapiAktif CAPD/HD/Transplantasi, tanggalMulaiTerapi -- Budi approx 2025-09, timezone Asia/Jakarta; passwordHash is a PLACEHOLDER string the loader overwrites -- do NOT hash in the generator, the loader owns hashing), plus embedded onboardingProgress (lastCompletedStep at max, reminderConfigured true, completedAt set) and therapyHistory rows.
  - reminder-schedule.json (all reminders with explicit UUIDs; obat daily = 7-day hariAktif; HD and EPO = senin/rabu/jumat; jenis obat/capd/hd; dosis/jenisObat/konsentrasiCapd/catatanWaktu filled; aktif true; fotoObat null). Lukman: 8 meds + 4 CAPD exchange reminders (06:00/12:00/18:00/22:00). Sari: 9 meds + HD reminder + EPO injeksi (jenisObat suntik, senin/jumat). Budi: 7 meds incl. takrolimus 08:00 and 20:00 (catatanWaktu "tiap 12 jam, jangan telat").
  - Sharded logs: fluid-log.{lukman,sari,budi}.json, medication-log.{lukman,sari,budi}.json, dialysis-log.{lukman,sari}.json, daily-activities.{lukman,sari,budi}.json. Volumes and clinical patterns per the task spec (CAPD 4x/day exchanges with masuk fill + keluar drain/UF/kondisiKeluar; HD Sen-Rab-Jum sessions + IDWG; medication_log from reminder x 180 days at 85-95% dikonfirmasi with occasional terlewat, waktuKonfirmasi = waktuPengingat + 0-45min; last-day future slots = tertunda). medication_log/dialysis_log rows carry the correct reminderId.
  - lab-results.json (4-6 panels/patient across 6 months; parameters + clinical ranges per spec; nilaiRujukan + kategori filled; sumber manual; explicit UUIDs; catatan = plaintext note for loader to encrypt).
  - anomaly-alerts.json (~6 total: Lukman effluent-keruh tinggi April + 1-2 normal; Sari kalium-tinggi post-Lebaran + 1 normal; Budi jadwal_terlewat Feb. deskripsi = plaintext Indonesian narrative WITH disclaimer; status of high-severity ones = ditindaklanjuti (NO active tinggi dated today, to avoid the emergency modal on demo login); ruleData sensible; isFallback false; tipePasien matches persona).
  - AI caches: ai-daily-summaries.json (~14 recent days/patient, plaintext-with-disclaimer, unique per user+tanggal), ai-weekly-insights.json (~8 recent weeks, pekan="2026-Wxx"), ai-lifestyle-suggestions.json (~7 recent days), ai-lab-analyses.json (1 per major lab panel, references lab_results id).
  - Community: community-posts.json (~35-45 posts across 6 months, explicit UUIDs, authored by rotating personas, kategori + metodeTerapi set, includes the 3 anchored narrative posts), community-replies.json (1-4 replies/post from other personas, explicit UUIDs, postId FK), community-reply-helpful.json (spread marks, respecting unique replyId+userId -- generator must dedupe pairs).
  - education-content.json (from content/education.ts array).
  - Do NOT emit push_subscriptions (device-specific). refresh_tokens / login_attempts / password_reset_tokens are truncate-only (no JSON).

Finally, DELETE the old seed/seed-demo.ts (replaced in Task 2 -- create the new file there) and DELETE seed/seed-education.ts (content now lives in content/education.ts + the new loader). Run the generator once and commit the produced data/*.json.
  </action>
  <verify>
    <automated>cd backend && npm run seed:demo:generate 2>&1 | tail -20 && for f in src/seed/data/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || { echo "BAD JSON: $f"; exit 1; }; done && node -e "const fs=require('fs');const u=JSON.parse(fs.readFileSync('src/seed/data/users.json','utf8'));const n=Array.isArray(u)?u.length:(u.users?u.users.length:0);if(n!==3)throw new Error('expected 3 users, got '+n);console.log('users ok')" && npx tsc --noEmit</automated>
  </verify>
  <done>
seed:demo:generate script exists and runs clean; all data/*.json parse as valid JSON; users.json has 3 personas; row counts across shards land in the spec ranges (fluid ~3-4k, medication ~7-9k, dialysis ~800, activities ~800-1200, community ~35-45 posts, education 28-32); tsc clean; old seed-demo.ts and seed-education.ts deleted; JSON committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Loader (new seed-demo.ts) -- TRUNCATE CASCADE + FK-ordered encrypted batch insert + npm scripts</name>
  <files>
    backend/src/seed/seed-demo.ts (new),
    backend/package.json
  </files>
  <action>
Write the new seed/seed-demo.ts loader. `import "dotenv/config"` first, then db/pool from lib/db.js, encrypt from lib/encryption.js, hashPassword from utils/passwordHash.js, sql from drizzle-orm, and the schema table objects from db/schema/index.js.

Structure:
1. Parse process.argv for an optional section arg (e.g. `npm run seed:demo -- fluid`). No arg = all sections. Define a section-to-loader-fn map so a single section can run in isolation. Document that single-section runs are for iterative debugging and assume parents already loaded; a full run (no arg) is the supported path.

2. On a full run, TRUNCATE all user-data tables in one statement using db.execute(sql`TRUNCATE TABLE ... RESTART IDENTITY CASCADE`) covering exactly: users, refresh_tokens, login_attempts, password_reset_tokens, onboarding_progress, reminder_schedule, therapy_history, push_subscriptions, fluid_log, medication_log, dialysis_log, daily_activities, lab_results, anomaly_alerts, ai_daily_summaries, ai_weekly_insights, ai_lab_analyses, ai_lifestyle_suggestions, community_posts, community_replies, community_reply_helpful, education_content. CASCADE handles FK order for truncation; RESTART IDENTITY resets serials. This satisfies requirement #3: zero old-seed rows survive.

3. Load each JSON section with JSON.parse(readFileSync(...)), insert in the FK order from contracts_verified, in batches of <=500 rows per insert (chunk arrays). For each table:
   - Hash passwords: replace each user's placeholder passwordHash with `await hashPassword("Demo1234!")` before inserting users.
   - Encrypt-on-load for the 8 sensitive columns listed in contracts_verified -- call encrypt(plaintext) per row when the value is non-null. Community + education inserted as-is (not encrypted).
   - Convert ISO-string timestamp fields from JSON back into Date objects for timestamp columns (drizzle expects Date); leave text date/time columns as strings.
   - Insert explicit id for users/reminder_schedule/lab_results/community_posts/community_replies (from JSON) so FKs resolve.
   - Log per-section progress in the style `✓ fluid_log: 3.240 rows` (clear per-section count line).

4. Wrap in try/finally that calls `await pool.end()`. On error, log which section failed and rethrow with a non-zero exit so a broken JSON file is immediately identifiable.

5. Update backend/package.json scripts: keep/replace seed:demo -> `node --import tsx src/seed/seed-demo.ts`; add seed:demo:generate -> `node --import tsx src/seed/generate-demo-data.ts`; remove the dead seed:education script (its file was deleted in Task 1).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && grep -q '"seed:demo:generate"' package.json && grep -q 'seed-demo.ts' package.json && ! grep -q 'seed:education' package.json && node -e "const s=require('fs').readFileSync('src/seed/seed-demo.ts','utf8');['encrypt(','hashPassword(','CASCADE','pool.end('].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});console.log('loader contract ok')"</automated>
  </verify>
  <done>
New seed-demo.ts exists; tsc clean; loader contains TRUNCATE ... CASCADE, encrypt(), hashPassword(), pool.end(); npm scripts seed:demo + seed:demo:generate present, seed:education removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run generate + load against Postgres, verify row counts + login + decryption end-to-end</name>
  <files>
    backend/src/seed/data/*.json (regenerated if any fix needed),
    backend/src/seed/seed-demo.ts (fixed if verify surfaces a bug)
  </files>
  <action>
Run the full pipeline against the live database and verify.

1. Confirm containers: `docker compose ps`. Preferred run path is inside the backend container (guarantees the same ENCRYPTION_KEY the API uses): `docker compose exec backend npm run seed:demo:generate` then `docker compose exec backend npm run seed:demo`. If the backend container is NOT running but db is, run from the host with the SAME ENCRYPTION_KEY as backend runtime (source it from root .env) and a DATABASE_URL pointing at the db container's published port. If NEITHER is up, `docker compose up -d db backend` first. Do NOT invent a new ENCRYPTION_KEY -- a mismatched key produces "Invalid ciphertext" on read.

2. Verify per-table row counts via psql in the db container, e.g. `docker compose exec db psql -U kidneybuddy -d kidneybuddy -c "SELECT 'users',count(*) FROM users UNION ALL SELECT 'fluid_log',count(*) FROM fluid_log UNION ALL SELECT 'medication_log',count(*) FROM medication_log UNION ALL SELECT 'dialysis_log',count(*) FROM dialysis_log UNION ALL SELECT 'daily_activities',count(*) FROM daily_activities UNION ALL SELECT 'lab_results',count(*) FROM lab_results UNION ALL SELECT 'anomaly_alerts',count(*) FROM anomaly_alerts UNION ALL SELECT 'community_posts',count(*) FROM community_posts UNION ALL SELECT 'education_content',count(*) FROM education_content;"`. Confirm counts match the spec ranges and that users = 3 (no leftover old-seed users).

3. Verify no active high-severity anomaly is dated today: `SELECT count(*) FROM anomaly_alerts WHERE severity='tinggi' AND status='aktif' AND created_at::date = CURRENT_DATE;` must return 0 (protects demo login from the emergency modal).

4. Login + decryption smoke test against the backend API (adjust base URL to the backend's published port, default from docker-compose): POST /api/auth/login with {"email":"lukman@demo.kidneybuddy.id","password":"Demo1234!"} -> expect 200 + access token. Then, using the returned token, GET the endpoints that read encrypted columns and confirm HTTP 200 with real decrypted text (NOT a 500 "Invalid ciphertext"): reminders list, fluid daily balance, education list, community feed, and (if reachable) an AI summary / anomaly list. Repeat the login for sari@ and budi@ to confirm all three accounts authenticate and land on beranda (onboarding complete).

5. Regression: `cd backend && npx tsc --noEmit` clean, and `npm test` (or at least any seed-related test file) shows no new failures beyond the 3 pre-documented container-only DB tests noted in STATE.md.

If any step fails, fix the generator or loader (re-generate + re-load), do NOT hand-edit committed JSON to paper over a generator bug.
  </action>
  <verify>
    <automated>cd backend && docker compose ps >/dev/null 2>&1 && echo "compose reachable"; docker compose exec -T db psql -U kidneybuddy -d kidneybuddy -tAc "SELECT count(*) FROM users;" | grep -q '^3$' && docker compose exec -T db psql -U kidneybuddy -d kidneybuddy -tAc "SELECT count(*) FROM anomaly_alerts WHERE severity='tinggi' AND status='aktif' AND created_at::date=CURRENT_DATE;" | grep -q '^0$' && echo "row+anomaly checks ok"</automated>
    <manual>curl -s -X POST http://localhost:PORT/api/auth/login -H 'Content-Type: application/json' -d '{"email":"lukman@demo.kidneybuddy.id","password":"Demo1234!"}' returns 200 + token; authenticated GET of reminders/fluid-balance/education/community/AI endpoints returns decrypted data with no "Invalid ciphertext" error, for all 3 personas.</manual>
  </verify>
  <done>
Full generate+load runs clean against Postgres; users=3 with zero old-seed rows; per-table counts in spec ranges; no active tinggi anomaly today; all 3 personas log in (200 + token) and every encrypted-column endpoint returns decrypted text; tsc clean; no new test regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed JSON -> database | Seed data is developer-authored, not user input; risk is data-integrity/encryption-consistency, not injection. |
| ENCRYPTION_KEY handling | Loader must use the same key as the API runtime; a mismatch silently corrupts read-time decryption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q0g-01 | Information Disclosure | Sensitive health columns seeded in plaintext JSON | accept | JSON is demo/synthetic data only (no real patient PII); plaintext-in-repo is intentional for inspectability. Loader encrypts at insert so the DB at-rest contract (NFR-02) still holds. |
| T-q0g-02 | Tampering | ENCRYPTION_KEY mismatch between loader and API | mitigate | Task 3 mandates running the loader inside the backend container (same env) or sourcing ENCRYPTION_KEY from the identical .env; decryption smoke test catches any mismatch before sign-off. |
| T-q0g-03 | Denial of Service | Demo login blocked by emergency high-severity modal | mitigate | Generator forbids active tinggi anomalies dated today; Task 3 SQL check asserts count = 0. |
| T-q0g-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies added (mulberry32 PRNG hand-written, all libs already in package.json); no install step, so no supply-chain surface introduced. |
</threat_model>

<verification>
- `npm run seed:demo:generate` reproducible (same seed -> identical JSON bytes); all data/*.json valid JSON.
- `npm run seed:demo` (full) TRUNCATEs then loads; users table = exactly 3; zero old-seed rows.
- All 3 personas authenticate via POST /api/auth/login (200 + token) and land on beranda (onboarding complete).
- Encrypted-column endpoints (fluid balance, activities, anomaly, AI summaries/insights/lab/lifestyle, lab notes) return decrypted text, no "Invalid ciphertext".
- Per-section CLI arg works: `npm run seed:demo -- fluid`.
- backend tsc clean; no new test regressions beyond the 3 documented container-only DB tests.
</verification>

<success_criteria>
- Two-part architecture in place: deterministic generator writes committed data/*.json (sharded big logs), loader reads + TRUNCATE CASCADE + FK-ordered encrypted batch insert.
- Hand-written prose confined to content/{education,community,aiText}.ts constants; no manually-authored bulk row JSON.
- 180 days of realistic data for Lukman (CAPD), Sari (HD), Budi (Transplantasi) with Ramadan/Lebaran seasonality and the engineered anomaly episodes.
- Old seed-demo.ts + seed-education.ts removed; no old-seed rows survive a load.
- npm scripts seed:demo + seed:demo:generate wired; end-to-end verified against live Postgres.
- 3 atomic commits (Task 1 generator+JSON, Task 2 loader+scripts, Task 3 verification fixes if any).
</success_criteria>

<output>
Create `.planning/quick/260706-q0g-rebuild-demo-seed-6-bulan-dengan-arsitek/260706-q0g-SUMMARY.md` when done.
</output>
