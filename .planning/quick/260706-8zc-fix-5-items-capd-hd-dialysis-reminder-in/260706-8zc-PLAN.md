---
quick_id: 260706-8zc
mode: quick
subsystem: reminders, dashboard, catatan-logs
tags: [pengingat-berikutnya-cuci-darah, medication-photo-catatan, reminder-photo-edit-bug, dosis-konsentrasi-catatan-display]
files_modified:
  - backend/src/services/medicationLog.service.ts
  - backend/src/services/dialysisLog.service.ts
  - frontend/components/pengingat/ReminderItem.tsx
  - frontend/components/beranda/PengingatBerikutnyaCard.tsx
  - frontend/components/beranda/ObatCard.tsx
  - frontend/components/beranda/CuciDarahCard.tsx
  - frontend/components/catatan/MedicationLogItem.tsx
  - frontend/components/catatan/DialysisLogItem.tsx
  - frontend/components/pengingat/ReminderList.tsx
autonomous: true
---

<objective>
Batch of 5 user-reported items for KidneyBuddy. Investigation of the current
codebase shows the picture is NOT "5 new features" — it is:

- **Item 1** (CAPD/HD in "Pengingat Berikutnya"): the backend `findNextUpcoming`
  ALREADY returns a grouped `{ obat, cuciDarah }` shape (cuciDarah = capd+hd),
  the service ALREADY therapy-scopes the cuciDarah group, and
  `PengingatBerikutnyaCard.tsx` ALREADY renders a "Pengingat Cuci Darah" section.
  This was wired in quick-260705-q7w. → **verification pass** (investigate & fix
  only if a genuine filtering bug is found).
- **Item 2** (medication photo in /catatan detail): the full chain already works —
  `medicationLog.service.getTodayLogs` attaches `fotoObat` from the owning
  reminder (medication_log has no fotoObat column of its own), `/catatan`'s
  `MedicationLogList` fetches `/api/medication-log/today`, and `MedicationLogItem`
  renders the photo with `unoptimized` (prior fixes: quick-260706-6oj + the
  quick-260706-573 UPLOAD_DIR path fix). → **verification pass**, folded into
  Task 2 since it touches the same component.
- **Item 3** (photo delete + re-upload bugs in the edit form): TWO real bugs.
  3(a) root cause FOUND — `ReminderList.handleUpdated` re-inserts the STALE
  pre-edit `reminder` object (ReminderItem passes the old prop, not fresh data),
  so a deleted photo persists in the UI even though the backend correctly nulls
  `fotoObat`. 3(b) (page reload on re-upload during edit) has no static
  smoking gun — the PATCH route DOES have multer and the form uses async
  `handleSubmit` — so it requires in-browser reproduction.
- **Items 4 + 5** (merged): surface `dosis` / `konsentrasiCapd` / `catatan` on
  ALL reminder & log card FACES across /beranda, /pengingat, /catatan. `catatan`
  is the existing `catatanWaktu` column — NO separate `catatan` field exists in
  any schema; do not invent one. Some surfaces need a backend change because
  `medication_log` / `dialysis_log` do not carry `catatanWaktu` (it lives on
  `reminder_schedule`).

Purpose: Close the 5 reported items with root-cause fixes, reusing the existing
muted sub-text pattern already established for `konsentrasiCapd`.
Output: 2 backend service edits, 7 frontend component edits, all UI text in
Bahasa Indonesia.
</objective>

<schema_truth>
Confirmed by reading the Drizzle schemas — do NOT invent fields:

- `reminder_schedule` columns: `dosis`, `jenisObat`, `fotoObat`, `konsentrasiCapd`,
  `catatanWaktu`, `jamPengingat`, `hariAktif`, `jenis`, `nama`, `aktif`. (full row)
- `medication_log` columns: `namaObat`, `dosis`, `jenisObat`, `status`,
  `waktuPengingat`, `waktuKonfirmasi`. **No `fotoObat`, no `catatanWaktu`.**
- `dialysis_log` columns: `jenis`, `nama`, `konsentrasiCapd`, `status`,
  `waktuPengingat`, `waktuKonfirmasi`. **No `catatanWaktu`.**

Endpoint field availability TODAY:
- `GET /api/reminders` (list) → full reminder rows: has dosis, konsentrasiCapd, catatanWaktu. ✓
- `GET /api/reminders/next` → full reminder rows (grouped): has dosis, konsentrasiCapd, catatanWaktu. ✓ (frontend interface just doesn't pick them)
- `GET /api/medication-log/today` → `MedicationLogWithFoto`: has dosis + fotoObat; **missing catatanWaktu**.
- `GET /api/dialysis-log/today` → `DialysisLog`: has konsentrasiCapd; **missing catatanWaktu**.

Rule for all display: render a field ONLY when it has a non-empty value. Never
show a bare "Dosis:" / "Catatan:" label with nothing after it. Use the existing
muted sub-text style (color `#7a8c8a`) already used for `konsentrasiCapd`.
</schema_truth>

<tasks>

<task type="auto">
  <name>Task 1: Backend — attach catatanWaktu to today-log responses</name>
  <files>backend/src/services/medicationLog.service.ts, backend/src/services/dialysisLog.service.ts</files>
  <action>
Surface `catatanWaktu` from the owning `reminder_schedule` row on both today-log
endpoints, mirroring the EXISTING `fotoObat` attach pattern in
`medicationLog.service.getTodayLogs` (the `fotoObatByReminderId` map built from
`listByUser` filtered to obat, then applied in the final `.map()` at the end of
the function).

medicationLog.service.ts:
- Extend the `MedicationLogWithFoto` type (currently `MedicationLog & { fotoObat: string | null }`)
  to also include `catatanWaktu: string | null`.
- Reuse the same `allObatReminders` lookup already built for fotoObat — add a
  parallel `catatanWaktuByReminderId` map (or store `{ fotoObat, catatanWaktu }`
  per reminderId). Source it from `listByUser` filtered to `jenis === "obat"`
  (NOT `findActiveObatByUser`) so a log whose reminder was later disabled still
  resolves its catatan, consistent with the existing fotoObat sourcing decision
  (quick-260706-573 task 2).
- In the final `.map()` that attaches `fotoObat`, also attach
  `catatanWaktu: log.reminderId ? map.get(log.reminderId) ?? null : null`.

dialysisLog.service.ts (`getTodayLogs`):
- Define a return type `DialysisLogWithCatatan = DialysisLog & { catatanWaktu: string | null }`
  and change the function signature to return `Promise<DialysisLogWithCatatan[]>`.
- Build a `catatanWaktuByReminderId` map from `reminderScheduleRepository.listByUser(userId)`
  filtered to `jenis === "capd" || jenis === "hd"` (all cuci-darah reminders,
  regardless of `aktif`, mirroring the medication decision).
- Attach `catatanWaktu` to every entry in the final `merged` array via a `.map()`
  (both the pseudo `scheduled-<id>` entries and the real DB rows), using the map
  keyed by `log.reminderId`, defaulting to `null`.

Do NOT change any other behavior (therapy-scoping, B3 not-yet-due filtering,
timezone bounds, sort). Do NOT add DB columns or migrations — this is a pure
read-time join from data already on `reminder_schedule`.
  </action>
  <verify>
`cd backend && npx tsc --noEmit` is clean for both edited service files.
`grep -c "catatanWaktu" backend/src/services/medicationLog.service.ts` returns >= 1
and same for `dialysisLog.service.ts`. If medication/dialysis service tests exist
(`ls backend/src/test/ | grep -iE "medicationlog|dialysis"`), run them with
`node --import tsx --test <files>` and confirm no regressions.
  </verify>
  <done>
`GET /api/medication-log/today` entries carry a `catatanWaktu` field (and still
carry `fotoObat` + `dosis`); `GET /api/dialysis-log/today` entries carry a
`catatanWaktu` field (and still carry `konsentrasiCapd`). Backend typechecks clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — surface dosis / konsentrasi / catatan on all card faces (+ verify /catatan photo)</name>
  <files>frontend/components/pengingat/ReminderItem.tsx, frontend/components/beranda/PengingatBerikutnyaCard.tsx, frontend/components/beranda/ObatCard.tsx, frontend/components/beranda/CuciDarahCard.tsx, frontend/components/catatan/MedicationLogItem.tsx, frontend/components/catatan/DialysisLogItem.tsx</files>
  <action>
Items 4+5 (merged) + item 2 verification. Add the relevant extra fields to each
card FACE, rendering ONLY when the value is non-empty, using the muted sub-text
style already present for `konsentrasiCapd` (`className="font-sans"` + `color: #7a8c8a`,
~13px, matching the surrounding lines in each component). All labels in Bahasa
Indonesia. Per surface:

1. ReminderItem.tsx (/pengingat) — obat already shows nama + `catatanWaktu`; capd
   already shows `konsentrasiCapd` + `catatanWaktu` (from quick-260706-6oj); hd
   shows `catatanWaktu`. ONLY change: add a `Dosis {reminder.dosis}` muted line
   for `jenis === "obat"` when `dosis` is present. `dosis` is already on the
   `Reminder` interface and returned by `/api/reminders`.

2. PengingatBerikutnyaCard.tsx (/beranda) — add `dosis?: string | null` and
   `konsentrasiCapd?: string | null` to the `NextReminder` interface (the backend
   `/api/reminders/next` already returns full reminder rows, so no backend change).
   In the obat section, render a muted `Dosis {rem.dosis}` line when present. In
   the cuciDarah section, render a muted `Konsentrasi {rem.konsentrasiCapd}` line
   when `rem.jenis === "capd"` and present. `catatanWaktu` is already rendered in
   both sections — leave it.

3. ObatCard.tsx (/beranda) — add `dosis?: string | null` and
   `catatanWaktu?: string | null` to the `MedicationEntry` interface. Under the
   `namaObat` + time lines, render muted `Dosis {entry.dosis}` and
   `Catatan: {entry.catatanWaktu}` lines when present. `dosis` is already on the
   endpoint; `catatanWaktu` comes from Task 1.

4. CuciDarahCard.tsx (/beranda) — `konsentrasiCapd` is already rendered on the
   face. Add `catatanWaktu?: string | null` to the `DialysisEntry` interface and
   render a muted `Catatan: {entry.catatanWaktu}` line when present (comes from
   Task 1). Keep the existing capd-only guard on the konsentrasi line.

5. MedicationLogItem.tsx (/catatan) — the card FACE currently shows only
   `namaObat` + time (dosis appears only in the detail overlay). Add
   `catatanWaktu?: string | null` to the `MedicationLog` interface, and on the
   card FACE render muted `Dosis {log.dosis}` and `Catatan: {log.catatanWaktu}`
   lines when present. ALSO (item 2 verification): confirm the existing detail-
   overlay photo block (`log.fotoObat` + `unoptimized` Image) actually renders a
   photo when the medication has one — this chain is already complete
   (getTodayLogs attaches fotoObat, unoptimized bypasses the optimizer). If, and
   only if, it does not render, root-cause it (check the resolved
   `${API_BASE}${log.fotoObat}` URL loads directly in the browser); otherwise
   make no change to the photo block.

6. DialysisLogItem.tsx (/catatan) — the card FACE shows nama + time·jenis;
   `konsentrasiCapd` currently appears only in the detail overlay. Add
   `catatanWaktu?: string | null` to the `DialysisLog` interface, and on the card
   FACE render muted `Konsentrasi {log.konsentrasiCapd}` (capd only) and
   `Catatan: {log.catatanWaktu}` lines when present (`catatanWaktu` comes from
   Task 1).

Do NOT add empty-label placeholders. Do NOT duplicate a `catatanWaktu` line where
one is already rendered (surfaces 1 and 2 already render it — don't add a second).
  </action>
  <verify>
`cd frontend && npx tsc --noEmit` is clean across all 6 edited files.
`grep -c "dosis" frontend/components/pengingat/ReminderItem.tsx` returns >= 1;
`grep -c "konsentrasiCapd" frontend/components/beranda/PengingatBerikutnyaCard.tsx`
returns >= 1; `grep -c "catatanWaktu" frontend/components/beranda/ObatCard.tsx`,
`frontend/components/beranda/CuciDarahCard.tsx`,
`frontend/components/catatan/MedicationLogItem.tsx`, and
`frontend/components/catatan/DialysisLogItem.tsx` each return >= 1.
  </verify>
  <done>
Obat cards show dosis (+ catatan where not already shown); CAPD cards show
konsentrasi + catatan; HD cards show catatan — on the card FACE across /beranda
(ObatCard, CuciDarahCard, PengingatBerikutnyaCard), /pengingat (ReminderItem),
and /catatan (MedicationLogItem, DialysisLogItem). Empty fields render nothing.
Medication photo confirmed visible in /catatan detail. Frontend typechecks clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify CAPD/HD reminders appear in "Pengingat Berikutnya" (item 1)</name>
  <files>backend/src/repositories/reminderSchedule.repository.ts, backend/src/services/reminders.service.ts, frontend/components/beranda/PengingatBerikutnyaCard.tsx</files>
  <action>
Item 1 is already implemented in code: `findNextUpcoming` returns
`{ obat, cuciDarah }` where `cuciDarah` = capd+hd reminders, `getNextUpcoming`
filters the cuciDarah group by the user's `metodeTerapiAktif` via
`isReminderVisibleForTherapy`, and `PengingatBerikutnyaCard.tsx` renders a
"Pengingat Cuci Darah" section whenever `grouped.cuciDarah.length > 0`.

This is a VERIFICATION task, not a rebuild. Do NOT re-implement the grouping.
Steps:
1. Trace the data path once more and confirm that, for a CAPD patient
   (`metodeTerapiAktif = "CAPD"`) with an active CAPD reminder scheduled for
   today/tomorrow, `findNextUpcoming` places it in `cuciDarah` and the therapy
   filter keeps it (isReminderVisibleForTherapy("capd", "CAPD") must be truthy),
   and likewise HD for an HD patient.
2. Confirm the ONLY reason the card would show obat-only is that `cuciDarah` is
   legitimately empty (no active dialysis reminder matching today/tomorrow's
   `hariAktif` and not already confirmed) — which is correct behavior, not a bug.
3. If, and ONLY if, you find a genuine filtering defect (e.g., a case-sensitivity
   mismatch between `metodeTerapiAktif` and `jenis`, or the `active`/`hariAktif`
   filter wrongly excluding valid dialysis reminders), fix it at the root in
   `reminderScheduleRepository.findNextUpcoming` or `isReminderVisibleForTherapy`
   and add/adjust a targeted test. Otherwise make NO code change and record in the
   SUMMARY that item 1 was already satisfied by quick-260705-q7w.

Do not "fix" by removing the therapy scope — an HD patient must NOT see CAPD
reminders and vice versa; that scoping is intentional (quick-260705-q7w decision).
  </action>
  <verify>
If a real defect is fixed: `cd backend && npx tsc --noEmit` clean and the relevant
test (`node --import tsx --test src/test/therapyReminderScope.test.ts` or a new
findNextUpcoming test) passes. If verification-only: document the confirmed data
path (which grouped bucket capd/hd land in, and that the card renders cuciDarah)
in the SUMMARY — no code diff required.
  </verify>
  <done>
Confirmed that an active CAPD (for a CAPD patient) or HD (for an HD patient)
reminder that is next-upcoming appears in the "Pengingat Cuci Darah" section of
"Pengingat Berikutnya". Either a root-cause fix landed with a passing test, or the
SUMMARY records that the behavior was already correct and why.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix photo delete persistence + re-upload-during-edit bugs (item 3)</name>
  <files>frontend/components/pengingat/ReminderList.tsx, frontend/components/pengingat/ReminderItem.tsx, frontend/components/pengingat/MedicationReminderForm.tsx</files>
  <action>
Two DISTINCT bugs — treat them separately, do not assume a shared root cause.

3(a) — Deleted photo still shows after save (ROOT CAUSE FOUND):
The backend delete path is CORRECT — the edit form sends `hapusFoto="true"` when
the existing photo was removed and no replacement chosen; `reminders.controller`
sets `fotoObat = null`; `reminders.service.updateReminder` + `reminderSchedule.update`
persist it via `.set({ ...data })`. The bug is purely frontend/state:
- `ReminderItem.handleEditSuccess` calls `onUpdated?.(reminder)` passing the STALE
  pre-edit `reminder` prop (still holding the old `fotoObat`).
- `ReminderList.handleUpdated` then re-inserts that stale object into list state
  (`prev.map(r => r.id === updated.id ? updated : r)`) — it MERGES the stale
  object instead of refetching. Reopening the detail overlay reads the stale
  `reminder`, showing the deleted photo.
Fix: after a successful EDIT, force a genuine server refetch instead of merging
the stale object. Add an `onEdited` prop to `ReminderItem` wired to
`ReminderList.fetchReminders`, and change `handleEditSuccess` to call
`onEdited?.()` (which refetches from `/api/reminders`) rather than
`onUpdated?.(reminder)`. Keep the existing `onUpdated(updated)` path for the
aktif-toggle (that path passes the REAL PATCH response, which is fine and avoids
an extra fetch). Ensure `SYNC_EVENTS.REMINDER_UPDATED` is still dispatched on
edit (fetchReminders path or explicitly). First VERIFY the backend actually nulls
the column: with a reminder that has a photo, remove it in the edit form, save,
then confirm via the API/DB that `fotoObat` is null — this proves the fix target
is the frontend refetch, not the backend.

3(b) — Page reloads / upload never completes when adding a NEW photo during edit
(NEEDS REPRODUCTION — no static smoking gun):
Confirmed facts: the PATCH `/api/reminders/:id` route DOES have
`medicationPhotoUpload.single("foto_obat")` multer middleware (field name matches
the form's `fd.append("foto_obat", ...)`), and the form submits via react-hook-form
`handleSubmit(onSubmit)` → `fetch(..., { method: "PATCH" })`, which should not
cause a native navigation. So the cause must be found by reproducing in the
browser. Required steps:
1. Reproduce: edit an existing obat reminder, add/replace a photo, submit; open
   DevTools Network + Console and observe whether (a) a multipart PATCH actually
   fires, (b) a full-page navigation/reload occurs instead, (c) any thrown error
   or unhandled rejection appears in onSubmit (e.g. during FormData construction
   or the `res.json()` parse).
2. Root-cause from the evidence. Likely candidates to check in order: a thrown
   exception in `onSubmit` BEFORE `fetch` (which would abort the handler and could
   surface as a dev-overlay reload), the `foto_obat` file failing multer's
   `fileFilter` (non jpg/png) and the error being mishandled, or the form
   escaping `handleSubmit` on some interaction. Add a defensive `e.preventDefault()`
   only if evidence shows a native submit is occurring.
3. Fix the actual confirmed cause at its root and re-test that BOTH create (must
   still work) and edit-with-new-photo now complete without reload and the new
   photo is stored and shown on next detail open.
If reproduction shows the PATCH already fires and succeeds and the only visible
symptom is the stale UI, then 3(b) collapses into 3(a) — note that in the SUMMARY
and confirm the 3(a) refetch fix resolves it.
  </action>
  <verify>
`cd frontend && npx tsc --noEmit` clean for all edited files. Manual/browser
reproduction documented in the SUMMARY: (1) removing a photo in edit + save →
reopening detail shows NO photo (and API confirms `fotoObat` null); (2) adding a
new photo in edit + save → no page reload, photo persists and shows on reopen;
(3) creating a reminder with a photo still works. If 3(b) required a code fix,
describe the confirmed root cause; if it collapsed into 3(a), say so explicitly.
  </verify>
  <done>
Deleting a photo in the edit form persists and the UI no longer shows the deleted
photo on reopen (root cause: stale merge replaced with a server refetch). Adding a
new photo during edit no longer reloads the page and the upload completes; create
path unaffected. Both sub-bugs root-caused, not surface-patched.
  </done>
</task>

</tasks>

<verification>
- Backend: `cd backend && npx tsc --noEmit` clean; medication/dialysis today
  responses include `catatanWaktu`.
- Frontend: `cd frontend && npx tsc --noEmit` clean across all 7 edited components.
- All new UI text is Bahasa Indonesia; empty fields render nothing (no bare labels).
- No new DB columns/migrations; no schema fields invented (catatan == catatanWaktu).
- Item 3 fixes are root-cause (frontend refetch for 3a; browser-reproduced cause
  for 3b), verified by the delete/re-upload walkthrough.
</verification>

<success_criteria>
1. CAPD/HD next-upcoming reminders appear in "Pengingat Berikutnya" (verified;
   fixed only if a real filtering defect was found).
2. Medication photo displays in the /catatan obat detail view (verified end-to-end).
3. Deleting a photo in the edit form persists and clears from the UI; adding a new
   photo during edit completes without a page reload.
4. Dosis (obat) / Konsentrasi Cairan (capd) / Catatan (catatanWaktu, all types)
   render on the card FACE across /beranda, /pengingat, and /catatan, only when
   non-empty, in the existing muted sub-text style.
</success_criteria>

<output>
Create `.planning/quick/260706-8zc-fix-5-items-capd-hd-dialysis-reminder-in/260706-8zc-SUMMARY.md`
when done. In it, explicitly state for items 1 and 2 whether they were
verification-only or required a fix, and for item 3 record the browser-reproduction
findings for 3(b).
</output>
