---
phase: quick-260707-9p9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/pengingat/MedicationReminderForm.tsx
  - frontend/components/pengingat/CAPDReminderForm.tsx
  - frontend/components/pengingat/ReminderDetailOverlay.tsx
autonomous: true
requirements: [UI-LABEL]
must_haves:
  truths:
    - "User melihat label \"Catatan\" (bukan \"Catatan Waktu\") di form pengingat obat"
    - "User melihat label \"Catatan\" di form pengingat CAPD"
    - "User melihat label \"Catatan\" saat menampilkan detail pengingat"
    - "Nama field internal `catatanWaktu` tetap utuh di semua file (schema/API/register)"
  artifacts:
    - path: "frontend/components/pengingat/MedicationReminderForm.tsx"
      provides: "Label form 'Catatan'"
      contains: "Catatan{"
    - path: "frontend/components/pengingat/CAPDReminderForm.tsx"
      provides: "Label form 'Catatan'"
      contains: "Catatan{"
    - path: "frontend/components/pengingat/ReminderDetailOverlay.tsx"
      provides: "DetailRow label 'Catatan'"
      contains: "label=\"Catatan\""
  key_links: []
---

<objective>
Ubah teks label yang tampil ke user dari "Catatan Waktu" menjadi "Catatan" saja di tiga komponen pengingat, mencocokkan wording HDReminderForm yang sudah benar. Ini murni perubahan teks UI.

Purpose: Konsistensi label antar form pengingat (HD sudah "Catatan"; Obat, CAPD, dan detail masih "Catatan Waktu") dan menyederhanakan wording untuk user awam.
Output: 3 file frontend dengan label user-facing "Catatan"; nama field internal `catatanWaktu` tidak berubah.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Referensi pola wording yang benar (sudah pakai "Catatan") — JANGAN diubah, hanya contoh
@frontend/components/pengingat/HDReminderForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ganti label "Catatan Waktu" menjadi "Catatan" di 3 komponen pengingat</name>
  <files>frontend/components/pengingat/MedicationReminderForm.tsx, frontend/components/pengingat/CAPDReminderForm.tsx, frontend/components/pengingat/ReminderDetailOverlay.tsx</files>
  <action>
Ubah HANYA teks label yang tampil ke user. Cocokkan wording dengan HDReminderForm.tsx (baris ~229) yang sudah benar memakai "Catatan".

1. MedicationReminderForm.tsx:
   - Baris ~280: ubah teks label dari `Catatan Waktu{" "}` menjadi `Catatan{" "}`. Biarkan `<span>...(opsional)</span>` di sebelahnya persis seperti semula.
   - Baris ~273 (komentar): ubah `{/* ── Catatan Waktu (optional) ── */}` menjadi `{/* ── Catatan (optional) ── */}` untuk konsistensi dengan HDReminderForm.

2. CAPDReminderForm.tsx:
   - Baris ~267: ubah teks label dari `Catatan Waktu{" "}` menjadi `Catatan{" "}`. Biarkan `<span>...(opsional)</span>` persis.
   - Baris ~260 (komentar): ubah `{/* ── Catatan Waktu (optional) ── */}` menjadi `{/* ── Catatan (optional) ── */}`.

3. ReminderDetailOverlay.tsx:
   - Baris ~80: ubah prop label pada DetailRow dari `label="Catatan Waktu"` menjadi `label="Catatan"`. Biarkan `value={reminder.catatanWaktu}` persis tidak berubah.

JANGAN sentuh: `{...register("catatanWaktu")}`, `value={reminder.catatanWaktu}`, id input (`rem-catatan`/`capd-catatan`), placeholder, schema, validator, atau kode backend. Nama field internal `catatanWaktu` HARUS tetap utuh di semua tempat.
  </action>
  <verify>
    <automated>cd frontend && grep -rn "Catatan Waktu" components/pengingat/ | grep -v "catatanWaktu" | wc -l | grep -q '^0$' && grep -q 'register("catatanWaktu")' components/pengingat/MedicationReminderForm.tsx && grep -q 'register("catatanWaktu")' components/pengingat/CAPDReminderForm.tsx && grep -q 'reminder.catatanWaktu' components/pengingat/ReminderDetailOverlay.tsx && echo PASS</automated>
  </verify>
  <done>
Tidak ada lagi teks user-facing "Catatan Waktu" di components/pengingat/ (grep = 0 kecuali identifier `catatanWaktu`). Ketiga file kini menampilkan label "Catatan". Field internal `catatanWaktu` (register/value) masih utuh di ketiga file.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "Catatan Waktu" frontend/components/pengingat/ | grep -v catatanWaktu` mengembalikan 0 baris.
- `grep -rn 'catatanWaktu' frontend/components/pengingat/` masih menemukan `register("catatanWaktu")` di kedua form dan `reminder.catatanWaktu` di overlay (field internal utuh).
- Rebuild frontend agar perubahan live: `docker compose build frontend && docker compose up -d frontend`.
- Manual (opsional): buka form pengingat obat & CAPD, dan detail pengingat — label kini "Catatan (opsional)" / "Catatan".
</verification>

<success_criteria>
- 3 label user-facing berubah dari "Catatan Waktu" menjadi "Catatan".
- 2 komentar kode diselaraskan menjadi "Catatan (optional)".
- Nama field internal `catatanWaktu` tidak berubah di schema/API/DB/register/value.
- Semua teks tetap Bahasa Indonesia awam.
- Frontend image di-rebuild agar perubahan tampil.
</success_criteria>

<output>
Create `.planning/quick/260707-9p9-ubah-label-catatan-waktu-menjadi-catatan/260707-9p9-SUMMARY.md` when done
</output>
