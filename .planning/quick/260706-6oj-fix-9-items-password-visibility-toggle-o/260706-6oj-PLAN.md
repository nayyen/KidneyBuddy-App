---
quick_id: 260706-6oj
type: quick
mode: quick
autonomous: true
requirements: [AUTH-06]
files_modified:
  - frontend/app/(auth)/login/page.tsx
  - frontend/components/shell/Sidebar.tsx
  - frontend/components/aktivitas/ActivityList.tsx
  - frontend/components/aktivitas/KegiatanModuleInline.tsx
  - frontend/components/pengingat/ReminderItem.tsx
  - frontend/components/beranda/CuciDarahCard.tsx
  - frontend/app/sw.ts
  - frontend/app/(app)/beranda/page.tsx
  - frontend/app/(app)/onboarding/page.tsx
  - frontend/app/(app)/onboarding/_components/TherapySelectStep.tsx
  - frontend/components/catatan/MedicationLogItem.tsx
  - frontend/components/pengingat/ReminderDetailOverlay.tsx
  - frontend/components/pengingat/MedicationReminderForm.tsx
  - backend/src/services/email.service.ts
  - backend/src/services/auth.service.ts
  - backend/src/test/auth.passwordReset.test.ts
  - .env.example
must_haves:
  truths:
    - "Login page password field has a working show/hide eye toggle"
    - "Medication photo displays in both the obat detail overlay and the reminder edit form"
    - "Desktop layout has a visible, dedicated button that navigates to /notifikasi"
    - "Forgot-password sends a real email via Resend when RESEND_API_KEY is set, with a console fallback when unset"
    - "A password reset token cannot be reused after a successful reset"
    - "Activities past their estimasi selesai show a 'Terlambat' overdue badge on /beranda and /catatan"
    - "CAPD reminder cards display the fluid concentration (konsentrasiCapd)"
    - "All reminder push notification action buttons read 'Sudah' (not 'Sudah diminum')"
    - "The 'belum mengatur pengingat' banner on beranda appears only when the user has zero reminders"
    - "Tutorial-mode onboarding shows only the existing user's current therapy on the Pilih Jenis Terapi step"
  artifacts:
    - path: "backend/src/services/email.service.ts"
      provides: "Real Resend email integration with dev console fallback"
  key_links:
    - from: "backend/src/services/auth.service.ts forgotPassword"
      to: "sendPasswordResetEmail (Resend)"
      via: "await call, generic success regardless of send outcome"
---

<objective>
Batch of 9 user-reported bug fixes / small features for KidneyBuddy. The user explicitly approved doing all 9 in one plan (one task per item). Tasks are ordered by risk/independence: simple UI-only fixes first, then the medication-photo display fix, then the Resend email integration last.

Purpose: Close a set of small, discrete gaps across auth UI, dashboard, reminders, onboarding, push notifications, and forgot-password email delivery.
Output: 9 focused fixes; all UI text stays in Bahasa Indonesia; no 4th Docker container introduced.
</objective>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Key files already inspected during planning (executor: read only the ones each task touches)
# - frontend/app/(auth)/login/page.tsx — plain type="password" inputs, no toggle; no existing eye-toggle pattern in register/reset-password to reuse
# - frontend/components/shell/{TopBar,Sidebar,AppShell}.tsx — TopBar already has a bell wired to onNotificationClick→router.push("/notifikasi"); Sidebar NAV list has NO /notifikasi entry
# - frontend/lib/nav.ts — NAV_ITEMS (Beranda/Catatan/Pengingat/Edukasi/Profil); Pengingat already uses the Bell icon
# - frontend/components/aktivitas/ActivityList.tsx — already computes `overdue` via isPastEstimasi, renders "· Lebih Dari Waktu Estimasi" text (not a "Terlambat" badge)
# - frontend/components/aktivitas/KegiatanModuleInline.tsx — beranda running activity already shows red "Terlewat X Menit"
# - frontend/components/pengingat/ReminderItem.tsx + ReminderDetailOverlay.tsx — detail overlay already shows "Konsentrasi CAPD"; the card face does not
# - frontend/components/beranda/CuciDarahCard.tsx — DialysisEntry carries konsentrasiCapd but never renders it
# - frontend/app/sw.ts — push handler hardcodes action title "Sudah diminum"; notificationclick already opens app at data.url generically (no per-type logic)
# - frontend/app/(app)/beranda/page.tsx — NoReminderBanner gated on `!onboardingProgress.reminderConfigured` (line ~110), NOT on actual reminder count
# - frontend/app/(app)/onboarding/page.tsx + _components/TherapySelectStep.tsx — tutorial mode loads all 3 therapy options; activeTherapy = user?.metodeTerapiAktif
# - backend/src/services/email.service.ts — sendPasswordResetEmail only console.logs; synchronous, fire-and-forget from auth.service.forgotPassword
# - backend/src/repositories/passwordResetToken.repository.ts — consumeIfValid() already atomically finds+marks used (single-use is ALREADY enforced; verify, don't rebuild)
# - backend/src/controllers/reminders.controller.ts — fotoObat stored as "/uploads/medication-photos/<file>"; GET /api/reminders returns an array
# - frontend/next.config.ts — next/image remotePatterns derives backend host from NEXT_PUBLIC_API_URL + always allows localhost:4000 /uploads/**
# - backend/src/lib/upload.ts — UPLOAD_DIR path mismatch already fixed in quick-260706-573
# - .env.example — existing env var conventions (placeholders only, never real secrets)
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Item 1): Login password show/hide toggle</name>
  <files>frontend/app/(auth)/login/page.tsx</files>
  <action>Add a show/hide eye toggle to the login password field. There is no existing toggle pattern in register/reset-password to reuse (all are plain `type="password"`), so build one here: add a `const [showPassword, setShowPassword] = useState(false)` and import `Eye`, `EyeOff` from `lucide-react`. Wrap the existing password `<input>` in a `relative` container, set its `type` to `showPassword ? "text" : "password"`, add right padding (e.g. `pr-11`) so text does not run under the icon, and add an absolutely-positioned `<button type="button">` on the right that toggles `showPassword` and renders `Eye`/`EyeOff`. Use the design-system teal for the icon color (e.g. `#3d6b66` idle) and keep the disabled-when-locked behavior consistent with the input. Give the button `aria-label={showPassword ? "Sembunyikan password" : "Tampilkan password"}`. Do not change any other field.</action>
  <verify>
    <automated>cd frontend && grep -q "EyeOff" "app/(auth)/login/page.tsx" && grep -q "showPassword" "app/(auth)/login/page.tsx" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^$" | head -1 | grep -qvi "login/page" && echo OK</automated>
  </verify>
  <done>Login password field toggles between masked and visible text via an eye icon; aria-label in Bahasa Indonesia; frontend typechecks.</done>
</task>

<task type="auto">
  <name>Task 2 (Item 3): Dedicated desktop /notifikasi button in the sidebar</name>
  <files>frontend/components/shell/Sidebar.tsx</files>
  <action>The desktop TopBar already has a bell wired to /notifikasi, but the desktop Sidebar (the primary desktop nav) has no notification entry, which is why it reads as "missing." Add a dedicated, desktop-only "Notifikasi" nav button to the Sidebar. Place it as an extra entry in the nav list (after the NAV_ITEMS map, or as its own button) so it visually matches the other sidebar items: same layout (icon + label, height 48, active/inactive border + color treatment), `onClick={() => router.push("/notifikasi")}`, `aria-label="Notifikasi"`, and active-state detection via `pathname.startsWith("/notifikasi")`. Use a bell-style lucide icon that is NOT the same as the Pengingat entry's `Bell` to avoid confusion — prefer `BellRing` from `lucide-react`. Optionally surface the unread count using the existing `useUnreadAnomalyCount` hook (mirror the TopBar dot badge) if it wires in cleanly; skip the badge if it adds noticeable complexity. Do not remove the TopBar bell.</action>
  <verify>
    <automated>cd frontend && grep -q "/notifikasi" components/shell/Sidebar.tsx && grep -q "BellRing\|Notifikasi" components/shell/Sidebar.tsx && echo OK</automated>
  </verify>
  <done>Desktop sidebar shows a "Notifikasi" entry that navigates to /notifikasi; label in Bahasa Indonesia; distinct icon from Pengingat.</done>
</task>

<task type="auto">
  <name>Task 3 (Item 5): "Terlambat" overdue badge on activities (beranda + catatan)</name>
  <files>frontend/components/aktivitas/ActivityList.tsx, frontend/components/aktivitas/KegiatanModuleInline.tsx</files>
  <action>Ensure activities past their estimasi selesai show a clear "Terlambat" overdue badge/label on BOTH the catatan list and beranda. In ActivityList.tsx an `overdue` boolean is already computed (status !== "selesai" && isPastEstimasi(estimasiSelesai)) and currently only appends "· Lebih Dari Waktu Estimasi" to the duration pill — add a distinct red "Terlambat" badge/label for overdue items (reuse the existing red palette `#d4183d` / bg `#fdecee`, radius ~10, small font) so the overdue state is unmistakable; keep the existing duration text. In KegiatanModuleInline.tsx the running activity already renders a red "Terlewat X Menit" indicator when past estimasi — confirm it fires for the overdue-and-not-completed case and, for wording consistency with the rest of the app, ensure the word "Terlambat" is present in that overdue label (adjust copy if needed). Do not change completed-activity rendering. Drive everything off the existing time comparison (now > estimasiSelesai); do not add new API calls.</action>
  <verify>
    <automated>cd frontend && grep -q "Terlambat" components/aktivitas/ActivityList.tsx && grep -qi "terl" components/aktivitas/KegiatanModuleInline.tsx && echo OK</automated>
  </verify>
  <done>Overdue activities display a "Terlambat" badge/label on /catatan and an overdue indicator on /beranda, both driven by now > estimasiSelesai; text in Bahasa Indonesia.</done>
</task>

<task type="auto">
  <name>Task 4 (Item 6): Show CAPD fluid concentration on reminder cards</name>
  <files>frontend/components/beranda/CuciDarahCard.tsx, frontend/components/pengingat/ReminderItem.tsx</files>
  <action>Surface the CAPD fluid concentration on the card faces (the detail overlay already shows it). The reminder/dialysis models expose `konsentrasiCapd` only — there is no separate "jenis cairan" field, so display `konsentrasiCapd` (the CAPD concentration, which encodes the fluid selection). In CuciDarahCard.tsx: for entries where `jenis === "capd"` and `konsentrasiCapd` is present, render the concentration on the session line (e.g. append after the time/jenis line: `Konsentrasi: {konsentrasiCapd}` or a small muted sub-label) — the field is already on `DialysisEntry`, just unused. In ReminderItem.tsx: for `reminder.jenis === "capd"` with a non-empty `reminder.konsentrasiCapd`, add a small muted line under the reminder name on the card face (e.g. `Konsentrasi {konsentrasiCapd}`). Only render when the value exists; do not invent new schema fields. Keep styling consistent with the existing muted sub-text (`#3d6b66` / `#7a8c8a`).</action>
  <verify>
    <automated>cd frontend && grep -q "konsentrasiCapd" components/beranda/CuciDarahCard.tsx && grep -c "konsentrasiCapd" components/pengingat/ReminderItem.tsx | grep -qv "^1$" && echo OK</automated>
  </verify>
  <done>CAPD reminder entries on /beranda (CuciDarahCard) and /pengingat (ReminderItem) show the fluid concentration when present; no schema changes.</done>
</task>

<task type="auto">
  <name>Task 5 (Item 7): Generalize push notification action label to "Sudah"</name>
  <files>frontend/app/sw.ts</files>
  <action>In the service worker push handler, change the confirm action title from "Sudah diminum" to the generic "Sudah" so the wording is correct for all reminder types (obat, CAPD, HD). Edit the `actions` array in `swSelf.registration.showNotification(...)`: `{ action: "confirm", title: "Sudah" }` (leave the "dismiss"/"Tutup" action unchanged). The existing `notificationclick` handler already works generically across reminder types — it opens/focuses the app at `event.notification.data.url` (defaulting to `/catatan`) where the user completes the in-app confirm; it deliberately does NOT POST from the SW (the SW has no access token — see the audit comment in the file). So no click-handler logic change is needed; only the label. Add a brief code comment noting the label is intentionally type-agnostic. Note in <done> that the service worker must be rebuilt/redeployed and the PWA updated on-device for the new label to appear.</action>
  <verify>
    <automated>cd frontend && grep -q 'title: "Sudah"' app/sw.ts && ! grep -q 'Sudah diminum' app/sw.ts && echo OK</automated>
  </verify>
  <done>Push notification confirm button reads "Sudah" for every reminder type; click still opens the app at the reminder's page. Requires SW rebuild + on-device PWA update to take effect.</done>
</task>

<task type="auto">
  <name>Task 6 (Item 8): Gate no-reminder banner on actual reminder count</name>
  <files>frontend/app/(app)/beranda/page.tsx</files>
  <action>The NoReminderBanner is currently rendered when `onboardingProgress && !onboardingProgress.reminderConfigured` (~line 110), which is an onboarding flag, not the real reminder count. Change the gate so the banner appears ONLY when the user has ZERO reminders configured. Add a fetch of `GET /api/reminders` (via `authFetch`, using the existing accessToken) that returns an array; store its length (e.g. `const [reminderCount, setReminderCount] = useState<number | null>(null)`). Render `<NoReminderBanner />` only when `reminderCount === 0`. While the count is still loading (`null`), do not flash the banner. Refresh the count when reminders change if a suitable sync event exists (e.g. the SYNC_EVENTS reminder-updated event already used elsewhere) — otherwise a fetch on mount is sufficient. Remove reliance on `onboardingProgress.reminderConfigured` for this banner's visibility.</action>
  <verify>
    <automated>cd frontend && grep -q "/api/reminders" "app/(app)/beranda/page.tsx" && grep -qE "reminderCount|length === 0|length > 0" "app/(app)/beranda/page.tsx" && echo OK</automated>
  </verify>
  <done>The "belum mengatur pengingat" banner shows only when the user has no reminders (reminder count 0); does not flash before the count loads.</done>
</task>

<task type="auto">
  <name>Task 7 (Item 9): Tutorial-mode onboarding scopes therapy step to current therapy</name>
  <files>frontend/app/(app)/onboarding/page.tsx, frontend/app/(app)/onboarding/_components/TherapySelectStep.tsx</files>
  <action>For existing users revisiting onboarding via `/onboarding?mode=tutorial` who already have a therapy set, the "Pilih Jenis Terapi" step must show ONLY their current therapy option; new users (no therapy set) keep all 3 options. In onboarding/page.tsx, `isTutorialMode` and `activeTherapy = selectedTherapy ?? user?.metodeTerapiAktif` already exist. Compute the options passed to TherapySelectStep: when `isTutorialMode && user?.metodeTerapiAktif`, filter `therapyOptions` to just the option whose `id` matches `user.metodeTerapiAktif`; otherwise pass the full list. Pass this filtered array (and optionally an `isLocked`/`lockedTherapy` prop) to `<TherapySelectStep .../>`. In TherapySelectStep.tsx, when only one option is present (or a lock prop is set), pre-select it (call `handleSelect`/`setValue("metodeTerapi", ...)` on mount so the "Lanjutkan" button is enabled) and it is fine that the single card is the only choice. Keep the full 3-option picker behavior unchanged for the new-user path. Ensure the therapy id values align (CAPD/HD/Transplantasi) between `user.metodeTerapiAktif` and `therapyOptions[].id`.</action>
  <verify>
    <automated>cd frontend && grep -qE "isTutorialMode|metodeTerapiAktif" "app/(app)/onboarding/page.tsx" && grep -qi "filter" "app/(app)/onboarding/page.tsx" && echo OK</automated>
  </verify>
  <done>Tutorial mode for a user with an existing therapy shows only that therapy on the selection step (pre-selected); new users still see all 3 options.</done>
</task>

<task type="auto">
  <name>Task 8 (Item 2): Fix medication photo display in detail view and edit form</name>
  <files>frontend/components/catatan/MedicationLogItem.tsx, frontend/components/pengingat/ReminderDetailOverlay.tsx, frontend/components/pengingat/MedicationReminderForm.tsx</files>
  <action>The medication photo still fails to render despite the earlier upload-path fix. All three display sites use `next/image` `<Image src={`${API_BASE}${fotoObat}`}>` against the backend origin. `next/image` routes remote images through the Next optimizer, which requires the backend host in `remotePatterns` AND the optimizer to be able to reach the backend server-side — a fragile setup for a cross-origin, dynamically-uploaded, non-CDN health photo (and the likely reason it silently fails outside the exact localhost config). Make the photo load robustly by bypassing the optimizer so the browser fetches the backend URL directly: add the `unoptimized` prop to each `next/image` used for `fotoObat` (MedicationLogItem detail overlay, ReminderDetailOverlay obat photo, and MedicationReminderForm's existing-photo preview), OR swap those specific `<Image>` usages for a plain `<img>` with the same sizing classes and the existing `onError` fallback (`setPhotoFailed(true)` where present). First verify the served path end-to-end: confirm the API returns `fotoObat` as `/uploads/medication-photos/<file>` and that `${API_BASE}${fotoObat}` is a directly-openable URL (backend serves `/uploads` statically). Keep the existing broken-image graceful fallback. Do not alter upload/storage logic (already fixed in quick-260706-573).</action>
  <verify>
    <automated>cd frontend && grep -qE "unoptimized|<img" components/catatan/MedicationLogItem.tsx && grep -qE "unoptimized|<img" components/pengingat/ReminderDetailOverlay.tsx && grep -qE "unoptimized|<img" components/pengingat/MedicationReminderForm.tsx && echo OK</automated>
  </verify>
  <done>Uploaded medication photo reliably displays in the obat detail overlay, the reminder detail overlay, and the reminder edit form's existing-photo preview; broken-image fallback preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9 (Item 4): Real forgot-password email via Resend + single-use verification</name>
  <files>backend/src/services/email.service.ts, backend/src/services/auth.service.ts, backend/src/test/auth.passwordReset.test.ts, .env.example</files>
  <behavior>
    - When RESEND_API_KEY is set, sendPasswordResetEmail calls the Resend send API once with the from/to/subject and a body containing the reset URL.
    - When RESEND_API_KEY is unset, sendPasswordResetEmail falls back to console logging the reset URL and does NOT throw (local dev without a key must not crash).
    - A password reset token is single-use: after resetPassword succeeds once, reusing the same token throws RESET_TOKEN_INVALID (already enforced by consumeIfValid — assert it).
    - forgotPassword still returns the generic success message regardless of send success/failure (no user enumeration).
  </behavior>
  <action>Integrate Resend as the real email provider while keeping a dev-safe fallback. Steps: (1) Add the `resend` npm package to the backend (`cd backend && npm install resend`). (2) Rewrite `sendPasswordResetEmail(email, rawToken)` in email.service.ts to be `async`: build `resetUrl = `${FRONTEND_URL}/reset-password/${rawToken}``; if `process.env.RESEND_API_KEY` is set, construct a Resend client and send an email `from` `process.env.RESEND_FROM_EMAIL` (fallback to a sensible placeholder like `KidneyBuddy <onboarding@resend.dev>` if unset), `to: email`, Indonesian subject (e.g. "Atur Ulang Password KidneyBuddy") and an HTML/text body containing `resetUrl`; if RESEND_API_KEY is unset, keep the current console.log fallback. To make this unit-testable without a live network call, give the function an optional injected sender parameter (deps pattern already used elsewhere in this codebase, e.g. 06-01) — e.g. `sendPasswordResetEmail(email, rawToken, sender?)` where `sender` defaults to the real Resend-backed implementation; tests pass a fake sender/spy. Read RESEND_API_KEY at call time (not module load) so tests can toggle it. (3) In auth.service.ts `forgotPassword`, `await` the now-async `sendPasswordResetEmail(...)` inside a try/catch so a send failure is swallowed (still return the generic success message — no enumeration leak). (4) Verify single-use: `consumeIfValid` in passwordResetToken.repository.ts already atomically finds+marks a token used — do NOT rebuild it; just confirm resetPassword uses it (it does). (5) Add placeholders to .env.example under a new "Resend (password-reset email)" section: `RESEND_API_KEY=<resend-api-key>` and `RESEND_FROM_EMAIL=KidneyBuddy <no-reply@yourdomain.com>` — placeholders only, never a real key. (6) Extend backend/src/test/auth.passwordReset.test.ts with unit tests that: assert the injected sender is invoked with the correct to/subject/url when a key path is exercised; assert the console fallback path is taken (spy on console.log) when RESEND_API_KEY is unset; and assert (documenting DB dependency where needed) that a token is single-use. Mock/inject — do NOT make real network calls. Structure so the integration works the instant the user pastes their own RESEND_API_KEY into .env.</action>
  <verify>
    <automated>cd backend && grep -q '"resend"' package.json && grep -q "RESEND_API_KEY" ../.env.example && node --test --test-name-pattern="passwordReset" 2>&1 | tail -5 | grep -qiE "pass|ok" && echo OK</automated>
  </verify>
  <done>`resend` installed; sendPasswordResetEmail sends via Resend when RESEND_API_KEY is set and console-logs otherwise without crashing; forgotPassword still returns generic success on send failure; token single-use verified; .env.example has RESEND_API_KEY + RESEND_FROM_EMAIL placeholders (no real secrets); tests mock the sender (no live network calls).</done>
</task>

</tasks>

<verification>
- Frontend typechecks: `cd frontend && npx tsc --noEmit` (Task 1–8 touch frontend).
- Backend typechecks + targeted tests: `cd backend && npx tsc --noEmit && node --test src/test/auth.passwordReset.test.ts` (Task 9).
- No 4th Docker service added; no real secrets committed (grep .env.example for placeholder angle-bracket values only).
- All added/changed UI copy is Bahasa Indonesia.
</verification>

<success_criteria>
All 9 items delivered:
1. Login password eye toggle works.
2. Medication photo shows in detail overlay + edit form.
3. Desktop sidebar has a /notifikasi button.
4. Forgot-password sends real Resend email with dev fallback; token single-use verified; tests mock the sender.
5. Overdue "Terlambat" badge on activities (beranda + catatan).
6. CAPD reminder cards show konsentrasiCapd.
7. Push confirm button reads "Sudah" for all reminder types.
8. No-reminder banner gated on zero reminders.
9. Tutorial-mode onboarding scopes therapy step to the user's current therapy.
</success_criteria>

<output>
On completion, write a SUMMARY to `.planning/quick/260706-6oj-fix-9-items-password-visibility-toggle-o/260706-6oj-SUMMARY.md` noting per-item outcomes, the SW-rebuild requirement for Item 7, and that Item 4 awaits the user's real RESEND_API_KEY.
</output>
