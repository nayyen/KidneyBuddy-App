---
phase: quick-260705-9n4
plan: 260705-9n4
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [BUGFIX-CROSS-PAGE]
files_modified:
  - backend/src/services/medicationLog.service.ts
  - backend/src/services/dialysisLog.service.ts
  - backend/src/utils/wib.ts
  - backend/src/db/schema/user.schema.ts
  - backend/src/controllers/profile.controller.ts
  - backend/src/jobs/reminderDispatch.job.ts
  - backend/src/jobs/reminderFollowUp.job.ts
  - backend/src/jobs/activityEndReminder.job.ts
  - backend/src/repositories/reminderSchedule.repository.ts
  - backend/src/repositories/labResult.repository.ts
  - backend/src/services/activities.service.ts
  - backend/src/controllers/activities.controller.ts
  - frontend/app/sw.ts
  - frontend/lib/pushClient.ts
  - frontend/components/beranda/ObatCard.tsx
  - frontend/components/beranda/CuciDarahCard.tsx
  - frontend/components/beranda/PengingatBerikutnyaCard.tsx
  - frontend/components/beranda/HumanFluidChart.tsx
  - frontend/components/beranda/DeltaCairanCard.tsx
  - frontend/components/catatan/MedicationLogList.tsx
  - frontend/components/catatan/DialysisLogList.tsx
  - frontend/components/aktivitas/MulaiKegiatanForm.tsx
  - frontend/components/aktivitas/ActivityList.tsx
  - frontend/components/pengingat/MedicationReminderForm.tsx
  - frontend/components/pengingat/ReminderDetailOverlay.tsx
  - frontend/components/pengingat/AddReminderSheet.tsx
  - frontend/components/cairan/CatatCairanForm.tsx
  - frontend/lib/validators/fluid.schema.ts
  - backend/src/services/fluid.service.ts

must_haves:
  truths:
    - "Checking or unchecking a medication/dialysis item on /beranda or /catatan succeeds (no HTTP 500)."
    - "All displayed times, reminder scheduling, and reminder-due firing follow the user's own device timezone, not hardcoded WIB."
    - "No form or label anywhere in the frontend contains the text 'WIB'."
    - "Logging or checking an item on one of /beranda, /catatan, /pengingat is reflected on the other two without a manual reload."
    - "The 'Sumber' fluid-in dropdown shows Makanan/Minuman/(Exchange CAPD if CAPD)/Lainnya and never Urine; Urine only appears for Cairan Keluar."
    - "Required fields no longer carry an '(opsional)' label."
    - "Mulai Kegiatan collects an estimated finish clock-time and an activity is only flagged 'terlambat' after that time has passed."
  artifacts:
    - path: backend/src/services/medicationLog.service.ts
      provides: "scheduled-<id> pseudo-entry safe confirm/uncheck routing"
    - path: backend/src/utils/wib.ts
      provides: "timezone-aware time helpers keyed by user timezone"
    - path: frontend/app/sw.ts
      provides: "env-driven API base and working push handlers"
  key_links:
    - from: frontend/components/beranda/ObatCard.tsx
      to: backend/src/services/medicationLog.service.ts
      via: "POST /api/medication-log/:logId/confirm"
      pattern: "medication-log/.*/confirm"
---

<objective>
Fix a batch of cross-page bugs reported against the shipped KidneyBuddy v1.0 build, spanning the /beranda, /catatan, /pengingat pages plus the shared reminder/timezone/push infrastructure. The single highest-impact defect is an HTTP 500 that blocks all check/uncheck actions on scheduled reminder items; several page bugs are downstream of it. Work proceeds foundation-first (500 fix, timezone, push, data-sync) then page-specific.

Purpose: Restore the app's core value — reliable daily tracking and reminders — by fixing data-entry/toggle failures, making times honor the user's own device timezone, and getting web push actually firing.
Output: A working check/uncheck flow, timezone-correct reminders and display, functioning push pipeline, synchronized cross-page state, and corrected page-level UI/logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Root-cause reference (already investigated during planning):
# - backend/src/services/medicationLog.service.ts getTodayLogs emits pseudo-entries
#   with id="scheduled-<reminderId>"; frontend posts these to confirmById → the id
#   is cast to a uuid column → Postgres "invalid input syntax for type uuid" → 500.
# - backend/src/utils/wib.ts hardcodes a +7h offset used by dispatch + getTodayLogs.
# - frontend/app/sw.ts hardcodes API_BASE="http://localhost:4000".
# - frontend/lib/validators/fluid.schema.ts FLUID_SUMBER = ["urine","capd","lainnya"].
@backend/src/services/medicationLog.service.ts
@backend/src/services/dialysisLog.service.ts
@backend/src/utils/wib.ts
@frontend/components/beranda/ObatCard.tsx
@frontend/components/cairan/CatatCairanForm.tsx
@frontend/lib/validators/fluid.schema.ts
</context>

<execution_note>
This is a large quick task executed sequentially. Run tasks in numeric order — later page tasks depend on earlier foundation tasks (Task 1's toggle fix, Task 2's timezone helpers, Task 5's sync events). Before editing each area, re-read the current file: several reported bugs may already be partially fixed by prior "phase 4.1"/"phase 4.2" work — verify actual current behavior and only change what is still broken. Commit after each task (or each coherent pair) so regressions are bisectable.
</execution_note>

<tasks>

<task type="auto">
  <name>Task 1: Fix HTTP 500 on check/uncheck of scheduled reminder items (item 2, B3, C1, C2 toggle half)</name>
  <files>backend/src/services/medicationLog.service.ts, backend/src/services/dialysisLog.service.ts, backend/src/test/medicationLog.service.test.ts (or nearest existing test)</files>
  <action>Root cause: getTodayLogs (both services) returns pseudo-entries whose id is the literal string "scheduled-<reminderId>" for reminders that have no real log row yet. The frontend cards/lists call confirmById/unconfirmById with that string; the repository passes it into a uuid column (medicationLog.id / dialysisLog.id), producing "invalid input syntax for type uuid" and a 500. Fix in the SERVICE layer so it is robust regardless of which frontend path calls it: in confirmById(userId, logId) detect logId starting with the prefix "scheduled-"; when present, strip the prefix to recover the reminderId and delegate to the existing reminder-based confirm(userId, reminderId) path (which creates-or-updates the real log row). In unconfirmById(userId, logId) detect the same prefix; strip it, look up the existing log via findByReminderAndUser(reminderId, userId), and mark it unconfirmed if found (no-op if none exists yet — an unconfirmed scheduled item is already effectively "tertunda"). Leave the real-uuid code paths unchanged. Apply the identical guard to BOTH medicationLog.service.ts and dialysisLog.service.ts. Do NOT change getTodayLogs' pseudo-id format — the frontend relies on it as a stable key.</action>
  <verify>
    <automated>cd backend && npm run build && npm test -- medicationLog.service 2>&1 | tail -20</automated>
  </verify>
  <done>confirmById/unconfirmById accept a "scheduled-<reminderId>" argument without any uuid cast error and correctly create/update the real log; backend builds; unit test covers the scheduled-prefix branch for at least the medication service.</done>
</task>

<task type="auto">
  <name>Task 2: Backend timezone foundation — per-user device timezone drives reminder due-time and "today" bounds (item 4 backend half)</name>
  <files>backend/src/db/schema/user.schema.ts, backend/src/db/migrations/ (new migration), backend/src/controllers/profile.controller.ts, backend/src/routes/ (profile route), backend/src/utils/wib.ts, backend/src/jobs/reminderDispatch.job.ts, backend/src/jobs/reminderFollowUp.job.ts, backend/src/jobs/activityEndReminder.job.ts, backend/src/services/medicationLog.service.ts, backend/src/services/dialysisLog.service.ts, backend/src/repositories/reminderSchedule.repository.ts</files>
  <action>Replace the hardcoded WIB (+7h) assumption with the user's own timezone. Add a nullable `timezone` text column to the users table (IANA name, e.g. "Asia/Jakarta", "Asia/Makassar") via a new Drizzle migration; default existing rows to "Asia/Jakarta" so behavior is unchanged until the client reports otherwise. Add a small authenticated endpoint (PATCH /api/profile/timezone or extend the existing profile update) that accepts { timezone } and persists it. In backend/src/utils/wib.ts, add timezone-aware variants of the helpers that currently assume +7h: functions to compute a user's current local "HH:mm", local lowercase day-name, and local calendar-day bounds given an IANA timezone (use Intl.DateTimeFormat with the timeZone option / the `Intl` `formatToParts` approach — do NOT add a new dependency; Node 20's built-in Intl is sufficient). Keep the existing WIB helpers for the fixed-time batch jobs that are intentionally Jakarta-anchored (anomaly/daily-summary/weekly-insight at fixed WIB clock times per prior decisions) unless they display user-facing per-user times. Update the per-minute reminder dispatch: instead of computing one global wibHHmm()/wibDayNameLower() and matching all users, the dispatch must fire a user's reminder when the CURRENT time in THAT user's timezone matches jamPengingat and their hariAktif includes the current day in their timezone. Implement this by either (a) grouping due-checking per user timezone, or (b) having findDueReminders join the user's timezone and compare in-query/in-code — choose the lower-risk option and document it in a code comment. Update reminderFollowUp.job.ts and activityEndReminder.job.ts equivalently where they compute "now" against stored times. Update getTodayLogs in both medication and dialysis services to compute the day-name filter and waktuPengingat using the requesting user's timezone rather than wibDayNameLower()/wibDateFromHHmm(). Assumption (state in a comment): jamPengingat is a wall-clock "HH:mm" string interpreted in the user's local timezone; a reminder set for 07:00 fires at 07:00 local wherever the user is.</action>
  <verify>
    <automated>cd backend && npm run build && npm test 2>&1 | tail -25</automated>
  </verify>
  <done>users.timezone column + migration exist; an endpoint persists the client-reported timezone; reminder dispatch and getTodayLogs compute due-ness/"today" from the user's stored timezone; backend builds and existing tests pass (update any test that hardcoded WIB expectations).</done>
</task>

<task type="auto">
  <name>Task 3: Frontend timezone capture + remove all WIB UI labels (item 4 frontend half, P3)</name>
  <files>frontend/lib/hooks/useAuth.ts (or a small effect in AppShell), frontend/components/pengingat/MedicationReminderForm.tsx, frontend/components/pengingat/CAPDReminderForm.tsx, frontend/components/pengingat/HDReminderForm.tsx, frontend/components/aktivitas/KegiatanModuleInline.tsx, frontend/components/aktivitas/ActivityList.tsx, frontend/components/beranda/AiDailySummaryCard.tsx, frontend/components/lab/WeeklyInsightCard.tsx, frontend/components/laporan/LaporanPreviewContent.tsx</files>
  <action>Two parts. (1) Timezone capture: on authenticated app load, read the browser's IANA timezone via Intl.DateTimeFormat().resolvedOptions().timeZone and PATCH it to the endpoint added in Task 2 (only when it differs from the last-sent value to avoid redundant calls — a module-scoped guard or localStorage flag is fine). Mount this in a place that runs once per session on any page (AppShell is a good host). (2) Remove WIB copy: grep the entire frontend/ (excluding node_modules and .next) for the literal "WIB" and remove or rephrase every user-facing occurrence — form helper text like "jam WIB", "waktu WIB", "(WIB)" suffixes on time labels in the reminder forms, activity components, AI summary/insight cards, and the laporan preview. Where a label currently reads e.g. "Jam Pengingat (WIB)" it becomes "Jam Pengingat"; where helper text explains a WIB assumption, delete that sentence. Do not touch backend-anchored fixed-time batch descriptions in code comments — only user-visible strings. After editing, a repository grep for "WIB" under frontend (excluding node_modules/.next) must return zero matches.</action>
  <verify>
    <automated>cd frontend && grep -rn "WIB" app components lib 2>/dev/null | grep -v node_modules | grep -v ".next" | wc -l | grep -qx 0 && echo "NO WIB LABELS OK" && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Browser timezone is reported to the backend once per session; zero "WIB" strings remain in frontend app/components/lib; frontend builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Audit + fix the web push notification pipeline (item 3)</name>
  <files>frontend/app/sw.ts, frontend/lib/pushClient.ts, frontend/components/push/NotificationPermissionBanner.tsx, backend/src/services/push.service.ts, backend/src/services/notification.service.ts, backend/src/lib/webPushClient.ts, backend/src/controllers/push.controller.ts</files>
  <action>The user has never received a push notification. Audit and repair the full pipeline end-to-end. Known defects to fix: (1) frontend/app/sw.ts hardcodes API_BASE="http://localhost:4000" — in the deployed build this points nowhere; make the service worker use the deployed backend origin (inject it at build time via the Serwist build define / a generated constant, or read from a value baked into the SW at build — the SW cannot read process.env at runtime, so it must be substituted at build). (2) Verify the notificationclick "confirm" handler's cross-origin cookie POST actually authenticates — if the session cookie is not sent cross-site (SameSite), switch that action to open the app to /catatan rather than silently failing, OR document that confirm-from-notification requires the tab. Audit and confirm each stage is wired: VAPID keys present in both frontend (NEXT_PUBLIC_VAPID_PUBLIC_KEY) and backend (VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY, set via webpush.setVapidDetails in webPushClient.ts); the permission banner requests Notification permission on a user gesture and then calls subscribeAndRegister; POST /api/push/subscribe stores the subscription per-device; reminderDispatch → sendToAllDevices → webpush.sendNotification runs without throwing (check logs). Fix whatever link is broken. Because live push delivery requires a real device and cannot be asserted by an automated test, this task ends in a human-verify checkpoint: the developer will run a live test together after the fix.</action>
  <what-built>Repaired push pipeline: env-driven SW backend origin, verified VAPID config, subscription registration, and dispatch fan-out.</what-built>
  <how-to-verify>
    1. Confirm VAPID env vars are set in backend and frontend (.env).
    2. In the deployed/preview app, grant notification permission when prompted; confirm a push_subscriptions row is created.
    3. Trigger a due reminder (or a manual test send) and confirm a native notification appears on the device.
    4. Tap the notification and confirm it behaves correctly (opens app / confirms dose).
  </how-to-verify>
  <resume-signal>Type "approved" once a push notification is confirmed received on a real device, or describe what stage still fails.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Cross-page data synchronization across /beranda, /catatan, /pengingat (item 5)</name>
  <files>frontend/components/beranda/ObatCard.tsx, frontend/components/beranda/CuciDarahCard.tsx, frontend/components/beranda/DeltaCairanCard.tsx, frontend/components/beranda/PengingatBerikutnyaCard.tsx, frontend/components/catatan/MedicationLogList.tsx, frontend/components/catatan/DialysisLogList.tsx, frontend/components/catatan/FluidLogList.tsx, frontend/app/(app)/pengingat/page.tsx, frontend/components/pengingat/ReminderList.tsx</files>
  <action>The app already uses window CustomEvents (fluid:saved, obat:confirmed, cucidarah:confirmed, reminder:updated, activity:saved, lab:saved) plus some polling, but coverage is inconsistent so the three pages drift out of sync. Standardize it: define a single canonical set of refresh events and make every fluid/medication/dialysis/reminder mutation dispatch the appropriate event(s), AND make every list/card that displays that data subscribe to those events and refetch. Specifically: confirming/unconfirming medication must dispatch obat:confirmed (already partially done) — ensure /catatan's MedicationLogList and /pengingat also listen; dialysis confirm/uncheck must dispatch cucidarah:confirmed and be listened for on all three surfaces; creating/editing/deleting a reminder must dispatch reminder:updated and be listened for by ObatCard, CuciDarahCard, PengingatBerikutnyaCard, and the /catatan lists; fluid save/edit/delete must dispatch fluid:saved and refresh DeltaCairanCard and the /catatan FluidLogList. Additionally add a "refetch on tab focus" via a visibilitychange/focus listener on each of the three pages so navigating back to a page (or returning to the tab) always shows current data. Keep the event-name constants DRY (a small shared module of event-name strings under frontend/lib is acceptable) so dispatchers and listeners cannot typo-drift.</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>A mutation on any one of the three pages causes the other two to reflect the change without a manual reload; returning focus to a page triggers a refetch; frontend builds.</done>
</task>

<task type="auto">
  <name>Task 6: /beranda visuals + daily reset (B1, B2) and verify bell (item 1)</name>
  <files>frontend/components/beranda/HumanFluidChart.tsx, frontend/components/beranda/DeltaCairanCard.tsx, frontend/components/shell/MobileHeader.tsx, frontend/components/shell/TopBar.tsx</files>
  <action>Three items. (B1) In HumanFluidChart.tsx, fix ONLY the foot/sole path so the soles curve outward (convex) instead of inward (concave); leave the head, torso, arms, and legs untouched. Identify the foot sub-path(s) and invert the curvature of the sole arc so the bottom of each foot bulges downward/outward. (B2) The human-silhouette fill and the "cairan masuk/keluar hari ini" numbers must reset to 0 at local-timezone midnight when no fluid has been logged for the new day. Root cause to check: DeltaCairanCard/HumanFluidChart likely key their "today" off stale client state or a WIB day boundary. Ensure the "today" balance is fetched using the user's local day (the backend getDailyBalance/"today" bounds now honor user timezone from Task 2) and that the component re-derives "today" on focus/day-change (a lightweight check on mount + on the visibilitychange refetch from Task 5 is sufficient) so yesterday's totals never persist into a new day with no entries. (item 1) Verify the header notification bell already navigates to /notifikasi — MobileHeader/TopBar are wired to onNotificationClick → router.push("/notifikasi") via AppShell; confirm this works and only change it if broken. Do not duplicate a bell elsewhere.</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Foot soles render convex; fluid chart + today's in/out totals show 0 on a fresh local-midnight day with no logs; bell navigates to /notifikasi (confirmed).</done>
</task>

<task type="auto">
  <name>Task 7: /beranda Pengingat Berikutnya rotation + Obat/Cuci Darah reminder-time gating (B3, B4)</name>
  <files>frontend/components/beranda/PengingatBerikutnyaCard.tsx, frontend/components/beranda/ObatCard.tsx, frontend/components/beranda/CuciDarahCard.tsx, backend/src/repositories/reminderSchedule.repository.ts, backend/src/controllers/reminders.controller.ts</files>
  <action>(B3) "Obat Hari Ini" and "Cuci Darah Hari Ini" cards should behave like reminders: a scheduled item only appears once the current local time has reached its jamPengingat (not all day). Adjust the today-list rendering (or the getTodayLogs merge) so scheduled-but-not-yet-due items are hidden until their time arrives, while already-confirmed/logged items remain visible; ensure items still support check/uncheck (relies on Task 1). (B4) Rework "Pengingat Berikutnya" rotation: for medication, show ALL medication reminders that share the SAME next upcoming time-slot together (the /api/reminders/next endpoint already returns grouped obat/cuciDarah arrays — confirm it groups by identical jamPengingat slot; extend the repository if it only returns a single item). When a shown medication item is checked (confirmed) in this widget, it must disappear from "Pengingat Berikutnya" and appear in "Obat Hari Ini" as completed, and the widget must advance to the next upcoming slot's medications once all items in the current slot are done. Un-checking an item in "Obat Hari Ini" moves it back into "Pengingat Berikutnya" under its slot. Apply the same rotation to Cuci Darah reminders. Implement the check/uncheck actions in the widget to call the same confirm/unconfirm endpoints and dispatch the sync events from Task 5 so all three sections update together. State assumption in a comment: "slot" = identical jamPengingat HH:mm among currently-upcoming reminders of the same jenis.</action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | tail -8 && cd ../frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Pengingat Berikutnya shows all same-slot meds together with inline check; checking moves an item to Obat Hari Ini and advances the slot; unchecking returns it; Obat/Cuci Darah cards only reveal items once their time is due; both builds pass.</done>
</task>

<task type="auto">
  <name>Task 8: Mulai Kegiatan "Estimasi Selesai Jam" + clock-based terlambat (B5)</name>
  <files>frontend/components/aktivitas/MulaiKegiatanForm.tsx, frontend/components/aktivitas/ActivityList.tsx, backend/src/controllers/activities.controller.ts, backend/src/services/activities.service.ts</files>
  <action>The backend dailyActivity schema already stores estimasiSelesai as an absolute timestamp, but MulaiKegiatanForm still collects "Estimasi Durasi (menit)" as a number and derives the finish time. Change the form field to "Estimasi Selesai Jam": a time picker (input type="time") where the user picks a wall-clock finish time (e.g. 17:00). On submit, send that finish time to the backend as an absolute finish timestamp for today in the user's local timezone (compute the absolute instant client-side from the picked HH:mm + today's local date, or send the HH:mm and let the backend build it using the user's timezone from Task 2 — pick one and keep the backend contract consistent). Update activities.controller/service to accept the finish-time form of the payload (replace or supplement the estimasiMenit contract; update the create schema/validation accordingly). In ActivityList, an activity is flagged/markable as "terlambat" only once the current local time has passed estimasiSelesai — remove any elapsed-minutes-based lateness logic and compare against the stored finish timestamp. Update the field label, helper text, and any test fixture that referenced estimasiMenit.</action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | tail -8 && cd ../frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Mulai Kegiatan collects a finish clock-time via a time picker; backend stores it as estimasiSelesai; activities show "terlambat" only after that time passes; both builds pass and no stale estimasiMenit label/copy remains in the form.</done>
</task>

<task type="auto">
  <name>Task 9: /catatan fixes — obat detail image + uncheck, cuci darah uncheck, lab dropdown pollution, aktivitas text size (C1, C2, C3, C4)</name>
  <files>frontend/components/catatan/MedicationLogList.tsx, frontend/components/catatan/MedicationLogItem.tsx, frontend/components/catatan/DialysisLogList.tsx, frontend/components/catatan/DialysisLogItem.tsx, frontend/components/lab/LabTrendChart.tsx, backend/src/repositories/labResult.repository.ts, backend/src/controllers/labResult.controller.ts, frontend/components/aktivitas/ActivityList.tsx</files>
  <action>(C1) Obat tab: ensure check AND uncheck both work (relies on Task 1's scheduled-id fix); when a medication entry's detail popup opens, display the actual uploaded medication photo inline. Find how the reminder/medication photo path is stored and served, build the correct image src (backend static/upload URL, not a raw filename or alt text), and render an <img> with a graceful fallback. (C2) Cuci Darah tab: ensure check/uncheck both work (same Task 1 fix path for dialysis). (C3) Lab tab: the parameter dropdown is polluted by uploaded file NAMES. Root cause: GET /api/lab/parameters returns DISTINCT values that include uploaded-file rows whose parameter/nama column holds the filename. Fix the backend query so /api/lab/parameters returns ONLY canonical lab parameter names (e.g. restrict to manual-entry rows, or to a known parameter enum/whitelist, or exclude rows that originated from file upload) — an uploaded file's name must never become a selectable parameter option. (C4) Aktivitas tab: the feeling-label text ("nyaman", "berat", etc.) renders too small — increase its font size to be consistent/readable with the rest of the list (match the surrounding body text size, ~14px).</action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | tail -8 && cd ../frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Obat/Cuci Darah entries check and uncheck without error; obat detail shows the real uploaded photo; lab parameter dropdown contains only real parameters (no uploaded filenames); aktivitas feeling text is readable; both builds pass.</done>
</task>

<task type="auto">
  <name>Task 10: /pengingat fixes — medication reminder image upload+preview, toast layout (P1, P2)</name>
  <files>frontend/components/pengingat/MedicationReminderForm.tsx, frontend/components/pengingat/ReminderDetailOverlay.tsx, frontend/components/pengingat/AddReminderSheet.tsx, backend/src/controllers/reminders.controller.ts, backend/src/lib/upload.ts, backend/src/services/reminders.service.ts</files>
  <action>(P1) When creating a medication reminder with an uploaded photo, the image is not stored/served correctly so the detail view shows alt text / a broken-image icon. Trace the upload path: confirm MedicationReminderForm sends the file as multipart to the reminders create/update endpoint, that multer stores it, that the stored path/URL is persisted on the reminder row, and that the endpoint returns a URL the frontend can load. Fix whichever link is broken so ReminderDetailOverlay renders the actual photo via a correct src (served from the backend upload location), with a graceful fallback when absent. Mirror the same image-serving approach used (or intended) for the medication log detail in Task 9 so both surfaces agree. (P2) The "reminder added" toast is visually broken/misaligned. Fix its styling (this project uses Sonner) so the toast box is clean and aligned — check for conflicting inline styles/custom toast markup and align it with the app's other toasts.</action>
  <verify>
    <automated>cd backend && npm run build 2>&1 | tail -8 && cd ../frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>A medication reminder created with a photo shows that real photo in its detail overlay; the reminder-added toast renders cleanly aligned; both builds pass.</done>
</task>

<task type="auto">
  <name>Task 11: Catat Cairan form — false-optional labels + Sumber dropdown options (F1, F2)</name>
  <files>frontend/components/cairan/CatatCairanForm.tsx, frontend/lib/validators/fluid.schema.ts, backend/src/services/fluid.service.ts</files>
  <action>(F1) Remove the "(opsional)" suffix from the "Sumber" label (currently line ~194-195 of CatatCairanForm.tsx) and from "Konsentrasi CAPD" when it is a required field. Broader audit: grep the frontend for "(opsional)" and remove it from any label whose field is actually required by its zod schema (Sumber and Konsentrasi CAPD are the known instances; leave genuinely-optional fields like Catatan untouched). (F2) Fix the Sumber (fluid-in) options. Current FLUID_SUMBER enum is ["urine","capd","lainnya"] and SUMBER_LABELS maps them — this is wrong. Required behavior: the Sumber dropdown (which applies to Cairan Masuk / fluid IN) must offer, for CAPD patients: Makanan, Minuman, Exchange CAPD, Lainnya; for non-CAPD patients: Makanan, Minuman, Lainnya. Urine must NEVER appear under Sumber (fluid IN) — it may only appear as a source under Cairan Keluar (fluid OUT). Implement by: adding "makanan" and "minuman" to the fluid sumber enum in BOTH frontend/lib/validators/fluid.schema.ts and the backend fluid.service.ts validation schema (keep "urine", "capd", "lainnya" too since urine is still valid for keluar); updating SUMBER_LABELS with Makanan/Minuman; and making the rendered option list depend on tipe (masuk vs keluar) and isCAPD — for tipe="masuk" show makanan/minuman/(capd if CAPD)/lainnya (never urine); for tipe="keluar" show urine/(capd if CAPD)/lainnya. Ensure the backend accepts the new enum values (no DB migration expected since sumber is stored as text — verify the column type; if it is a Postgres enum type, add a migration to extend it).</action>
  <verify>
    <automated>cd frontend && grep -c "makanan\|minuman" lib/validators/fluid.schema.ts && npm run build 2>&1 | tail -10 && cd ../backend && npm run build 2>&1 | tail -8</automated>
  </verify>
  <done>Sumber and required Konsentrasi CAPD labels no longer say "(opsional)"; Sumber options are correct per CAPD/non-CAPD and per masuk/keluar with Urine only on keluar; makanan/minuman accepted by both frontend and backend validation; builds pass.</done>
</task>

</tasks>

<verification>
- No check/uncheck action on /beranda or /catatan returns HTTP 500 (Task 1).
- Reminder due-firing and all displayed times honor the user's device timezone; zero "WIB" strings remain in frontend (Tasks 2, 3).
- Push notification received on a real device (Task 4 human-verify).
- A mutation on one of /beranda, /catatan, /pengingat updates the other two without reload (Task 5).
- backend `npm run build` + `npm test` pass; frontend `npm run build` passes after each code task.
</verification>

<success_criteria>
Every numbered item in the verbatim user report (1–5, B1–B5, C1–C4, P1–P3, F1–F2) is either fixed or, where already fixed by prior work, verified working. Item 1 (bell) is expected to be already wired — verified, not re-implemented. Item 3 (push) is fixed in code and confirmed live via the human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/quick/260705-9n4-fix-cross-page-bugs-notification-bell-li/260705-9n4-SUMMARY.md` when done, listing per-item disposition (fixed / already-working-verified / needs-follow-up) and the commit(s) for each task.
</output>
