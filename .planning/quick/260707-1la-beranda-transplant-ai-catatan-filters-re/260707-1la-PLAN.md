---
quick_id: 260707-1la
type: quick
mode: quick
autonomous: false
files_modified:
  # Task 1 — Beranda UI
  - frontend/app/(app)/beranda/page.tsx
  - frontend/components/anomaly/AnomalyAlertCard.tsx
  - frontend/components/beranda/HumanFluidChart.tsx
  # Task 2 — AI data/prompt
  - backend/src/services/aiInsight.service.ts
  - backend/src/services/aiLabAnalysis.service.ts
  - backend/src/services/aiLifestyle.service.ts
  - backend/src/controllers/ai.controller.ts
  - backend/src/routes/ai.routes.ts
  - frontend/components/edukasi/LifestyleSuggestionCard.tsx
  - frontend/components/lab/LabAnalysisCard.tsx
  # Task 3 — /catatan tabs + doctor report
  - frontend/app/(app)/catatan/page.tsx
  - frontend/components/lab/LabResultList.tsx
  - frontend/components/lab/LabTrendChart.tsx
  - frontend/components/lab/LabEditSheet.tsx
  - frontend/components/catatan/FluidLogList.tsx
  - frontend/components/aktivitas/ActivityList.tsx
  - backend/src/controllers/labResult.controller.ts
  - backend/src/routes/labResult.routes.ts
  - backend/src/services/labResult.service.ts
  - backend/src/controllers/fluid.controller.ts
  - backend/src/repositories/report.repository.ts
  - frontend/app/globals.css
---

<objective>
Batch of 13 PO-requested fixes/features across beranda pasien, AI features, /catatan tabs, and laporan dokter (caregiver doctor reports).

Purpose: Polish beranda for transplant patients, enrich AI insights with full /catatan data + solutions, replace lab archive with delete + add range filters across catatan tabs, add file-entry editing, and fix two doctor-report bugs (count inconsistency + print whitespace).
Output: 13 discrete changes grouped into 3 coherent tasks. Every sub-item is listed explicitly below so none are dropped.

**Global rules (apply to ALL tasks):**
- All UI text in Bahasa Indonesia awam.
- Follow DESIGN_SYSTEM_KidneyBuddy_v3.md tokens: teal `#2a9d8f`, amber `#ef9f27`, cream `#fdf9f3`, ink `#1a2e2c`, muted-but-readable `#3d6b66` (NOT the washed-out `#7a8c8a`/`#cfe8e4` for anything meant to be readable).
- **Backend container has NO nodemon** — after ANY edit under `backend/src/`, the executor MUST restart the backend container (or the local backend process) before verifying, otherwise changes won't take effect.
- Frontend runs via `next dev` (hot reload) — no restart needed for frontend-only edits.
- Run `tsc` (frontend + backend) clean before marking done.

**Range-filter design decision (shared by items 10, 12, 13):**
Use a single preset dropdown component with these options, default **"Semua data"**:
`Semua data` · `30 hari terakhir` · `90 hari terakhir` · `6 bulan terakhir` · `1 tahun terakhir`.
Map to a `days` value sent to the backend (`0`/omitted = semua, `30`, `90`, `180`, `365`). Keep the option labels and `days` mapping IDENTICAL across the Lab, Aktivitas, and Cairan tabs for consistency.
</objective>

<context>
@.planning/STATE.md
@./CLAUDE.md

Reference-only (patterns to reuse):
@frontend/components/pengingat/DeleteReminderConfirm.tsx   # confirm-dialog pattern for item 7
@frontend/components/lab/UploadFileForm.tsx                # file-form fields for item 11
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beranda UI fixes (items 1, 2, 3)</name>
  <files>frontend/app/(app)/beranda/page.tsx, frontend/components/anomaly/AnomalyAlertCard.tsx, frontend/components/beranda/HumanFluidChart.tsx</files>
  <action>
Three independent frontend-only beranda fixes.

**Item 1 — Transplant: widen "Obat Hari Ini" card.**
In `frontend/app/(app)/beranda/page.tsx`, "Row 3" (around lines 146-156) is a `md:grid-cols-3` row containing `KegiatanModuleInline`, `ObatCard`, and (for non-transplant only) `CuciDarahCard`. `isTransplant` is already computed at line 28. For transplant patients `CuciDarahCard` is hidden, leaving an empty third column. Fix: when `isTransplant`, make `ObatCard` span the remaining width so it equals the combined width of the obat+cuci-darah columns — wrap `ObatCard` in a `<div className={isTransplant ? "md:col-span-2" : ""}>` (KegiatanModuleInline keeps col-span-1). Net effect: transplant row = Kegiatan (1 col) + Obat (2 cols, full remaining). Verify no empty column remains and non-transplant layout is unchanged (3 equal cards).

**Item 2 — "Peringatan dari AI" font size matches "Ringkasan AI Hari Ini".**
The beranda "Peringatan" card renders via `AnomalyAlertSection` → `AnomalyAlertCard`. The alert description paragraph (`alert.deskripsi`) in `frontend/components/anomaly/AnomalyAlertCard.tsx` (around line 162-179) is `fontSize: 12`. The "Ringkasan AI Hari Ini" narrative (`AiDailySummaryCard.tsx`) uses `fontSize: 15`. Change the `deskripsi` paragraph to `fontSize: 15` so it matches. This card is also used on `/notifikasi` — scope the bump so it applies at least on the beranda usage; simplest acceptable approach is to bump the `deskripsi` fontSize to 15 for all uses (keep `lineHeight: 1.6`). Leave the timestamp/badge/title sizes untouched.

**Item 3 — "Keseimbangan Cairan Hari Ini": add normal-range caption.**
In `frontend/components/beranda/HumanFluidChart.tsx`, the overlay-text block (around lines 337-380) ends with the conditional `statusLabel`. The chart's own alert logic (lines ~16-18, 146-157) treats `-1000 ≤ delta ≤ 0` as normal (teal), `delta > 0` as "Cairan tertahan" (amber), `delta < -1000` as "Output berlebih" (red). Add a small caption directly BELOW the status label (still inside the `flex flex-col items-center` container, always rendered when `hasData`) explaining the normal range so the patient understands why the chart alerts. Suggested text: `"Rentang normal selisih harian: -1000 ml sampai 0 ml"`. Make it readable — `fontSize: 13`, `color: "#3d6b66"`, `marginTop: 8`, `textAlign: center`. Do NOT use `#7a8c8a`/`#cfe8e4` or a tiny font (PO explicitly said not too small, not faded).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
- Transplant beranda: Obat card fills the width previously occupied by Obat+CuciDarah; no empty column. Non-transplant unchanged.
- Peringatan dari AI description text renders at 15px (visually equal to Ringkasan AI narrative).
- Keseimbangan Cairan card shows a readable normal-range caption at the bottom.
- frontend tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: AI data/prompt enhancements (items 4, 5, 6)</name>
  <files>backend/src/services/aiInsight.service.ts, backend/src/services/aiLabAnalysis.service.ts, backend/src/services/aiLifestyle.service.ts, backend/src/controllers/ai.controller.ts, backend/src/routes/ai.routes.ts, frontend/components/edukasi/LifestyleSuggestionCard.tsx, frontend/components/lab/LabAnalysisCard.tsx</files>
  <action>
Backend AI service + prompt changes (RESTART BACKEND after edits), plus small frontend wiring.

**Item 4 — "Wawasan Tren Mingguan" must cover ALL /catatan data.**
`backend/src/services/aiInsight.service.ts` → `gatherWeeklyData` (lines ~56-90) already gathers: fluid masuk/keluar, CAPD effluent conditions, medication adherence, dialysis adherence (covers both CAPD exchange AND HD), and recent manual lab. The MISSING dimension is **aktivitas** (activity). Add activity data to the gather: use the activities repository/service to fetch this user's activities in the `start`..`end` window (see `backend/src/services/activities.service.ts` / `dailyActivity.repository.ts` for the query pattern — reuse an existing range/all query, filter to the WIB window). Summarize (e.g. count of activities logged, count completed/`selesai`). Then extend `buildWeeklyPrompt` (lines ~92-108) to include an activity line (e.g. `- Aktivitas: X kegiatan tercatat, Y selesai`). Keep the existing 5 dimensions. The prompt already asks for a calm 4-6 sentence insight + 1-2 concrete suggestions — leave that framing, just ensure activity is now part of the data it reasons over. Regenerate is exposed via `POST /api/ai/weekly-insight/regenerate` for testing.

**Item 5 — "Analisis Hasil Lab" must follow the displayed trend range + give food/activity solutions.**
Currently `GET /api/ai/lab-analysis/:labResultId` (`aiLabAnalysis.service.ts` → `generateAndCacheLabAnalysis`, lines ~183+) analyzes ONE lab result plus its same-parameter history. Two changes:
  (a) **Trend-range awareness:** the analysis must reflect the data currently shown in the trend chart, which varies with the range filter (item 10). Make the analysis range-aware: accept an optional `days` (or range) parameter through the endpoint → controller (`ai.controller.ts` getLabAnalysis) → service. When a range is provided, the same-parameter history fed to the prompt (the `sameParameter` fetch at lines ~195-203) must be constrained to that window so the narrative changes when the user changes the trend filter. Add the `days` query param to the route in `ai.routes.ts` (`GET /api/ai/lab-analysis/:labResultId?days=N`). NOTE: analysis is cached — ensure a changed range produces a fresh analysis rather than returning a stale cached one keyed only by labResultId (either include the range in the cache key, or force regeneration when `days` is passed). Keep the existing cache-on-miss behavior for the default (all-data) case.
  (b) **Solutions, not just reading:** extend the prompt (`buildLabAnalysisPrompt`, lines ~69-81) so that after describing the lab value(s) and trend, the AI ALSO gives concrete, non-diagnostic saran konsumsi makanan/minuman dan aktivitas yang membantu for a CKD patient (e.g. lower-potassium food choices, fluid guidance, gentle activity) — phrased as general lifestyle guidance, WITHOUT diagnosing. Keep the existing prompt-injection defense (structured fields only, no raw `catatan`) and the disclaimer append.
  Frontend: `frontend/components/lab/LabAnalysisCard.tsx` fetches `/api/ai/lab-analysis/${labResultId}` (line ~117). Add an optional `days`/range prop and include it in the fetch URL + effect deps so the card refetches when the trend range changes. (The catatan page will pass the current trend range into this card — see Task 3 item 10; wire the prop here so Task 3 can supply it.)

**Item 6 — "Saran Gaya Hidup untuk Anda" (Edukasi) based on ALL lab + activity, with a "berdasarkan …" note.**
`backend/src/services/aiLifestyle.service.ts` → `gatherLifestyleData` (lines ~64-101) currently uses only: therapy method + the SINGLE latest manual lab + fluid-tracking day count. Expand it to base suggestions on the whole lab picture AND activity: (a) include more than just the latest lab (e.g. the most recent value per distinct parameter, or the recent lab set within a lookback window) via `labResultRepo.findByUser`; (b) add an activity summary for the lookback window (reuse the same activities query as item 4). Update `buildLifestylePrompt` to feed these in and ask for suggestions grounded in lab + activity + fluid data. Then surface a "berdasarkan …" keterangan in the UI: in `frontend/components/edukasi/LifestyleSuggestionCard.tsx`, add a readable caption near the suggestion (e.g. below the narrative, `fontSize: 12`, `color: "#3d6b66"`) reading something like `"Berdasarkan: hasil lab terbaru, aktivitas, dan catatan cairan Anda."` — the caption may be static text describing the data basis (does not need to be AI-generated).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
- Weekly insight gather + prompt now include aktivitas alongside fluid/med/dialysis(CAPD+HD)/lab. `POST /api/ai/weekly-insight/regenerate` produces text that can reference activity.
- Lab analysis endpoint accepts a range/`days` param; changing the trend range yields a different (non-stale) analysis; prompt now includes food/drink/activity suggestions; LabAnalysisCard refetches on range change.
- Lifestyle suggestion is grounded in all-lab + activity + fluid data; card shows a readable "Berdasarkan: …" caption.
- backend + frontend tsc clean. Backend restarted before verification.
  </done>
</task>

<task type="auto">
  <name>Task 3: /catatan tabs (items 7, 10, 11, 12, 13) + doctor report fixes (items 8, 9)</name>
  <files>frontend/app/(app)/catatan/page.tsx, frontend/components/lab/LabResultList.tsx, frontend/components/lab/LabTrendChart.tsx, frontend/components/lab/LabEditSheet.tsx, frontend/components/catatan/FluidLogList.tsx, frontend/components/aktivitas/ActivityList.tsx, backend/src/controllers/labResult.controller.ts, backend/src/routes/labResult.routes.ts, backend/src/services/labResult.service.ts, backend/src/controllers/fluid.controller.ts, backend/src/repositories/report.repository.ts, frontend/app/globals.css</files>
  <action>
Largest task — 7 sub-items. Work through them in order. RESTART BACKEND after any `backend/src/` edit.

**Item 7 — Lab tab: remove archive, add delete-with-confirm.**
In `frontend/app/(app)/catatan/page.tsx` Lab tab (lines ~263-293): remove the "Lihat Arsip"/"Sembunyikan Arsip" toggle button, the `showArchivedLab` state, and the `<LabArchivedList>` block entirely. In `frontend/components/lab/LabResultList.tsx`: replace the "Arsip"/`handleArchive` action (button labelled "Arsipkan", lines ~74-88, ~189) with a "Hapus" button that opens a confirm dialog (reuse the `AlertDialog` pattern from `frontend/components/pengingat/DeleteReminderConfirm.tsx`); on confirm, delete the entry. Backend: add a real delete endpoint — `DELETE /api/lab/:id` in `backend/src/routes/labResult.routes.ts` → `labResultController.remove` in `backend/src/controllers/labResult.controller.ts` → `labResultService.deleteEntry` in `backend/src/services/labResult.service.ts`. A soft-delete (reuse the existing `archiveById` mechanism, since "Lihat Arsip" is now gone the archived rows are simply invisible) is acceptable and lower-risk than hard-delete; if hard-deleting, also remove any uploaded file. Keep `dispatchSyncEvent(SYNC_EVENTS.LAB_SAVED)` on success. Toast: "Hasil lab dihapus". (You may leave the now-unused `/archived` + `/restore` endpoints and `LabArchivedList.tsx` in the repo, or remove them — do not break other callers; grep first.)

**Item 10 — Lab tab: trend default = ALL data + range filters, with interlinked list/trend ranges.**
`frontend/components/lab/LabTrendChart.tsx`: the trend fetch (line ~97) hardcodes `&days=90` and the UI shows "— 90 hari terakhir". Change default to ALL data and REMOVE the "90 hari terakhir" label text. Add a range dropdown (the shared preset design from `<objective>`) controlling the `days` sent to `/api/lab/trend`. The backend `getTrend` (controller line ~304-321, service `getTrendData`) currently defaults `days=30`; ensure it supports "all" (omit/`0` = no lower bound) and the larger presets (90/180/365) — extend the repository `findTrendData` day-window accordingly. Also add a range dropdown to the lab RESULTS LIST (`LabResultList.tsx`), default "Semua data", filtering which entries display. Backend list (`GET /api/lab`, controller `list`) currently supports `?tanggal=` — add support for a range/`days` param (or filter client-side if simpler and data volume is small; prefer backend for correctness).
  **Interlink logic (implement by lifting both range states into `catatan/page.tsx`):** hold `labListRange` and `labTrendRange` state in the page. If `labListRange === "Semua data"`, all trend range options are selectable. If the user picks a bounded list range, constrain the trend dropdown options to ranges within (≤) the selected list range (e.g. list=90 hari → trend options limited to Semua-within-90/30/90; disable 6 bulan & 1 tahun). Pass the effective range into `LabTrendChart` and into `LabAnalysisCard` (Task 2 item 5 added the prop) so the AI analysis follows the displayed trend window.

**Item 11 — Lab tab: edit for file-based (upload) entries.**
Currently `LabEditSheet.tsx` explicitly does NOT support upload entries (renders only when `result.sumber !== "upload"`, LabResultList line ~177). Add editing for upload entries: user can change nama (display name), tanggal, and replace the document with a different one — same fields as `UploadFileForm.tsx`; on submit a file is REQUIRED (min 1 file). Backend: add an update-with-file endpoint, e.g. `PUT /api/lab/:id/upload` in `labResult.routes.ts` using `labFileUpload.single("file")` → controller → service that validates a file is present, saves the new file, deletes/replaces the old `fileId`, and updates `namaParameter`(display name) + `tanggalPemeriksaan`. Frontend: render an Edit affordance for upload entries too (either extend `LabEditSheet` with an upload variant, or a small dedicated upload-edit sheet) posting multipart to the new endpoint; enforce the min-1-file rule client-side before submit. NOTE: there is a known Docker-only-path bug flagged for `uploadLab.ts` (`UPLOAD_DIR_LAB` defaults to `/app/uploads/lab-files`) — if file save/serve fails when running outside Docker, fix `uploadLab.ts` to derive the dir the same way the static server does (mirror the earlier `upload.ts` fix from quick-260706-573).

**Item 12 — Aktivitas tab: range filter.**
`frontend/components/aktivitas/ActivityList.tsx` fetches `/api/activities/all` (line ~116). Add the shared range dropdown (default "Semua data"); when a bounded range is chosen, filter activities to that window. Prefer backend filtering (extend the activities all/list endpoint to accept `days`/range in `activities.controller.ts` + `activities.service.ts`); client-side filtering on the already-fetched `all` set is acceptable if simpler. Keep labels/`days` mapping identical to item 10.

**Item 13 — Cairan tab: range filter.**
`frontend/components/catatan/FluidLogList.tsx` fetches `/api/fluid/recent?days=7` (line ~93). Add the shared range dropdown (default "Semua data"). IMPORTANT: `fluid.controller.ts` `getRecent` currently CAPS `days` at 30 (`Math.min(parseInt(...) || 7, 30)`, line ~145) — raise/remove this cap so 90/180/365/all work (guard against absurd values but allow the presets). Keep labels/`days` mapping identical to items 10 & 12.

**Item 8 — Doctor report: reconcile "Kepatuhan Cuci Darah" vs "Kondisi CAPD" counts.**
ROOT-CAUSE FINDING (already investigated — confirm, then fix): the two numbers come from DIFFERENT tables in `backend/src/repositories/report.repository.ts`:
  - "Kepatuhan Cuci Darah" (`103 dari 120 sesi`) = `getDialysisAdherenceByRange` (lines ~166-191), counting rows in the **`dialysis_log`** table (scheduled CAPD-exchange / HD sessions; total=scheduled, confirmed=`status='dikonfirmasi'`).
  - "Kondisi CAPD" (`114x` total) = `getCAPDConditionsByRange` (lines ~203+), counting **`fluid_log`** rows where `tipe='keluar'` AND `kondisiKeluar` is set (sum of jernih+keruh+keruh_gumpalan+berdarah).
  These are logged independently (a fluid "keluar" entry with a condition is separate from confirming a scheduled dialysis reminder), so nothing forces 114 (effluent-condition entries) to equal 103 (confirmed sessions) or 120 (scheduled). Investigate the demo seed generator (`backend/src/seed/`) to confirm the two are generated independently. FIX so the report is konsisten / masuk akal — choose the cleanest of: (a) reconcile the demo seed so CAPD effluent-condition logs are generated ~1:1 with confirmed CAPD exchange sessions; and/or (b) clarify the report copy so it's unambiguous what each count measures (e.g. Kondisi CAPD subtitle "dari N catatan cairan keluar" vs Kepatuhan "dari N sesi terjadwal"), so the numbers no longer read as a contradiction. Aim for both numbers being self-explanatory and mutually consistent for a CAPD patient. Confirm the final numbers make sense in the regenerated Lukman report.

**Item 9 — Doctor report print: remove whitespace between header block and "Ringkasan Cairan".**
For Lukman (CAPD, no doctor note) the print preview shows a large empty gap after the header block (Nama/Metode Terapi/Periode/Dibuat) before "Ringkasan Cairan". Structure: `LaporanPreviewContent.tsx` (lines ~75-117) is `space-y-6`, header info → `<Separator/>` → optional doctor-note (skipped when empty) → `RingkasanCairan`. Print styles live in `frontend/app/globals.css` `@media print` (lines ~82-179); `.laporan-section-card` has `page-break-inside: avoid` (line ~120). Likely cause: the tall Ringkasan Cairan section can't fit after the header on page 1, so `page-break-inside: avoid` pushes the whole section to page 2, leaving page 1 mostly empty. Investigate in a real print preview (Lukman). Fix via print CSS so Ringkasan Cairan sits directly under the header: e.g. tighten print spacing (reduce `space-y`/margins in print), and/or allow the first/tall section to break across pages (relax `page-break-inside: avoid` for the first section, or add `page-break-after: avoid` on the header block so it stays with the following section). Verify the gap is gone in print preview and pagination of later sections is still clean.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
- Item 7: Lab tab has no archive UI; each entry has a "Hapus" button with a confirm dialog; confirming deletes (DELETE endpoint works); list refreshes.
- Item 10: Trend defaults to all data, no "90 hari terakhir" text, has a range dropdown; results list has a "Semua data"-default range dropdown; the two are interlinked per the rule; trend + LabAnalysisCard follow the chosen range.
- Item 11: Upload (file) entries have an Edit affordance; edit form allows changing nama/tanggal and replacing the file, requires ≥1 file on submit; backend update-with-file endpoint works and file serves correctly outside Docker.
- Item 12 & 13: Aktivitas and Cairan tabs each have the shared range dropdown (default "Semua data"); fluid days cap raised to support presets; filtering works.
- Item 8: root cause (two-table source) confirmed and reconciled so Kepatuhan Cuci Darah vs Kondisi CAPD counts are consistent/self-explanatory in Lukman's report.
- Item 9: print preview shows Ringkasan Cairan directly beneath the header block with no large gap; later-section pagination still clean.
- backend + frontend tsc clean. Backend restarted before verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 13 items across beranda, AI features, /catatan tabs, and doctor reports.</what-built>
  <how-to-verify>
1. **Beranda (transplant user)**: Obat Hari Ini card widens to fill the row (no empty column). Peringatan dari AI text is as large as Ringkasan AI. Keseimbangan Cairan shows a readable normal-range caption.
2. **AI**: Regenerate Wawasan Tren Mingguan — it references cairan, obat, cuci darah, aktivitas, and lab. Open Analisis Hasil Lab, change the trend range filter — analysis content changes and includes food/activity saran. Edukasi → Saran Gaya Hidup shows a "Berdasarkan: …" note and reflects lab+activity.
3. **/catatan Lab**: No "Lihat Arsip". Each lab entry has "Hapus" → confirm dialog → deletes. Trend has range dropdown (default shows all data, no "90 hari" text). Results list has "Semua data" dropdown; picking a list range constrains the trend options. A file-upload lab entry can be edited (nama/tanggal/replace file, file required).
4. **/catatan Aktivitas & Cairan**: each has a range dropdown defaulting to "Semua data" that filters the list (test 90 hari / 6 bulan).
5. **Laporan dokter (Lukman, CAPD)**: Kepatuhan Cuci Darah and Kondisi CAPD numbers are consistent/clearly labelled. Print preview (Cetak/Simpan PDF): Ringkasan Cairan sits directly under the header — no big empty gap.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any item still wrong (reference its number).</resume-signal>
</task>

</tasks>

<success_criteria>
All 13 PO items implemented against the real files named above; backend restarted after backend edits; frontend + backend tsc clean; human verification of the batch passed.
</success_criteria>

<output>
Create `.planning/quick/260707-1la-beranda-transplant-ai-catatan-filters-re/260707-1la-SUMMARY.md` when done, listing each of the 13 items with its resolution and the files touched.
</output>
